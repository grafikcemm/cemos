# DECISIONS — CemOS Rebuild (append-only ADR)

> Her karar: bağlam → karar → gerekçe → kanıt/kaynak. Append-only; bir karar değişirse yeni ADR eklenir, eski SUPERSEDED işaretlenir. Tarih formatı mutlak.

---

### ADR-001 — Branch: `feature/cemos-rebuild`
**2026-07-15.** Mevcut `feature/ui-dark-redesign` HEAD (`f8b72b8`) üzerinden yeni branch açıldı. Push YOK. **Gerekçe:** dark-redesign primitive'leri + baseline'ı temel almak; kullanıcı onayı olmadan yayına çıkmamak. **Kanıt:** `git checkout -b` çıktısı "Switched to a new branch".

### ADR-002 — Untracked dosya kaderi
**2026-07-15.** `docs/CEMOS-CLAUDE-CODE-MASTER-REBUILD-PROMPT.md` Faz 0 doküman commit'ine dahil edilir. `shots/` kanıt galerisi olarak **untracked bırakılır**; kullanıcı izni olmadan `.gitignore` değiştirilmez. **Gerekçe:** master prompt bağlayıcı kaynak, versiyonlanmalı; ekran görüntüleri büyük binary, repo'yu şişirir ama kanıt olarak diskte kalmalı.

### ADR-003 — Açık editorial tema, token isimleri stabil
**2026-07-15.** Varsayılan tema koyu dashboard'dan **açık editorial**'e geçer (master prompt emri). `globals.css` token DEĞERLERİ flip edilir, token İSİMLERİ korunur → 30 primitive kırılmadan çalışır. **Gerekçe:** isim-stabil swap, component başına refactor riskini ortadan kaldırır. **Kanıt:** `src/components/ui/` 30 primitive tümü `var(--token)` tüketiyor (keşif).

### ADR-004 — Vurgu rengi: tasarım onay kapısında dondurulur
**2026-07-15.** Turuncu (`#ff5538` ailesi) yalnız İLK HİPOTEZ; kesin karar değil. Faz 0 tasarım araştırması 3 accent adayı + WCAG AA kontrast tablosu üretir; kullanıcı tasarım onay kapısında dondurur. **Gerekçe:** kullanıcı v2/v3 geri bildiriminde "turuncuyu önceden verilmiş karar sayma" dedi. **Durum:** design research sonucu 06 spec'e işlenecek; kullanıcı seçecek.

### ADR-005 — IA: 3 görev (Bugün/Plan/Kütüphane) + Toolbox + Profil
**2026-07-15.** Ana nav 3 göreve iner; Toolbox hızlı utility; Sistem/Maliyet/Ayarlar/Memory/Entegrasyonlar Profil menüsünde. Legacy motor adları (Haber/YouTube/Viral Radar/Keşif) alt-nav'dan çıkar → Plan/Fırsatlar'ı besleyen arka plan. **Gerekçe:** master prompt "ana nav yalnız görev göstersin"; canlı denetim ~16-item sidebar'ı doğruladı (karmaşıklık kanıtı). **Kanıt:** `shots/cemos-rebuild/baseline/prod-1280-morning-styled.jpeg`.
> **REVİZE — ADR-040 (2026-07-19):** "3 görev" kararı GEÇERLİ ama artık "yalnız üç **TOP-LEVEL** alan" olarak okunur. Kabiliyetler saklanmaz: aktif alan sidebar'da alt hedeflerini açar, "Araştırma" grubu (eski motor adları) sidebar'da hiyerarşik + keşfedilebilir, "Şimdi" gerçek-veri özeti Profil üstünde. Minimalizm = az top-level; işlev saklama veya boş ray DEĞİL.

### ADR-006 — Legacy ekran: iki sınıf (ABSORBED / REDESIGNED-ADVANCED), üçüncüsü yok
**2026-07-15.** Kullanıcı-erişilebilir HİÇBİR ekran legacy kompozisyonla kalamaz. Her ekran ya ABSORBED (erişimden çıkar, id alias'la yeni eve) ya REDESIGNED-ADVANCED (araştırma detayı, tam yeniden tasarlanır). "Minimum token uyumu" ara sınıfı YASAK. **Gerekçe:** kullanıcı v3 geri bildirimi — "erişilebilir ekran = uygulamanın parçası, legacy kalamaz". Nihai tablo: `04-COMPLETE-UI-REDESIGN-PLAN.md`.

### ADR-007 — Store migration v9: yalnız ABSORBED id'ler taşınır
**2026-07-15.** `migrations.ts`'e additive v9. YALNIZ ABSORBED `activeTab` değerleri yeni evlere migrate edilir. REDESIGNED-ADVANCED id'ler (`news-pool`,`youtube`,`flow-radar`,`discovery-engine`,`source-intelligence`) DEĞİŞMEZ — persist `youtube` yeni YouTube araştırma ekranını açar. `"xagent-store"` anahtarı DEĞİŞMEZ (rename = kullanıcı state kaybı). **Gerekçe:** kullanıcı düzeltmesi — advanced ID'leri migrate etmek onları erişilemez/yanlış-açar yapardı. **Kanıt:** `xagent.ts:358` activeTab persist; `migrations.ts` v8 mevcut.

### ADR-008 — Readiness sözleşmesi: `ready/needs_edit/blocked`, fail-closed, persist EDİLMEZ
**2026-07-15.** Zorunlu kozmetik edit-gate (`publishService.ts:97-104`) kaldırılır; yerine readiness. Persist edilmez; yayın anında `editedContent ?? content` üzerinden YENİDEN hesaplanır. `judged=false`/skor eksik → ASLA ready (fail-closed → needs_edit). `blocked` yalnız güvenlik/ciddi doğruluk/kaynaksız somut iddia/policy; stil kusuru blocked yapmaz. Soru-CTA/emoji kuralları hesap-bazlı (`accounts.ts`), global yasak yok. **Gerekçe:** master prompt + kullanıcı fail-closed düzeltmesi. `applyQualityGate` SARILIR, değiştirilmez.

### ADR-009 — Yabancı sızıntı: oran + allowlist (kelime-özel değil)
**2026-07-15.** "Trajectory" gibi sızıntılar için kelime-özel kontrol YAZILMAZ; Türkçe-dışı token oranı + marka/teknik terim allowlist'i (`AI`, `prompt`, ürün adları). **Gerekçe:** kullanıcı düzeltmesi — tek kelime kontrolü kırılgan, genellenebilir tespit gerekir. **Kanıt:** canlı taslakta "yörüngedir" (Trajectory çevirisi) gözlendi.

### ADR-010 — Thread: `threadSegments` typed alan Faz 1C'de, fail-closed
**2026-07-15.** `QueueItem.threadSegments` typed additive JSON alanı + basit segment editörü **Faz 1C'de** eklenir. Yapısal segment verisi olmayan thread taslağı fail-closed `needs_edit`; metindeki `1/` kanıt sayılmaz. **Faz 2 tekrar typed alan EKLEMEZ** — yalnız üretim hattı segment üretimi + backfill + kalibrasyon. **Gerekçe:** kullanıcı "belirsiz bırakma, seçenek seç" → seçenek 1 (alanı Faz 1C'ye al). Kullanıcının son yorumu bu düzeltmeyi bağladı.

### ADR-011 — Publish state machine: additive `PublishAttempt`, intent ≠ published
**2026-07-15.** Mevcut `PublishLog` prepared için ZORLANMAZ (success default true, publishedAt now, queueItemId yok → günlük limit sorgularını kirletir). Additive `PublishAttempt` modeli (prepared|succeeded|failed, adapter, idempotencyKey, contentHash, readinessPolicyVersion, readinessSnapshotJson, externalId, `@@unique([accountId,adapter,idempotencyKey])`). Intent = yalnız `PublishAttempt(prepared)`; PublishLog+PublishedPost yalnız manuel "Paylaşıldı" onayı VEYA API başarısı (aynı transaction). Ağ çağrısı transaction dışında; idempotent reconciliation. **Gerekçe:** kullanıcı düzeltmesi — intent açmak yayın başarısı değildir; PublishLog modeli prepared'a uygun değil. **Kanıt:** `prisma/schema.prisma` PublishLog (success bool, publishedAt now).

### ADR-012 — Doğrulama ≠ fact-check: 5 durum
**2026-07-15.** UI doğrulama durumu ayrık: `verified | partially_verified | source_available | unverified | stale`. `SourcePost.scannedAt` iddia doğrulama tarihi olarak GÖSTERİLMEZ. "fact-check tamamlandı" dili yalnız `verified`. **Gerekçe:** kullanıcı düzeltmesi — kaynak varlığı ≠ doğruluk kontrolü.

### ADR-013 — Auth: `proxy.ts` + ayrı hash+secret + HMAC'li kalıcı throttling
**2026-07-15.** Next 16 middleware = `proxy.ts` (yerel doc doğrulandı). `ACCESS_PASSWORD_HASH` (scrypt) ≠ `SESSION_SECRET` (cookie HMAC). Throttling: DB-backed atomik sayaç, ham IP saklanmaz (`HMAC(ip, SESSION_SECRET)`), güvenilir Vercel forwarding zincirinden IP, TTL+sweep, IP-sınır + global saldırı penceresi, başarıda sıfırlama. Prod'da env eksikse fail-closed. sameOriginGuard CSRF olarak kalır — **same-origin ≠ authentication**. **Gerekçe:** master prompt P0 + kullanıcı sertleştirme düzeltmesi; canlı denetim auth'suz erişimi doğruladı. **Kanıt:** `node_modules/next/dist/docs/.../proxy.md`; `shots/cemos-rebuild/baseline/`.
**[USER-ACTION]** Vercel Firewall/WAF (rate-limit) operasyonel öneri — kod-üstü ek katman.

### ADR-014 — 3 katmanlı sağlık
**2026-07-15.** `healthService` → altyapı (DB/cron/API/credential) + pipeline tazeliği (haber/failed-backlog/staleness/son-üretim) + bugünkü hazırlık (hazır içerik/karar-bekleyen). "Kuyruk tamamlandı" ≠ sağlıksız. Topbar yalnız kullanıcı-müdahalesi-gereken. **Gerekçe:** kullanıcı düzeltmesi — canlı "Sağlıklı" ile gerçek backlog karışabilir; tamamlanma ≠ üretim-hiç-olmadı.

### ADR-015 — X API: REQUIRES-USER-PAYMENT-APPROVAL (teknik FEASIBLE)
**2026-07-15.** Resmî developer.x.com/docs.x.com araştırması: `POST /2/tweets` OAuth2 PKCE user-context ile fizibil; scope `tweet.read tweet.write users.read offline.access`; rate limit 100/15dk-per-user (bağlayıcı değil); politika insan-onaylı özgün postu izin veriyor. **ENGEL:** yeni geliştiriciye free tier YOK; pay-per-use kredisi gerçek ödeme gerektiriyor. **Maliyet senaryosu** (resmî orandan türetildi — $0.015/post, **link'li $0.20/post**, okuma $0.005/post; 5-8 post/gün × 30 gün): %0 link **$2.25–3.60/ay** · %50 link **$16.13–25.80/ay** · %100 link **$30–48/ay**. Ek: okuma maliyeti, media/metadata işlemleri (resmî tabloda ayrı kalem yok — açık), retry ihtimali; fiyatlar değişebilir → güncel fiyat **Developer Console**'dan doğrulanır. Bu "yeni ücretli servis" dur-kuralına girer → Ali Cem onayı olmadan gerçek entegrasyon BAŞLAMAZ. **Açık mühendislik riskleri (blocker değil, tasarlanacak):** (1) sunucu-taraf idempotency YOK → client-side dedup; (2) belgesiz duplicate-content 403 penceresi. **Kanıt:** [docs.x.com/x-api/getting-started/pricing](https://docs.x.com/x-api/getting-started/pricing), [create-post](https://docs.x.com/x-api/posts/create-post), [OAuth2 PKCE](https://docs.x.com/fundamentals/authentication/oauth-2-0/authorization-code), [rate-limits](https://docs.x.com/x-api/fundamentals/rate-limits) (erişim 2026-07-15). Detay: `03-DELIVERY-ROADMAP.md` + `02-AGENT-MEMORY-DATA-ARCHITECTURE.md` publish bölümü.
**Sonuç:** Faz 1E'de adapter CONTRACT + intent fallback tamamlanır; gerçek `XApiPublishAdapter` yalnız kullanıcı maliyet onayı sonrası. Onaya kadar `BLOCKED-EXTERNAL`.

### ADR-016 — Dinamik hesap kaynağı: Faz 2'ye ertelenir (Faz 1 minimal)
**2026-07-15.** Hesaplar `src/lib/accounts.ts`'te hardcoded (literal union `grafikcem|maskulenkod`). Master prompt "DB/config'den dinamik oku" istiyor. **Karar:** Faz 1'de UI hesap filtreleri `Account` DB tablosundan okunabilir (mevcut model), fakat persona/mode source-of-truth Faz 2'ye kadar TS profillerinde kalır (literal cast genişletilir, hardcode 2-hesap varsayımı UI'dan kalkar). **Gerekçe:** tam dinamikleştirme veri-modeli işi; Faz 1 çekirdeği bloklamamalı. **Risk:** `publishService.ts:156` literal cast — Faz 1'de genişletilir.

### ADR-017 — Tasarım onayı + bağlayıcı kullanıcı kararları (Faz 0 kapısı)
**2026-07-15.** Kullanıcı (Ali Cem) tasarım yönünü ONAYLADI. Bağlayıcı kararlar:
1. **Accent = Terracotta/Rust `#A8481F`** (indigo/plum elendi). Açık zeminde ve beyaz metinle AA geçer.
2. **X API ödemesi ONAYLANMADI.** Faz 1E gerçek X API çağrısı YAPMAZ — yalnız `PublishAdapter` contract + `PublishAttempt` state machine + `IntentPublishAdapter` + dürüst intent fallback tamamlanır. `XApiPublishAdapter` gerçek bağlantı **BLOCKED-EXTERNAL** kalır.
3. **Publish CTA (mevcut ürün durumu):** ready kartın birincil CTA'sı **"X'te aç"** (intent) — "Onayla ve yayınla" GÖSTERİLMEZ. Intent açıldıktan sonra kart durumu **`publish_prepared`**; kullanıcı geri döndüğünde ayrı **"Paylaşıldı olarak işaretle"** eylemi. Bu manuel onay olmadan PublishLog/PublishedPost/`manual_published` YAZILMAZ. "Onayla ve yayınla" spec'te yalnız **gerçek API bağlı** koşullu varyant; blocked-external mockup'ında görünmez.
4. **Dil:** "X'e otomatik yayın" → "**CemOS içinden doğrudan yayın**" (her yerde). CemOS'ta otomatik yayın YOKTUR.
5. **Doğrulama:** kart ve drawer aynı QueueItem için birebir aynı 5-durum (`verified`/`partially_verified`/`source_available`/`unverified`/`stale`); "kaynak mevcut" ≠ "iddia doğrulandı".
6. **WCAG:** kontrast tabloları gerçek WCAG formülüyle yeniden hesaplandı (ADR-018); token contrast testi Faz 1A kapsamında.
7. **Kısıt:** push/deploy/production migration/ücretli servis YOK. Phase 0 belgeleri + master prompt `feature/cemos-rebuild`'e commit edilir; `shots/` untracked kalır. Faz 1A→1F sırayla, her dilim ayrı commit + test/typecheck/lint (UI'da +build/playwright/console).

### ADR-018 — WCAG kontrast düzeltmesi (gerçek hesap)
**2026-07-15.** Faz 0 06-spec'teki bazı kontrast değerleri el-hesabı hatalıydı (text-muted 4.6:1 iddia → gerçek 3.39:1; warn chip 3.29:1; ok chip 4.30:1 — 12-16px normal metinde AA-altı). **Düzeltme (gerçek WCAG 2.1 relative-luminance):** `--text-muted` `#8A857D`→**`#726C64`** (bg-base'de **4.80:1** AA); ok chip metni `#256B44` (tint'te **5.50:1**); warn chip metni `#8A5A08` (tint'te **5.05:1**); err chip `#B3261E` (**5.33:1**, korunur). `--text-faint` yalnız dekoratif/disabled — anlam taşıyan metinde KULLANILMAZ. Accent `#A8481F` korunur (bg **5.42:1**, beyaz metin **5.86:1**). Tam tablo `06-DESIGN-SYSTEM-SPEC.md` §2. **Faz 1A:** `theme-tokens.test.ts` gerçek kontrast oranını otomatik doğrular (metin/zemin çiftleri ≥4.5:1).

### ADR-019 — Faz 1B: 3-görevli shell + store v9 + sınıflandırma uygulaması
**2026-07-15.** IA rebuild kod düzeyinde uygulandı.
1. **3 birincil alan:** `PRIMARY_AREAS` = Bugün[morning] · Plan[plan-takvim/plan-firsatlar/plan-seriler] · Kütüphane[lib-tumu/lib-ilham/lib-ogrenme]. Alt-navigasyon workspace'te `SubNav` (Plan/Kütüphane çok-sekmeli; Bugün tek-sekme → subnav yok).
2. **Toolbox = utility, ayrı;** `UTILITY_TABS=[toolbox]`. **costs/system/settings ana navdan Profil menüsüne taşındı** → `PROFILE_TABS` = CemOS'un bildikleri(profile-memory) · Entegrasyonlar(profile-integrations) · Sistem · Maliyet · Ayarlar. Profil = sidebar tetikleyici → yukarı açılan `ProfileMenu` popover (5 yüzey + Çıkış); Profil yüzeyi aktifken workspace'te 5-yüzey `SubNav`.
3. **Collapse KALDIRILDI** (06 §7: "3 item icon-rail'i hak etmez" + onaylı mockup'larda collapse kontrolü yok). Desktop sidebar sabit 232px; ≤640 bottom-nav'a devreder. Bilinçli sadeleştirme — regresyon değil; gerekirse önemsiz eklenir. `COLLAPSE_KEY`/collapsed state silindi.
4. **ABSORBED (TAB_ALIASES + v9 migration):** `daily-queue`→morning, `viral/keyword/prompt/pattern-library`→lib-tumu, `learn-dashboard`→lib-ogrenme, `instagram`→plan-seriler. Eski ekran bileşenleri (ViralLibraryTab…) SİLİNMEZ (emeklilik Faz 4) ama registry'den çıkar → kullanıcı erişimi kesilir.
5. **REDESIGNED-ADVANCED (alias'lanMAZ, v9 dokunmaz):** `news-pool`/`youtube`/`flow-radar`/`discovery-engine`/`source-intelligence` → `ADVANCED_TABS`, `parentArea=plan`. Sidebar'da yok; Cmd+K + deep-link + (Faz 1D) Fırsatlar'dan açılır. Aktifken Plan highlight'lı, breadcrumb "Plan / X", subnav yok.
6. **Store v9:** yalnız ABSORBED `activeTab` yeni eve taşınır; advanced/utility/profile id'leri pass-through. `XAGENT_STORE_NAME="xagent-store"` const'a alındı (legacy invariant — değeri değişmez, test garantisi).
7. **Yeni host ekranları (plan-*, lib-*, profile-memory, profile-integrations):** Faz 1B'de dürüst `HostPlaceholder` (05 empty-state; ölü CTA yok). Tam kompozisyon Faz 1D. Eski dashboard kompozisyonu KOPYALANMADI.
8. **Varsayım:** `lib-ogrenme` her zaman görünür (Öğrenme birinci-sınıf Kütüphane alt-alanı); `NEXT_PUBLIC_LEARN_ENABLED` gate'i Learn ÖZELLİĞİNE taşınır (Faz 1D).
9. **E2E harness sertleştirme:** cold Next-dev ilk-compile 30s'yi aşabildiğinden `global-setup` warmup GET + retry + 120s timeout ile güçlendirildi.

### ADR-020 — Tema kararı DEĞİŞTİ: DESKTOP DARK EDITORIAL (ADR-017 §tema'yı supersede eder)
**2026-07-15 (kullanıcı kararı, Faz 1B sonrası).** Kullanıcı önceki "açık editorial" tema kararını (ADR-017 madde-örtük + ADR-018 açık kontrast tablosu) **değiştirdi**. ADR-017/018 append-only kayıt olarak KALIR (silinmez); tema yönü bu ADR ile güncellenir.
1. **CemOS yalnız DESKTOP.** Mobil artık ürün kabul kriteri değil.
2. **Tek tema = DARK EDITORIAL** (sıcak antrasit/kömür). Light/dark toggle YOK. `color-scheme: dark`.
3. **Terracotta accent KORUNUR** (marka). Eski CemOS mor/violet/neon/glow/glassmorphism YASAK; eski dark dashboard token'ları geri gelmez.
4. **Palet (Faz 1B.5'te uygulandı, gerçek WCAG ile ayarlandı):** bg-base `#101114` · workspace `#131519` · rail `#0D0F12` · sunken `#0D0F13` · surface `#191C21` · elevated `#20242A` · hover `#242830` · border `#2B3038` · border-strong `#3A404A` · text-primary `#F2EFE8` · text-secondary `#B8B2A8` · text-muted `#918B82` · text-faint `#6A655E` (dekoratif) · **accent split:** solid-fill `#A8481F` (+beyaz fg 7.5:1) / text-icon-link-focus `#E4865E` (dark bg'de ~6.6:1). Durum/chart metinleri dark'ta açık varyant (status-error `#E5595E`, ok-text `#56CB8C`, warn-text `#E0A94A`; chart açık terracotta/nötr). Token İSİMLERİ değişmedi — yalnız değerler + dark elevation (siyah gölge, glow yok).
5. **Focus ring** dark'ta `accent-text` (#E4865E, ≥3:1 UI eşiği) — solid accent çok koyu (2.5:1) olduğu için.
6. **Desktop içerik genişliği varyantları:** `--content-reading` 960 · `--content-standard` 1080 · `--content-wide` 1280. Shell ekran-bazlı uygular (Bugün/Toolbox/Profil=standard; Takvim/Kütüphane/advanced=wide); `PageScaffold width` prop reading/standard/wide ile ekran daha da daraltabilir. Hiçbir ekran 1920'ye yayılmaz (max wide 1280, ortalı).
7. **Desktop destek sözleşmesi:** birincil 1280/1440/1920; minimum graceful 1024; **1024–1920 yatay taşma yok**. Eski "1280+390" ve "320–1440" kriterleri bununla değiştirildi (docs güncellendi).
8. **MobileNav + responsive kod** silinmedi — "best-effort / mevcut compatibility". Mobil başarısızlığı desktop ürününün tamamlanmasını engellemez; yeni 390 screenshot/mobil-özel component üretilmez. 05 spec mobil wireframe'leri tarihsel tasarım kaydı (implementasyon zorunluluğu değil).
9. **`theme-tokens.test.ts`** dark'a güncellendi: gerçek WCAG (metin ≥4.5:1, focus ≥3:1), dark-tema-kilidi (yüzey relLum düşük / metin yüksek — açık-temaya dönüş engeli), açık-tema literal regression bans (#f7f6f2…), mor/neon/glow bans korunur. Salt-hex listesi değil — kontrast hesabı gerçek.

### ADR-021 — Kullanıcı-sağlanan desktop dashboard referansı = CemOS'un bağlayıcı görsel yapısı
**2026-07-15 (kullanıcı kararı, Faz 1C öncesi).** Kaynak görsel: `C:\Users\alice\Desktop\8712256020b90f2075e851cdbe1c0b83.jpg` (Rebaid dashboard). ADR-020 dark editorial yönünü **somutlaştırır** (supersede etmez); shell geometrisi + yüzey hiyerarşisi bu referansa yakından hizalanır.
1. **Yapı yakından uygulanır; marka/içerik KOPYALANMAZ.** Rebaid logosu, "Need help" kartı, sahte chart/KPI/avatar üretilmez. CemOS'un gerçek görevleri aynı tasarım sistemine yerleşir.
2. **Shell:** near-black rail (`bg-rail #0D0F12`, `--sidebar-w` 244) + sıcak grafit workspace (`bg-workspace`, float + border, büyük yüzey farkı). İçerik keskin ortalı beyaz panel gibi görünmez.
3. **Sidebar:** logo → **hesap bağlam kartı** (avatar + @handle + "Aktif hesap" + kanal değiştirici) → Bugün/Plan/Kütüphane. **Aktif nav = nötr grafit dolgu** (`bg-elevated`); terracotta yalnız ikon/indicator vurgusu (satırı boyamaz). Toolbox ayrı utility; Profil dipte.
4. **Workspace header:** büyük sayfa başlığı (`PageHeader size="hero"` ~28px, açıklama altında) → segmented pill subnav → gövde. Breadcrumb TopStrip'te ikincil; arama/Cmd-K + sağlık sağ üstte (küçük).
5. **SubNav:** segmented pill rail (koyu gömük rail + açık grafit aktif kapsül; terracotta yalnız aktif ayrıntı).
6. **Kontrollü açık-ada yüzeyleri:** koyu kanvasta ivory (`--inverse-*`) + peach (`--peach-*`) KARAR kartları. Yeni token ailesi (globals.css + @theme mirror), gerçek WCAG (theme-tokens.test): ivory/peach üstünde koyu metin ≥4.5:1, adalar açık kalır (dark-kilit istisnası). **Kullanım:** ready DraftReviewCard = ivory; needs_edit = peach; blocked = koyu hata-tint; kuyruk/ikincil = koyu. Her kart ivory YAPILMAZ, sayfa light'a çevrilmez. `Surface`/`InverseCard`/`PeachCard` primitive'i (ton renkleri `--sf-*` CSS var ile çocuklara akar); hardcoded hex yok.
7. **Bugün ekranı Faz 1D için design archetype'tır** — tüm yüzeyler bu dile taşınacak.

**Yayın sözleşmesi düzeltmesi (bağlayıcı, IMPLEMENTATION-STATE'in "needs_edit → izin ama uyarı" yorumu YANLIŞTI — uygulanmadı):**
- **ready:** intent açılabilir + manuel "Paylaşıldı" onayı yapılabilir (düzenleme ŞART değil).
- **needs_edit:** yayınlanamaz; "Düzenle" ana CTA; intent oluşturulmaz; PublishLog/PublishedPost oluşmaz; servis `edit_required` + nedenler döner.
- **blocked:** yayınlanamaz; intent yok; servis `readiness_blocked`; route 422 + Türkçe nedenler.
- İnsan onayı kalite/policy kapısını SESSİZCE bypass etmez; kullanıcı metni düzenleyerek ready yapar. Kozmetik `edited !== original` edit-gate'i KALDIRILDI; `markManualPublished` yayın anında `editedContent ?? content` üzerinde readiness'i yeniden koşar.

**USER-DB-ACTION (BLOCKED-EXTERNAL değil):** `QueueItem.threadSegments` additive migration'ı (`prisma/migrations/20260716000000_add_thread_segments`) üretildi + `migrate diff` ile additive kanıtlandı ama canlı DB'ye **UYGULANMADI** (kısıt: prisma db push yok). Sonuç: daily-queue gerçek istekte "column does not exist" → 500; Bugün ekranı **graceful ErrorState** gösterir (sessizce gizlenmez). Kullanıcı migration'ı uygulayınca (dev/prod DB'ye `prisma migrate deploy` veya eşdeğeri) gerçek veri akar. Kart tonları mock-preview ile canlı doğrulandı (`shots/faz1c-cards`).

### ADR-022 — Faz 1C.1: referans görsel sadakat + DB migration uygulaması
**2026-07-15 (kullanıcı: mevcut tasarım referansa yeterince benzemiyordu — DOĞRU kabul edildi; ayrıca additive DB migration yetkisi verildi).** ADR-021 yönünü sadakatle uygulayan dilim; Faz 1D'den önce.

**Gap matrix (referans → önceki CemOS → değişiklik):**
| Alan | Referans | Önceki | Değişiklik |
|---|---|---|---|
| Wordmark | Belirgin lockup + mark | Terracotta kare + basit X | Özgün rounded C-arc+node mark + "Cem"/"OS" accent wordmark |
| Font | Geometrik yuvarlak sans | Inter | Plus Jakarta Sans (next/font, variable, latin-ext) |
| İkon | Tek tutarlı outline aile | Ham Lucide, karışık boyut | AppIcon primitive (tek optik boyut/stroke/hizalama) |
| Aktif nav | Nötr grafit slab | Nötr ama dar | Ferah slab (.cx-nav-item, CSS :hover) |
| Hesap kartı | Avatar+ad+rol | Monogram düz | Gradient monogram, işlenmiş kart |
| Kart | Divider + meta ritim | Düz | Başlık/gövde ince ayraç |

**Bilinçli KOPYALANMAYAN (CemOS kimliği korunur):** Rebaid logo/wordmark/metin, "Need help"/Blog/Contact kartları, sahte chart/KPI/dekoratif avatar, influencer satırı, dil-toggle. Referans = yapı/his; marka/içerik değil.

**Font:** Plus Jakarta Sans (variable, latin-ext = TR glyph İıŞşĞğÇçÖöÜü). `--font-inter`→`--font-app-sans`; Newsreader app-yüzeyinde SIFIR; IBM Plex Mono (teknik string) korunur. Computed font-family + gerçek glyph render canlı doğrulandı.

**İkon:** tek aile = Lucide + `AppIcon` primitive (sm15/md18/lg20, stroke 1.75, aktif=accent-text). Yeni bağımlılık (phosphor) EKLENMEDİ — disiplinli Lucide referans kalitesini karşıladı; `fonts-icons.test` tek-aile kilidi (phosphor/heroicons/react-icons importu = 0).

**Sidebar:** ortak CSS sınıfları (`.cx-sidebar/.cx-nav-item/.cx-account-card`, CSS `:hover` — JS hover kalktı); özgün mark; işlenmiş hesap kartı; nötr slab nav; dikey ritim + grup ayırıcı.

**DB (kullanıcı yetkisiyle uygulandı; additive-only, non-destructive):** Canlı Neon DB `db push` origin (`_prisma_migrations` YOK, `migration_lock.toml`=sqlite → provider-uyumsuz; `migrate deploy` uygulanamaz). Additive DDL (`ADD COLUMN`/`CREATE TABLE`/`CREATE INDEX`, hepsi IF NOT EXISTS) `prisma db execute` ile uygulandı — idempotent, DROP/TRUNCATE/ALTER TYPE yok. `QueueItem.threadSegments` YENİ (→ daily-queue 500 gitti, gerçek istek 200 + 2 taslak, readiness/whyToday dolu) + `AuthAttempt` zaten vardı (no-op). Read-only doğrulandı (kolon + tablo + indexler). Migration geçmişini postgres'e baseline'lamak (potansiyel destructive) YAPILMADI — ayrı kullanıcı kararı.

**Doğrulama/gate:** 1024/1280/1440/1920 taşma 0; 0 console error; gerçek veri. Kanıt `shots/cemos-rebuild/faz1c1-fidelity/` (01-08; 01/02 gerçek veri, 08 fixture harness — gerçek kanıt değil). typecheck · unit 1316 · lint 0 · build · e2e 27.

### ADR-023 — Faz 1C.2: shell + DB kapanış düzeltmeleri (PostgreSQL baseline, tek sistem sağlığı, kart eylem sadeliği)
**2026-07-16 (kullanıcı: 1C.1 sonrası kapanış dilimi; metadata/baseline yetkisi standing; Faz 1D onaysız YOK).** 5 odak: (A) PG migration baseline, (B) sidebar profil viewport, (C) tek sistem-sağlığı, (D) ready kart eylem sadeliği, (E) hesap değiştirici popover + (§8E) stale/readiness dili.

**A — PostgreSQL migration baseline (non-destructive, P3019 kalıcı çözüldü):** Read-only kanıt `migrate diff --from-schema-datasource → --to-schema-datamodel` = **"No difference detected"** (canlı şema = datamodel). Tek postgres baseline `prisma/migrations/0_init` üretildi (`--from-empty --to-schema-datamodel`, 71 tablo = 71 model; TIMESTAMP(3)/DOUBLE PRECISION, 0 PRAGMA/DATETIME). `migration_lock.toml` sqlite→postgresql. 7 eski migration `prisma/migrations-archive/`'e taşındı (SİLİNMEDİ): `sqlite-origin/` (5 dev migration) + `folded-into-baseline/` (2 postgres additive — DDL'leri 0_init'e katıldı; `add_thread_segments` IF NOT EXISTS kullanmadığından ayrı tutulsa fresh-apply'ı bozardı). Canlı DB'ye baseline SQL **çalıştırılmadı**; `migrate resolve --applied 0_init` ile bağlandı (yalnız `_prisma_migrations` satırı, DDL yok). Sonuç: `migrate status` = "up to date", **P3019 yok**; read-only doğrulandı (`_prisma_migrations` 0_init=applied, threadSegments=text, AuthAttempt + 3 index korundu). Fresh-target apply çalıştırılmadı — izole PG hedefi (docker/psql/yedek Neon) yok; doğruluk construction (tek from-datamodel baseline, 71=71) + "no difference" diff ile kanıtlı.

**C — Tek türetilmiş sistem sağlığı (§8C):** Önce AutomationManager (tam-genişlik band) + TopStrip chip + MorningHeroStats tiki AYRI fetch/türetim yapıyordu → çelişki. Artık SAF `deriveSystemHealth` (checking|healthy|warning|unavailable) + `SystemHealthProvider` (TEK fetch/state) topbar chip + Bugün tiki + drawer'ı besler. Kurallar: worker durmuşsa ASLA healthy; fetch fail → unavailable (kesin sayı yok); checking ≠ final. Tam-genişlik kırmızı band KALDIRILDI; ana gösterim topbar chip. `OperatorReadinessGate` AYRI eksen ("Üretim akışı uyarılı" — sistem sağlığı maskesi kalktı; üretim/operator hazırlığı = today-readiness). *Not: 3-kontrat sağlık ayrımı (altyapı/pipeline/bugün-hazırlık) Faz 1F.*

**B — Sidebar profil:** Tam-genişlik band kaldırılınca sticky-100vh sidebar'ın dip Profil'i viewport dibine oturur; 768/900/1080'de görünür (canlı: bottom 756/888/1068 ≤ vh). min-height:0 flex + `.cx-sidebar-scroll` nav-scroll korunur.

**D — Ready kart eylem hiyerarşisi (§8D):** Dinlenirken ≤3 ana eylem: **X'te aç** (primary) + **Düzenle** (secondary) + **⋯** (taşma). `Kaydet` yalnız dirty; **"Paylaşıldı olarak işaretle"** yalnız X intent açılıp `publish_prepared` olunca belirir (gerçek paylaşım sırası). Kopyala/Görsel üret/Detay → tek `Popover` taşma menüsü (klavye erişilebilir). needs_edit/blocked güvenlik sözleşmesi (intent/publish YOK) DEĞİŞMEDİ.

**E — Hesap değiştirici + shared `Popover` (§8F):** `ui/Popover` primitive — non-modal (transparent dışarı-tık katmanı, workspace KARARMAZ; modal perde/`--shadow-modal` DEĞİL, `--shadow-lg`), Escape kapatır + focus tetikleyiciye döner, roving klavye (Arrow/Home/End), WAI-ARIA menü-butonu. Sidebar AccountCard + kart taşma menüsü paylaşır; ProfileMenu gölgesi hafifletildi. Canlı: açıkken dark fullscreen scrim YOK, Escape+focus-return çalışır.

**§8E — Stale kaynak vs readiness dili:** readiness `ready` etiketi "Yayına hazır" → **"Kontrolleri geçti"** (teknik/kalite geçti, YAYIN GARANTİSİ değil). `whyToday` stale etiketi "Bayat kaynak" → "Kaynak eski"; + `freshnessWarning(state)` helper AYRI güncellik uyarısı ("Kaynak eski; yayınlamadan önce güncelliği kontrol et.") — kart + drawer AYNI cümle. Böylece "Kontrolleri geçti" + "Kaynak eski" çelişki değil, iki ayrı eksen. **ÜRÜN NOTU (readiness eşiği DEĞİŞMEDİ):** stale kaynak viral/güncel içerik hedefini gerçekten bozuyorsa "stale → needs_edit" kuralı önerisi — ancak yeterli canlı veri olmadan güvenlik/iş mantığı sessizce değiştirilMEDİ; eşik kalibrasyonu Faz 2 (canlı queue verisiyle).

**Doğrulama/gate:** typecheck · unit **1331** (+systemHealth/whyToday) · lint 0 err · build · e2e **31** · `migrate status` up-to-date (P3019 yok) · daily-queue gerçek Neon **200** (58 item). Canlı: profil 4 yükseklikte görünür, chip=tik tutarlı, popover non-modal, ready kart 3 eylem, 0 console error, 1024–1920 taşma 0. Kanıt `shots/cemos-rebuild/faz1c2-closeout/` (01-08; 01-05/08 gerçek shell/veri, 06/07 gerçek DraftReviewCard route-mock harness). Push/deploy yok.

### ADR-024 — Faz 1D: 13 yüzey gerçek işleve taşındı (host placeholder'lar + advanced araştırma)
**2026-07-16 (kullanıcı: 1C.2 görsel/teknik kapısı ONAYLANDI → Faz 1D izni verildi; ara onay beklenmez, sonda dur).** Kapsam: 8 host placeholder → gerçek yüzey + 5 REDESIGNED-ADVANCED araştırma ekranı arketipe = **13 gerçek yüzey**. Kullanıcı-erişilebilir HostPlaceholder = 0.

**1D-A Plan (bare host; başlık+SubNav shell'de):** `TakvimTab` (ReelPlanSlot + zamanlanmış QueueItem; Ay=yoğunluk-KALICI-KANAL-RENGİ-YOK / Hafta=kanal-saat-önizleme; slot→dossier drawer; stale bayrak; **ölü "+ Slot" YOK** → gerçek düzenlenebilir reels-plan oluşturucu; Instagram=Meta manuel blocked-external). `FirsatlarTab` + saf `opportunityCuration` (haber/YouTube/rakip/keşif 4 motoru Promise.all + yut = kısmi hata bütünü bozmaz; deterministik buzz×uyum×tazelik; **LLM kürasyonu Faz 2**; yetersiz örneklem çarpanı şişmez). `SerilerTab` (/api/series DNA kartı + düzenleme=PUT=sürüm=insan onayı; provenance operatör; Meta yoksa manuel DNA). Paylaşılan `useAccounts` + saf `calendarGrid`. **KARAR — Fırsatlar eylemleri = navigasyon köprüsü:** İçerik üret→Bugün, Plana ekle→Takvim, Seriye ekle→Seriler, Ham araştır→advanced; hedefe yönlendirir + toast (görünür gerçek etki, sessiz no-op DEĞİL). Otomatik ön-doldurma Faz 2 (cross-surface state).

**1D-B Kütüphane:** `LibTumuTab` — 4 legacy kütüphane (viral/keyword/prompt/pattern) + içerik havuzu TEK aramada; **yeni server-side sayfalı `GET /api/library/search`** (büyük veri seti client'a çekilmez; keyword statik JSON'dan, DB route yok — mevcut kullanılır). Tür segment+sayaç, platform "gelişmiş" panelde, "/" odak. `LibIlhamTab` — greenfield boards/ideas UI (backend vardı); capture (URL/not; ekran görüntüsü/video dürüst "yakında" = Faz 4); **yapısal analiz GERÇEK (reverse-engineer, 402-farkında) ya da "analiz edilmedi" — SAHTE AI YOK**; fikir→Bugün (create-draft). `LibOgrenmeTab` — env kapısı korunur (404→blocked-external, env NAME); durum sekmeleri; mevcut Learn alt-görünümleri (Processing/Pack/Review) yeniden kullanıldı. `Input` React-19 ref forwarding.

**1D-C Profil (kendi başlığını taşır):** `ProfileMemoryTab` — tam-sayfa hafıza onay kuyruğu; bağlayıcı "onaylanmadan taslakları etkilemez"; kural ekle (yazan=onaylayan→anında aktif), çelişki rozeti, supersede rollback. `ProfileIntegrationsTab` + **additive `GET /api/integrations`** (process.env VAR/YOK + env adları; **secret VALUE ASLA** istemciye/DOM'a); canlı çekirdek durumu /api/health'ten merge; X API doğrudan yayın kalıcı BLOCKED-EXTERNAL + maliyet senaryosu + "Onay ver" (gerçek ödeme YAPMAZ); her eksik/engelli için tek kurtarma.

**1D-D/E Advanced araştırma (5):** `news-pool` + `discovery-engine` canlı denetimde ZATEN yeni arketipte (sessiz metrik, ADR-012 doğrulama rozetleri, tek accent, dense liste, hand-modal yok) → değişiklik gerekmedi. `youtube`: hero KPI kutuları (biri accent) → sessiz satır-içi metrik. `flow-radar` → **`ViralRadarScreen`** (8-KUTU KPI hero KALDIRILDI → sessiz MetricGrid, summary null→"–"; ham `<select>`→FilterBar/Select; el-modal→DetailPanel+Drawer; EntityCard+ScoreBars; 3-varyant üretim+critic; StaleNotice ADR-012; 402→üretim blocked, liste okunur). `source-intelligence` → **`SourceIntelScreen`** (StatCard hero→sessiz MetricGrid; **ÖLÜ "Tara"+"Flow'a Gönder" (cursor:not-allowed) KALDIRILDI**; 3 el-modal→Drawer+DetailPanel 7-ScoreBars; 320px+1fr split-pane — **inline-style specificity bug'ı bulunup düzeltildi**; CRUD gerçek). **Yöntem:** iki ağır rebuild builder alt-ajanla üretildi, bu oturumda incelendi (anti-pattern grep + red-flag) + canlı doğrulandı (Builder→Reviewer ayrımı). Eski `FlowRadarTab`/`SourceIntelligenceTab` disk'te kalır (import edilmez=erişilemez; fiziksel emeklilik Faz 4).

**Legacy/nav audit (§14):** `HostPlaceholder` yalnız kendi dosyasında (0 import=erişilemez); `hostScreens.tsx` emekli (git rm — 8/8 host dönüştü). ABSORBED alias'lar çalışır (canlı: daily-queue→Bugün). Advanced id'ler canlı/sabit (store-jump ile 5'i de render). Cmd+K advanced açar.

**Bilinen küçük varyans (dürüstlük):** sessiz-metrik iki geçerli biçimde: satır-içi metin (news-pool/youtube) vs `MetricGrid` (öğrenme/viral-radar/source-intel) — ikisi de "sessiz, hero değil"; istenirse tek biçime birleştirilir (trivial follow-up).

**Gate:** typecheck 0 · lint 0 err (5 pre-existing warn) · unit **1358** (+27: opportunityCuration/calendarGrid/librarySearch) · build 0 · e2e [gate koşusu] · `migrate status` up-to-date (Faz 1D şema değişikliği YOK — 2 route salt-okuma). Canlı doğrulama (dev, gerçek Neon): 13 yüzeyin TAMAMI render + her birinde **0 console error** (Takvim 10-slot, Fırsatlar YouTube outlier, Seriler 8-seri, Tümü 3714 birleşik, Öğrenme 3/40/0, Memory gerçek kurallar, Entegrasyonlar env-code+X-blocked, ViralRadar 300 aday+ScoreBars, SourceIntel 62/300 split). Commit'ler `5a7f5bd`(Plan)/`3077945`(Library)/`2202869`(Profile)/`bfc2920`(News+YouTube)/`512c373`(advanced). Push/deploy yok. **Kalan dış:** X API ödeme onayı yok (intent-only, Faz 1E); OpenRouter/SocialData 402 olası (üretim blocked, okuma sürer); Meta/YouTube/Obsidian env opsiyonel.

### ADR-025 — Faz 1E: PublishAttempt, intent persistence ve atomik confirmation
**2026-07-16.** X API ödeme izni VERİLMEDİ → gerçek X API'ye bağlanılmadı, OAuth kurulmadı, canlı istek atılmadı, ödeme başlatılmadı. "CemOS içinden doğrudan yayın" AKTİF DEĞİL — Entegrasyonlar + XApiPublishAdapter kalıcı typed `payment_approval_required` blocked-external döner ve HİÇBİR koşulda ağ isteği yapmaz (test fetch-spy ile kanıtlı). **Intent ≠ yayın:** "X'te aç" YALNIZ `PublishAttempt(prepared)` satırı üretir; QueueItem published/manual_published OLAMAZ, PublishLog/PublishedPost OLUŞMAZ, UI "yayınlandı" DEMEZ.

**Kapatılan açıklar (repo'da doğrulandı):** (1) `publishPrepared` yalnız client `useState`'ti → reload'da kayboluyordu; artık SERVER kaynağı (daily-queue payload `publishAttempt` + `staleForCurrentContent`), canlı Neon'da reload-persist doğrulandı. (2) `markManualPublished` 4 ayrı yazımdı (PublishedPost best-effort) → artık TEK `prisma.$transaction`: attempt→succeeded + QueueItem→manual_published + PublishLog + PublishedPost + UsageLog. (3) Genel PATCH `status=manual_published` state machine'i bypass ediyordu → `publishService.markManualPublished` KALDIRILDI; mark-published + daily-queue PATCH yalnız `publishAttemptService.confirmManualPublish` üzerinden; prepared attempt yoksa 422 "hazırlık bulunamadı".

**Model (additive migration `20260716072050_add_publish_attempt`, `migrate deploy` ile Neon'a uygulandı, status clean, read-only introspection doğrulandı):** PublishAttempt{queueItemId, accountId, adapter:intent|x_api, state:prepared|succeeded|failed, idempotencyKey, contentHash, readinessPolicyVersion, readinessSnapshotJson, externalId?, errorCode?, errorMessage?, createdAt/updatedAt/completedAt} + `@@unique(accountId, adapter, idempotencyKey)` + (queueItemId,state)/(accountId,state) indeksleri.

**Kurallar:** prepare anında ve confirm anında readiness YENİDEN koşar (ready değilse 422; snapshot yalnız kanıt). contentHash = sha256(editedContent ?? content, trim); idempotencyKey = queueItemId+hash → retry duplicate üretmez; içerik değişince eski prepared'lar `superseded` olur ve confirm `content_changed` (Türkçe: "İçerik değişti — yeniden X'te aç") ile reddeder. Tekrarlanan confirm idempotent (mevcut başarı döner, duplicate log/post/usage yok); eşzamanlı confirm optimistic claim (updateMany state=prepared) ile tekilleşir. Popup-blocker: pencere kullanıcı gesture'ında SENKRON açılır (about:blank, opener=null), server prepare başarınca intent URL'e yönlendirilir; başarısızsa pencere kapanır + Türkçe hata; server kaydı oluşmadan client "prepared" SAYILMAZ. Dirty metin önce kaydedilir. Yan etkiler (öğrenme sinyali + maskulenkod görseli) transaction DIŞI best-effort kaldı — yayın durumunun parçası değil.

**Gate/kanıt:** 18 yeni state-machine testi (gerçek assessReadiness ile, fail-closed mock'lanmadı) + adapter no-network testleri; e2e: prepare-route-mock ile buton yalnız server başarısında belirir + 422'de belirmez; canlı: prepared → reload → "Paylaşıldı olarak işaretle" KORUNUR (gerçek Neon, confirm bilinçli yapılmadı). Commit'ler: 8530245(db)/41c266d(adapters+prepare)/0a3de7b(atomic confirm)/18c8abc+bdec0f9(test).

### ADR-026 — Faz 1F: Üç sözleşmeli sistem sağlığı (infrastructure / pipelineFreshness / todayReadiness)
**2026-07-16.** Düz health listesi üç AYRI typed domain sözleşmesine ayrıldı (`src/lib/health/healthContracts.ts`, SAF türetim + `healthContractService` montajı; /api/health'e additive `contracts` alanı — eski payload korunur).

**infrastructure:** DB, worker/cron canlılığı (lokal worker stale=error — ASLA healthy; cron stale=warn), cron auth (CRON_SECRET), gerekli provider'lar (OpenRouter/SocialData), credential süresi (Meta token warn/critical). **Opsiyonel + kullanılmayan entegrasyonun eksik credential'ı sistemi KIRMIZI YAPMAZ** (optionalUnconfigured, bölüm durumunu etkilemez). Secret değeri hiçbir API/UI/log'da yok — yalnız ENV adı (`DATABASE_URL`/`CRON_SECRET`/`OPENROUTER_API_KEY`...).

**pipelineFreshness:** son başarılı haber çekimi + sabah üretimi (CronRun), failed backlog, ham birikim. Her akış için `never_ran / fresh / delayed / failing` ayrımı; isimli eşikler `PIPELINE_FRESH_HOURS=26`, `NEWS_RAW_BACKLOG_WARN=30`. **Stale = amber uyarı; otomatik infrastructure error DEĞİL.** "Cron ok ama 24s'te 0 analiz + ham birikim" yanlış-yeşili failing'e çevrilir.

**todayReadiness:** günün akış fazı — `production_not_run ≠ no_drafts_produced ≠ awaiting_decision ≠ ready_available ≠ queue_completed ≠ target_met`. **"Queue boş" her zaman hata değildir; "kuyruk tamamlandı" healthy/NEUTRAL'dır ve "üretim hiç çalışmadı" ile AYNI state olamaz.** Sayılar: ready/needs_edit/blocked (gerçek assessQueueItemReadiness), hazırlanmış intent (PublishAttempt prepared), bugün yayınlanan, günlük hedef (Schedule.dailyMaxPosts toplamı).

**Topbar:** yalnız EN YÜKSEK öncelikli actionable sinyal — infra error > infra warn (credential süresi) > pipeline stale warn > bugün-taslak-yok warn > karar-bekliyor/hazır-var action > none. "Gün tamam" = uyarı YOK; ilgisiz sorunlar tek kırmızı sayaca EZİLMEZ; tüm bölümler bilinmiyorsa "sağlıklı" İDDİA EDİLMEZ ("durum belirsiz"). SystemHealthProvider TEK fetch kaynağı — topbar chip + Profil/Sistem AYNI istekten beslenir (tekrar fetch yok, deep=true probe YOK).

**Profil/Sistem:** üç açık bölüm (Altyapı / Akış güncelliği / Bugünün hazırlığı) + sessiz MetricStrip hücreleri (hero StatCell KALDIRILDI); bölüm başına bağımsız fail-soft (tek bölüm düşerse diğerleri yaşar; hepsi düşerse gerçek ErrorState); Entegrasyonlar/Haber Havuzu/Maliyetler deep-link; eyleme dönük Türkçe detay satırları.

**operatorReadinessService overlap kararı:** Bugün'ün OperatorReadinessGate'i AYRI eksen olarak KALIR (public /api/settings/operator-readiness sözleşmesi değişmedi — "şu an çalışabilir miyim" blocker analizi); todayReadiness ise günün AKIŞ FAZI (Sistem/topbar). İkisi aynı day-bounds util'ini kullanır; kalan örtüşme iki count sorgusuyla sınırlı ve bilinçli — gate'in isim/sözleşme değişikliği legacy etki riski taşıdığından Faz 2'ye bırakıldı.

**Gate/kanıt:** 26 saf test (§6.6 matrisi: DB down, cron auth, worker offline vs cron gecikmiş, opsiyonel/gerekli provider, credential expired, fresh/stale/never-ran/failing, failed backlog, today 6 faz, section fail-soft, topbar yalnız-actionable + queue-completed-uyarı-üretmez + tek-sinyal). Canlı (gerçek Neon): Sistem üç bölüm doldu (Altyapı=sorunlu[dev worker kapalı, dürüst], Akış=uyarı[332 hatalı haber gecikmiş, sabah üretimi güncel], Bugün=sağlıklı[2 hazır]); topbar tek sinyal "Worker"; 1024/1440/1920 taşma 0; console 0. Commit'ler: c9b2480/b00c88a.

### ADR-027 — Faz 2A: Config-driven Agent/Skill Registry ve execution güvenlik sınırı
**2026-07-16 (kullanıcı Phase 2A onayı; Phase 2'nin dilim haritası `07-PHASE2-PLAN.md`).** 12 ürün rolü + Opportunity Curator skill'i typed, config-driven registry'ye alındı (`src/lib/agents/registry/`). **Envanter sonucu: hiçbir rol EKSİK değildi** — her tanım MEVCUT servise adapter ile bağlanır (aynı işi yapan yeni agent YAZILMADI; 2 KISMEN rol — Trend Scout, Fact Checker — mevcut yapı taşlarının compose edilmesiyle kapandı, façade derinleşmesi 2B–2E).

**Metadata ↔ adapter ayrımı (bağlayıcı):** `definitions.ts` = YALNIZ deklaratif veri (id/version/policyVersion/amaç/tetikleyici/Zod input-output/capability allowlist/preset/budgetClass/timeout/retry/fallback/memory scope/provenance/trace policy/eval fixture/enabled-blocked). Çalıştırılabilir köprüler YALNIZ server-side `adapters.ts` allowlist'inde; client'a fonksiyon/prompt/secret gitmez; delegasyonlar sabit literal modül yollarıdır (keyfî dynamic import yok). Ana navigasyona "Agents/Skills" sayfası EKLENMEDİ — arka plan altyapısı.

**Fail-fast doğrulama (`validate.ts`, modül yüklenirken):** duplicate id · adapter'sız aktif entry · şemasız entry · llm/hybrid entry'de geçersiz preset · timeout/retry sınır dışı · tanımsız capability · **external provenance hafıza YAZAMAZ** (assertIdentityWriteAllowed ile aynı sınır, konfigürasyon düzeyinde de kilitli) · adapter'ın kullandığı capability/memory-write tanım allowlist'ini aşamaz · aktif entry ≥1 deterministik fixture + fixture inputSchema'dan geçmek zorunda → tek sorun bile `AgentRegistryError`.

**Execution contract (`executor.ts`, zorunlu sıra):** tanım çöz → input Zod → capability/memory izin → bütçe/blocked-external → adapter (timeout + retry; blocked/validation hatası retry EDİLMEZ) → output Zod → trace → maliyet sahipliği → typed sonuç (`succeeded / deterministic_fallback / blocked_external / failed_validation / failed_execution / timed_out`). **LLM kapalı/kredisizken sahte AI sonucu ÜRETİLMEZ:** `BudgetExceededError`/`AgentBlockedError` → deterministik fallback varsa `deterministic_fallback` + `fallbackUsed=true` + `blockedReason` (UI/trace'te gizlenemez), yoksa `blocked_external`. Output ihlali fail-closed; fallback deterministik yolu korur. Fail-open sessiz değildir — trace'e yazılır.

**Trace/cost:** PipelineTrace/UsageLog YENİDEN YAZILMADI, yeni tablo YOK. Stage şekline ADDITIVE opsiyonel alanlar (agentId/agentVersion/adapterId/executionMode/preset/outcome/fallbackUsed/blockedReason/retryCount/estimatedCostUsd/inputSchemaVersion/outputSchemaVersion/policyVersion) — eski stage kayıtları aynı parse ile okunur; PipelineTraceDrawer alansız kayıtlarla çalışmaya devam eder, yeni alanları sessiz tek satırda gösterir. Trace yazımı best-effort ama kayıp ÖLÇÜLÜR (`getLostTraceCount`). Maliyet invariant'ı: ücretli çağrının TEK UsageLog'unu gated primitive yazar (executor ikinci kez yazmaz); deterministik koşu costUsd=0 açık temsil; blocked koşu ücret doğuramaz.

**Opportunity Curator (§11):** deterministik `opportunityCuration` GÜVENLİ FALLBACK olarak korundu (yeniden yazılmadı). Typed LLM yolu hazır ama `ENABLE_AGENT_CURATION=1` olmadan ASLA çalışmaz (`AgentBlockedError`, ağ çağrısı yok — fetch-spy testli). Çıktı 5 gerekçe ekseni taşır (persona/tazelik/çeşitlilik/somutluk/risk); her seçim giriş `sourceId`'lerinden birine bağlanmak ZORUNDA — uydurma sourceId fail-closed reddedilir ve deterministik sıralama korunur. **Canlı ücretli kürasyon 2A'da YAPILMADI** (aktivasyon 2E'de, kredi onayıyla).

**Gate/kanıt:** 29 registry/executor testi + 6 curator eval testi (12-rol varlığı, tüm red matrisi, blocked'ta ağ-çağrısı-yok, tek-çağrı-tek-UsageLog, trace metadata tam, timeout/retry). Commit'ler: b300e84(plan) / 67e5e49(registry) / 0ad5f46(executor+trace).

### ADR-028 — Faz 2A: Kalıcı Opportunity Handoff ve cross-surface idempotency
**2026-07-16.** Fırsatlar'ın üç eylemi (İçerik üret / Plana ekle / Seriye ekle) navigasyon+toast köprüsünden (ADR-024'te bilinçli ertelenen boşluk) **server-persisted typed handoff**'a geçti.

**Persistence kararı:** mevcut modeller değerlendirildi — `Idea` = içerik tohumu (aktarım durumu yok), `ContentOpportunity` = haber-tweet fikri (Fırsatlar Opportunity'si değil), `ReelPlanSlot.topicHint` = yalnız plan sonucu, nav payload'ı yalnız `setActiveTab(string)`. **"Hazırlanmış ama tüketilmemiş" cross-surface işi hiçbirinde temsil edilemiyordu** → küçük additive `OpportunityHandoff` modeli (migration `20260716085323`, saf CREATE TABLE+index+FK, `migrate deploy` ile Neon'da, status clean). Kaynak veri KOPYALANMAZ — sourceKind+sourceId orijinaline işaret eder (gereksiz çok-tablo kopyası yok); payloadJson versiyonlu ve her okumada Zod'dan geçer (yabancı sürüm fail-closed). Zustand/localStorage'a KONMADI — reload-persist server kaynağıdır (store v10 gerekmedi; `"xagent-store"` dokunulmadı).

**State machine:** `pending → consumed | cancelled`; doğrudan status yazan route YOK (PATCH yalnız op=cancel). Dedup `@@unique(accountId, action, fingerprint)` (sha256 kaynak+seed) — duplicate click/eşzamanlı yarış (P2002) yeni satır üretmez; cancelled tekrar istenirse pending'e döner; consume optimistic-claim'dir (tekrar onay mevcut sonucu döner, duplicate slot/ilişki/taslak İMKÂNSIZ). `blockedReason` fırsatı KAYBETMEDEN dış engeli işaretler (pending kalır).

**Üç eylemin gerçek davranışı:** (1) **generate** — registry Content Creator adapter'ı üzerinden gerçek üretim; consume claim transaction'ın BAŞINDA (yarışı kaybeden QueueItem yazmaz), başarıda QueueItem `status:"new"` (insan onayı korunur, otomatik publish yok) + provenance scores + sonuç referansı; **blocked-external/mock-fallback'ta SAHTE taslak yazılmaz** — handoff pending + Türkçe engel + Entegrasyonlar kurtarma yolu; üretilen taslak Bugün'de odaklanır (ReviewQueue focusSeed). (2) **plan** — kullanıcı GÜN seçip onaylamadan HİÇBİR takvim kaydı oluşmaz; onay = consume+ReelPlanSlot tek transaction; ay planı yoksa dürüst `plan_not_found` + "önce planı kur"; iptal tüketmez. (3) **series** — seri = DNA şablonu (bölüm koleksiyonu değil) → kalıcı ilişki handoff satırının KENDİSİDİR (consumed + resultRef=seriesKey); hedef seri kullanıcı seçmeden bağlanmaz; ayni-hesap doğrulaması cross-account sızıntıyı keser; Seriler DNA kartı altında "Fırsattan gelen aday konular" listelenir; uygun seri yoksa dürüst boş durum (sahte "eklendi" yok).

**Görsel sözleşme (§13):** kompakt "Fırsattan geldi" provenance bandı — mevcut primitive'ler, hero/banner yok, tek birincil eylem + açık Vazgeç; tüm mutation route'ları `isOperatorOrCronAuthorized` (CSRF sınıfı) arkasında.

**Kritik operasyon notu (2026-07-16):** kanıt koşusu öncesi ücretsiz `/auth/key` ön-kontrolü OpenRouter kredisinin ARTIK MEVCUT olduğunu gösterdi (limit 10, kalan ~7.70) — bu yüzden canlı "Taslak üret" TIKLANMADI (tıklama gerçek ücretli çağrı olurdu; direktif yasaklıyor). Blocked-external ve başarı yolları unit+e2e'de kanıtlı; blocked görseli harness'tır (raporda açık). **2E canlı kürasyon aktivasyonu artık kredi açısından mümkün — yalnız kullanıcı onayı bekliyor.**

**Gate/kanıt:** 21 unit (service state machine + generate route blocked/mock/başarı/idempotent-retry/yarış) + 6 hermetik e2e (üç akış, reload-persist, onay-öncesi-kayıt-yok/onay-sonrası-tek-kayıt, duplicate-yok, 1024–1920 taşma-yok). Canlı (gerçek Neon, ücretsiz yollar): handoff oluştu → reload → band KORUNDU; Takvim prefilled panel; temizlik cancel ile yapıldı. Commit'ler: 3d4d13a(db) / acf0591(wire) / dff2479(e2e).

### ADR-029 — Faz 2B: MemoryEvidence, distinct-source idempotency ve insan-onaylı promotion
**2026-07-16 (kullanıcı Phase 2B onayı; Phase 2A commit sayısı düzeltmesi: 7).** Hafıza governance'ı kaynaklı ve insan kapılı hale getirildi.

**Kanıt defteri (`MemoryEvidence`, migration `20260716120000` — saf additive: nullable ADD COLUMN + CREATE TABLE + 4 index + 1 FK; `migrate deploy` ile Neon'da, status clean):** her kanıt satırı kontrollü sourceType (feedback_event/operator_assertion/operator_revision/validated_pattern/legacy_unattributed) + sourceId + signalType + direction + ≤280 excerpt + versiyonlu metadataJson taşır. **Idempotency `@@unique(memoryFactId, sourceType, sourceId, signalType)`** — aynı FeedbackEvent'in retry'ı kanıt sayısını ARTIRAMAZ. `MemoryFact.canonicalKey` (nullable UNIQUE, account+type+fold'lanmış-statement hash'i) eşzamanlı aynı sinyalin iki proposal üretmesini DB düzeyinde keser (P2002 kaybeden mevcut satıra corroborate). `evidenceCount` defterli fact'te defter özetidir; **eski fact'lere sahte kaynak backfill EDİLMEZ** — defteri olmayan kayıt UI'da "eski kayıt · kaynak ayrıntısı yok"; eski aktif fact'ler aktif kalır (üretim davranışı geriye dönük bozulmaz).

**Promotion (BAĞLAYICI — eski davranış KALDIRILDI):** eski `proposeFact` semantic tipi 3 kanıtta OTOMATİK aktifleştiriyordu. Artık **feedback/self-judge/consolidation/own-metric kaynaklı hiçbir MemoryFact otomatik aktifleşemez**; ≥3 distinct KAYNAKLI kanıt yalnız "incelemeye hazır" yapar; 1/3–2/3 önerinin "Onayla"sı kapalıdır (UI disabled + **server-side fail-closed** `insufficient_evidence`). Operatörün kendi yazdığı kural (createdBy=operator) anında aktif olabilir; eski/kanıtsız proposal'ı kullanıcı "Sahiplen" ile üstlenirse tarihsel kaynak UYDURULMAZ — bugünkü `operator_assertion` kanıtı yazılır.

**Atomik geçişler:** approve/rollback/revise tek `$transaction`; revise statement'ı YERİNDE MUTATE ETMEZ (yeni operatör sürümü + supersedesId zinciri + eski superseded + `operator_revision` kanıtı; rollback görünür); rollback = yeni rejected + eski active + tInvalid geri, tek transaction; yanlış-durum eylemleri typed `MemoryGovernanceError` ile reddedilir; fact-id mutasyonları `expectedAccountHandle` ile cross-account'a kapalı; DELETE yok.

**Deterministik sinyal köprüsü (`signalBridge.ts`, LLM'SİZ):** tanımlı tag'ler (not_my_tone/hook_weak/too_ai/make_stronger/make_clearer) SABİT canonical kural adaylarına gider; gerçek kullanıcı reason'ı operatörün KENDİ sözleri olarak verbatim statement olur (ambigü metinden anlam uydurulmaz); **mekanik nedenler ("Daily Queue editor feedback", "Manuel paylaşıldı…", "Editor action:") ve reason'sız approved/rejected ÖĞRENİLMEZ; editDistance tek başına kural üretemez; engagement_* identity'ye yazılmaz.** `processFeedback` sonrası best-effort ingestion (memory hatası esas feedback kaydını kaybettirmez → warning) + haftalık learn zincirinde LLM'siz `reconcileFeedbackSignals` (aynı Pazartesi slotu, YENİ CRON YOK; unique sayesinde idempotent). Registry knowledge-curator v1.1.0 `reconcile_signals` eylemi + fixture. Canlı LLM extraction ÇALIŞTIRILMADI (mock'lu test).

**Gate/kanıt:** 24 governance + 13 bridge testi; commit'ler 48209fe(db) / 10f1406(governance) / e41a676(bridge).

### ADR-030 — Faz 2B: Identity ↔ validated performance ayrımı ve kaynaklı read model
**2026-07-16.** İki karar:

**(1) Performans dersleri identity'den AYRI truth store'dur.** Tek başarılı post kural OLAMAZ; tek edit/tek ret de olamaz (köprü zaten proposal+kanıt üretir, aktifleşme insan ister). "Performanstan öğrenilen ders" YALNIZ lessonGate'in tekrar+anlamlılık+marka-veto kapılarını geçmiş `validated ViralPattern`'dir (gerçek validatedAt/validatedSupport; **DB'de olmayan p-value/lift/kanıt UYDURULMAZ**); identity MemoryFact'ine otomatik KOPYALANMAZ ve identity retrieval'ına girmez. Profil hafıza görünümünde iki ayrı bölüm: "İnsan onaylı kimlik/yazım kuralları" ve "Performanstan doğrulanmış dersler".

**(2) Kaynaklı knowledge read modeli + influence provenance.** `GET /api/memory/knowledge?accountHandle=` (same-origin guard) typed, LLM'siz model döner: deterministik özet (gerçek sayılardan), aktif kurallar + bekleyen öneriler (kanıt defteri, reviewReady, legacyUnattributed, kaynak-hâlâ-mevcut-mu, bozuk-metadata fail-closed işareti), performans dersleri, son sinyaller (mekanik/açık ayrımı), bölüm-bazlı fail-soft; secret/credential/sınırsız dış içerik dönmez. **Influence provenance:** `buildIdentityMemoryContext` typed dönüşü (block + memoryFactIds + policyVersion) → `GroundingContext.memoryFactIds` (additive) → QueueItem `scores.groundingMemoryFactIds`. Yalnız AKTİF fact id'si yazılabilir (retrieval yalnız active okur — proposal/superseded İMKÂNSIZ); grounding düşerse boş (sahte influence yok); dedupe; eski scores JSON'ları alansız okunmaya devam eder (yeni kolon yok). SystemHealthProvider benzeri tek-fetch: ProfileMemoryTab tek knowledge çağrısından beslenir.

**UI (Profil → CemOS'un bildikleri derinleşti; yeni ana nav YOK):** hesap seçici + deterministik özet + sessiz MetricStrip (hero KPI yok); aktif/proposed/performance görsel olarak ayrık; "Taslakları etkiliyor" açık; kanıt Drawer'ı; 1/3–2/3'te Onayla disabled + Sahiplen; düzenleme yeni-sürüm akışı; rollback görünür; legacy dürüst; loading/empty/partial/error/success durumları; cross-account sızıntı yok.

**Gate/kanıt:** 1483/1483 unit (154 dosya) · 65/65 e2e · build 0 · lint 0 err · migrate clean · gerçek yüzey console 0, 1024–1920 taşma 0. Kanıt `shots/cemos-rebuild/faz2b/` (01/05/06/07/08/09 gerçek Neon — canlıda 16 aktif kural hepsi dürüst legacy, 0 validated ders dürüst boş; 02/03/04 HARNESS — kaynaklı kanıt drawer'ı + 1/3 disabled + 3/3 enabled; gerçek DB öğrenmesi olarak SUNULMAZ). Gerçek identity hafızasına test kuralı YAZILMADI (üretimi etkilerdi); canlı ücretli LLM çağrısı YOK. Commit'ler: 856e114(read model) / 71f759a(provenance) / 6c573e9(UI) / 7d048cc+16796f7+c466fd8(test).

### ADR-031 — Faz 2C: Dinamik hesap profili — DB runtime source of truth
**2026-07-16 (kullanıcı Phase 2C onayı).** `src/lib/accounts.ts` içindeki hardcoded `AccountHandle` literal union'ı ve TS `accountProfiles` sabiti RUNTIME OTORİTESİ olmaktan çıkarıldı.

**Karar:** Runtime otoritesi DB'dir — `Account` (Phase 2C additive kolonları: displayName/language/autonomy/defaultDraftCount/formatsJson/benchmarkInput/profileStatus/isActive/profileVersion/profileUpdatedAt; migration `20260716210000`, migrate deploy Neon, saf additive) + mevcut `StyleProfile` (toneRules/formatRules/forbiddenRules/modes — zaten seed'li JSON). Okuma yolu `src/lib/accounts/profileRepository.ts`: her JSON alanı Zod'dan geçer (`RuntimeAccountProfile`); kısa-TTL in-process cache hızlandırıcıdır, otorite değildir.

**Bootstrap verisi YOK EDİLMEDİ, rolü daraltıldı:** `accountProfiles` = (1) seed/backfill kaynağı, (2) test fixture'ı, (3) yalnız DB-BAĞLANTI hatasında tohumlu iki hesap için `degraded: true` İŞARETLİ kullanılabilirlik fallback'i. Bilinmeyen handle bootstrap'tan asla yetki alamaz.

**Trust boundary'ler async DB doğrulamasına geçti (fail-closed):** memory scope guard'ı (`memoryFactService.assertScope` + `knowledgeReadModel`), feedback ingestion (z.enum kaldırıldı; bilinmeyen/devre-dışı hesap yazamaz), registry `assertHandle`, vector-memory/toolbox/news-pool route doğrulamaları, cron hesap seçimi (`resolveCronHandles` — draft/inaktif hesap manuel parametreyle bile cron'a giremez), operator readiness/mode/scan hedef listeleri. Senkron `isKnownAccountHandle` KALDI ama rolü "bootstrap tip daraltma" (heuristik scoring bağlamları) olarak açıkça yeniden adlandırıldı-dokümante edildi; güvenlik sınırı DEĞİL.

**Üretim hazırlığı fail-closed:** `getRuntimeProfile(handle, { requireGenerationReady: true })` — `isActive=false` → inactive; `profileStatus !== "active"` (draft/configuration_required) → profile_incomplete; bozuk JSON → profile_invalid. Yeni hesap profili tamamlanmadan üretime GİREMEZ; constitutions/persona/keyword sabitlerinde tohumlu olmayan handle jenerik/boş fallback alır — BAŞKA HESABIN personasına sessiz düşüş YOK (`getVoiceConstitution` null; council persona lens'i jenerik soru; competitor/niche boş).

**Legacy invariant'lar korunur:** `useXAgentStore`/"xagent-store"/`XAgentApp.tsx`/`src/store/xagent.ts` adları değişmedi; `Channel` client tipi string'e genişledi (client seçimi UX'tir, otorite değildir — Sidebar switcher /api/settings'ten DB listesiyle beslenir, düşerse bootstrap fail-soft). Bilinen dürüst sınır: eval goldenSet/readiness/publishAttempt içi literal kopya union'lar ile Record-tabanlı heuristik sabitler (keywords/watched-sources/competitors) tohumlu iki hesaba özeldir — yeni hesap için fail-soft çalışır, kalite eşleniği Phase 3+ işi.

### ADR-032 — Faz 2C: Composio Instagram read-only köprüsü
**2026-07-16.** Kullanıcının Composio'da bağladığı Instagram Business/Creator hesabı CemOS'a READ-ONLY bağlandı.

**Neden Composio:** Meta app izin süreci (`instagram_basic`+insights) dış blocker'dı; Composio managed-auth OAuth token'ları KENDİ tarafında tutar (CemOS token/credential SAKLAMAZ) ve tek MCP sözleşmesiyle read tool'ları sunar.

**İki katman AYRIDIR:** (A) Claude Code user-scope MCP config'i = yalnız geliştirme/test aracı; (B) CemOS runtime köprüsü = `src/lib/composio/mcpClient.ts` — Next.js server/cron içinde, Claude Code config'inden bağımsız, `https://connect.composio.dev/mcp` endpoint'ine (SABİT; Composio REST SDK'sına sessiz geçiş YASAK) JSON-RPC/Streamable-HTTP konuşan deterministik LLM'siz istemci. Auth `x-consumer-api-key` YALNIZ server env'den (`COMPOSIO_CONSUMER_API_KEY`; NEXT_PUBLIC yok, log/rapor/DB'ye değer yazılmaz — hata metinleri redakte).

**Read-only allowlist (çift katman + discovery):** explicit slug allowlist (GET_USER_INFO/USER_INSIGHTS/IG_USER_MEDIA/IG_MEDIA/IG_MEDIA_CHILDREN/IG_MEDIA_INSIGHTS/IG_MEDIA_COMMENTS) + deny-verb kalıbı (CREATE/POST/PUBLISH/SEND/DELETE/REPLY/UPDATE/MARK_SEEN/COMMENT/MESSAGE/CONVERSATION/…) — DM read tool'ları bile bilinçli DIŞARIDA (direktif DM'i yasaklar). Çağrı yolu ek olarak canlı `tools/list` discovery'sinde slug'ı doğrular; sözleşme kaymışsa fail-closed `tool_not_found`. Generic "tool çalıştır" endpoint'i YOK — tek komut bounded read-only sync. timeout + sınırlı retry/backoff + 2MB yanıt tavanı + yapılandırılmış hata sınıfları.

**Provider soyutlaması + fallback:** normalize Zod sözleşmesi `InstagramReadProvider` (yazma/DM metodu yüzeyde YOK); `ComposioInstagramReadProvider` (birincil; `connected_account_id` her çağrıda AÇIK — sessiz hesap seçimi yok) + `MetaGraphInstagramReadProvider` (mevcut igClient SİLİNMEDİ, sarmalandı). `INSTAGRAM_DATA_PROVIDER`: auto → Composio sağlıklıysa Composio, değilse `fallbackReason` İŞARETLİ Meta; explicit composio → sessiz fallback YOK.

**Hesap binding'i (AccountPlatformBinding, ADR-031 tablosuyla birlikte):** CemOS hesabı + platform + provider deterministik unique; credential/token TUTMAZ — yalnız bağlantı kimliği (connected account id + IG username), durum, toolkit sürümü ve son sync özeti. `COMPOSIO_INSTAGRAM_ACCOUNT_HANDLE` DB'de aktif hesaba çözülemezse sync FAIL-CLOSED hiç başlamaz. IntegrationCredential yeniden KULLANILMADI: o model şifreli key-value credential deposu; binding ise credential İÇERMEYEN durum/eşleme kaydı — iki ayrı sorumluluk.

**Veri mevcut modellere yazılır (duplicate tablo yok):** media→`IgMedia`, yorum→`IgComment`, günlük insight→`IgInsightSnapshot` (date @unique idempotent; rawJson'a ham provider yanıtı DEĞİL yalnız provider+sürüm etiketi), içerik köprüsü→mevcut normalizer/ingest ile `ContentItem`; engagement learning mevcut `syncInstagram` yolundan snapshot'ı tüketmeye devam eder. Upsert anahtarları external Instagram ID — aynı sync tekrar koşunca duplicate yok. Tek-Instagram sınırı: `IgInsightSnapshot.date` @unique yapısı korunur (multi-IG = Phase 3 dürüst sınırlama).

**Rakip verisi ÇÖZÜLMEDİ (dürüst sınır):** Composio Instagram toolkit'inde (20260708_00) business_discovery YOK — `igCompetitorService` Meta business_discovery yolu aynen kaldı; Meta izni dış blocker olarak sürer. Own-account bağlantısı rakip verisi gibi SUNULMAZ.

**BLOCKED-EXTERNAL:** `COMPOSIO_CONSUMER_API_KEY` hiçbir güvenli kaynakta bulunamadı → Claude Code MCP kaydı + canlı MCP discovery + canlı read-only smoke + gerçek Composio sync BU PASS'TE DOĞRULANAMADI. Kod + testler tam (deterministik fetch-mock); slug'lar dokümante toolkit sürümünden. Kullanıcı aksiyonu: key'i güvenli local/Vercel env'e koy (chat'e yapıştırma) + `claude mcp add --scope user --transport http composio https://connect.composio.dev/mcp --header "x-consumer-api-key: <env'den>"`.


### ADR-033 — Faz 2D: Doğrudan thread üretimi ve dürüst thread yayın sözleşmesi
**2026-07-16 (kullanıcı Phase 2D onayı; kod öncesi gerçek DB audit'i zorunluydu).**

**Kod-öncesi audit bulgusu (Neon, read-only, `scripts/audit-thread-readiness.ts`):** 60 QueueItem içinde 13 thread taslağı — TAMAMI `mode=thread` + `draftType=TWEET` + `threadSegments=NULL` + `status=new`; readiness draftType'a baktığı için **12'si tek-tweet gibi yanlışlıkla "ready"** görünüyordu (894 karaktere kadar yapısız metin). Etiketli kalibrasyon örneği: 0 pozitif (published/approved) + 0 negatif (rejected) judged thread → **calibrationStatus = insufficient_sample**.

**Canonical authority (bağlayıcı):** `threadSegments` thread'in canonical publication payload'ıdır; array sırası = thread sırası; her segment trim'li ve boş değildir; thread ≥2 segment taşır (ürün hedefi 5–8; şema güvenlik tavanı 25). `QueueItem.content` yalnız backward-compatible birleşik temsildir — `joinThreadSegments` ("\n\n") ile HER ZAMAN segmentlerden türetilir. Non-thread taslakta threadSegments null. Metindeki "1/" numaralandırma yapısal kanıt DEĞİLDİR. **Segment sınırı tek primitive'dir:** `effectiveThreadSegmentLimit = min(280, account.maxChars)` — UI/readiness/generation/schedule/publish aynı fonksiyonu kullanır; UI'daki ayrı `SEGMENT_MAX=280` literali KALDIRILDI. Thread tespiti mode-farkındalıdır: `isThreadDraft(draftType, mode)` — audit'teki uyumsuz sınıf artık tek tweet gibi değerlendirilemez. **Yeni QueueItem thread kolonu EKLENMEDİ** (alan Faz 1C'den beri var; ADR-010 sözleşmesi korunur); bu pass'te şema migration'ı YOK.

**Writer strict schema:** `WRITER_RESPONSE_SCHEMA` (strict json_schema; degrade zinciri json_object'e düşer) `threadSegments` alanını taşır. Çağıranın format niyeti writer'a AÇIKÇA gider (`RequestedDraftFormat`: thread zorunlu / tweet yasak / auto). Explicit thread isteğinde geçerli segment yoksa BİR repair retry; hâlâ yoksa **judge'a harcama yapılmadan** typed `threadRequestUnsatisfied` → çağıran QueueItem YAZMADAN blocked döner. Sahte tek-segment/mock thread ÜRETİLMEZ (`createMockBenchmark` segment üretmez; mock + thread isteği = blocked).

**Judge provenance:** writer taslakları deterministik 0-tabanlı indeksle sunulur; judge `sourceDraftIndex` döndürür. `bindJudgeCandidates`: canonical content/mode/threadSegments/payoff WRITER adayından, skor/verdict/reason judge'dan; geçersiz indeks → birebir content eşleşmesiyle deterministik fallback; çözülemeyen aday DÜŞER; aynı taslak iki indeksle duplicate EDİLEMEZ. Fast/risk_based/deadline/judge-empty yolları segmentleri korur. Eski persist candidatesJson (alan yok) okunmaya devam eder.

**Final editor parity:** thread kazananında final editor AÇIKÇA atlanır (`finalEditor="skipped:thread_segment_contract"`, trace'te görünür) — birleşik metni yeniden yazıp segmentleri stale bırakma sınıfı kapatıldı. Non-thread davranış değişmedi.

**Persistence:** thread sonucu TEK create ile `draftType=THREAD` + `mode=thread` + canonical content + serialize edilmiş segmentler + telemetry (formatIntent/threadSelection/segmentCount/min/max/totalChars) yazılır. `fitToMaxChars` thread birleşimine UYGULANMAZ (sessiz truncation yasak; segment sınırı doğrulanır, ihlal = invalid → repair/blocked). Thread lint'i deterministik ve SEGMENT bazlıdır (`half_sentence_end`/`below_min_chars` segmentte uygulanmaz; segment başına ücretli LLM lint ÇAĞRILMAZ — thread'de LLM mikro-pass bilinçli atlanır). Format niyeti: draftType=THREAD / mode=thread / Source.mode=thread → thread ZORUNLU; çağıranın bilinen non-thread modu → explicit TWEET kilidi (thread kazanan uzun içerik TWEET diye persist EDİLMEZ; thread-dışı aday yoksa dürüst `no_non_thread_candidate`); körlemesine `draftType:"TWEET"` default'u = auto (geçerli thread kazanan → THREAD'e yükseltilir, `auto_thread_winner`; geçersiz thread kazanan → görünür non-thread fallback). Opportunity handoff akışı bilinçli explicit-TWEET kaldı (thread kazananda non-thread aday seçer, yoksa dürüst hata). Atomize: "thread" packageRole YALNIZ geçerli segmentli adaya (THREAD+segments persist); segmentsiz aday thread etiketi alamaz; reused candidate maliyeti 0 kalır.

**Readiness parity (policy 1.0.0→1.1.0-provisional):** `ReadinessInput.accountHandle` string — bilinmeyen hesap grafikcem'e MAP EDİLMEZ; seed'li politikalar (emoji) yalnız EXACT handle eşleşmesinde. Thread metni canonical segmentlerden türetilir → stale `content` güvenlik kontrollerini bypass EDEMEZ. Her segment `effectiveThreadSegmentLimit` ile denetlenir; boş/tek/oversized segment fail-closed (needs_edit); thread birleşik uzunluğu `over_char_limit` bloğunu tetiklemez. Non-thread readiness değişmedi. Eski PublishAttempt snapshot'ları eski versiyon damgasıyla okunur (şekil aynı).

**Düzenleme atomikliği:** PATCH segment kaydı server Zod doğrulamalı (boş segment 400, şema tavanı 400); thread taslağında `threadSegments` + `editedContent`(=canonical birleşim) AYNI update'te yazılır — generated `content` korunur; segmentler ile güncel metin ayrışamaz; prepared attempt hash segmentlerden türediği için edit sonrası stale olur.

**Dürüst intent publish:** X web intent tek çağrıda çok gönderili zincir OLUŞTURMAZ → thread intent'i YALNIZ İLK segmenti açar (`intentMode="thread_first_segment"`, typed). UI CTA "İlk gönderiyi X'te aç"; prepared sonrası açıklama: "X yalnız ilk segmenti açtı. Kalan segmentleri yanıt olarak paylaş; zincir tamamlanınca thread'i paylaşıldı olarak işaretle." + kalan segmentler için kopyalama eylemleri. Otomatik reply/publish YOK ve öyleymiş gibi GÖSTERİLMEZ; "Paylaşıldı olarak işaretle" bütün zincir için manuel kullanıcı beyanıdır. **contentHash:** thread'de `threadPublicationHashInput` (sıra+değer dahil, uzunluk-öncekli çerçeveleme) — segment edit/reorder → prepared stale → confirm `content_changed`; non-thread hash davranışı birebir geriye uyumlu. Confirm: readiness yeniden koşar; `UsageLog.tweetCount=segmentCount`; PublishLog payload `manualThread+segmentCount`; PublishedPost content=birleşim; idempotency değişmedi. X API gerçek multi-post publish UYGULANMADI (Faz 1E ödeme-onayı blocker sözleşmesi sürer).

**Schedule/approve parity:** thread approve/schedule birleşik toplam uzunlukla REDDEDİLMEZ; segment-bazlı denetim readiness'te — ready değilse `readiness_not_ready` fail-closed; joined lint thread'de koşulmaz; non-thread davranış aynen. Schedule dış platformda otomatik yayın DEĞİLDİR (yalnız CemOS takvim durumu).

**Deterministik backfill (uygulandı, Neon):** `scripts/backfill-thread-segments.ts` — default DRY-RUN; yalnız `threadSegments IS NULL` + korumasız status; LLM'siz iki strateji (boş-satır blokları / kesintisiz 1..n numaralı işaretler — işaret metni korunur, yeniden yazma yok); tek uzun metin tek segment YAPILMAZ; compare-and-set (`WHERE threadSegments IS NULL`) yarış-korumalı. **Sonuç:** dry-run 13 aday → 11 blank_blocks + 1 numbered split, 1 segment-sınırı-aşımı DOKUNULMADI; `--apply` 12 mutasyon (12'si draftType TWEET→THREAD yükseltmesi, reason kodlu); **ikinci dry-run 0 mutasyon (idempotency kanıtı)**; status/content mutate edilmedi; içerik metni loglanmadı. Kalan 1 kayıt dürüstçe `structureless_thread` needs_edit.

**Kalibrasyon (dürüst durum):** örneklem yeterlilik kriteri DOKÜMANTE: eşik kalibrasyonu için ≥10 judged pozitif VE ≥10 judged negatif etiketli thread örneği. Canlı veri: 0/0 → **calibrationStatus = insufficient_sample; skor eşikleri PROVISIONAL kaldı** (policy adında `-provisional` korunur). Yapısal 280-parity düzeltildi + regression fixture'ları genişletildi; sahte "kalibrasyon tamamlandı" İDDİA EDİLMEDİ. Canlı ücretli LLM çağrısı yapılmadı → canlı model thread çıktısı TEST EDİLMEDİ (2E kapsamı).


### ADR-034 — Faz 2E: Kalıcı eval sistemi, curator production wiring, canonical operator readiness, dürüst trace gözlemi
**2026-07-17 (kullanıcı Phase 2E onayı; OPENROUTER_KEY_ROTATED_AT yokken canlı bölüm BLOCKED-EXTERNAL).**

**Kalıcı eval geçmişi (§B):** Additive `EvalRun` + `EvalCaseResult` (migration `20260717100000_add_eval_run_history`, elle yazılmış SQL + `prisma migrate deploy`; ikinci deploy "No pending" = idempotent; `PipelineTrace(pipelineId, createdAt)` indeksi dahil). EvalTest'in son-sonuç overwrite'ı artık tek audit kaynağı DEĞİL: her koşu `kind`(registry_contract|golden_live|curator_live|thread_smoke) + `mode`(deterministic|live) + `trigger`(manual|cron) + sayaçlar + `policyVersion` + bounded `summaryJson` ile tarihsel adreslenebilir; case'ler `unique(runId, caseKey)` ile upsert (tekrar koşum güvenli). Boyut tavanı 8000 karakter — raw secret/tam prompt/dış platform payload'u saklanmaz. Crash artığı `running` koşular `reconcileStaleRunning` ile `partial + stale_running_reconciled`'a uzlaşır.

**Registry contract runner (§C):** `AgentEvalFixture` artık zorunlu `contract` taşır: `expectedOutcomes` + `deterministic` beklentisi + `liveAllowlisted` + opsiyonel `assertOutput`. Enabled 13 agent'ın tamamının ≥1 contract fixture'ı var (16 fixture). Hermetic koşuda yan etkili adapter'lar (11/13: DB/network/LLM) `hermeticAdapters` mock'larıyla değişir; YALNIZ saf yollar gerçek koşar (originality-critic metin benzerliği; opportunity-curator'ın deterministik kürasyon yolu). Runner: bounded concurrency (4) + per-case timeout (30 sn) + deterministik beklentide çift-koşu karşılaştırması; sonuç EvalRun/EvalCaseResult'a yazılır; exit code gerçek sonucu temsil eder. DÜRÜSTLÜK: hermetic mock geçişi "production agent canlı doğrulandı" iddiası DEĞİLDİR — mode=deterministic etiketi her yüzeyde korunur. Canlı allowlist yalnız opportunity-curator + content-creator (evaluation-mode thread smoke); publish/social-write/memory-write agent'ları canlı eval'e ALINMAZ. CLI: `scripts/run-registry-eval.ts` (`--dry-run`, `--max-cases`, `--live`; secret basmaz). İlk gerçek koşu: 16/16 PASS, $0.00 — 2 case'te Neon pool timeout'u trace yazımını düşürdü ve `traceStatus=failed` + `trace_write_failed` structured log DÜRÜSTÇE gözlendi (sistemin kendisi ilk koşuda kanıt üretti).

**Golden runner modernizasyonu (§D):** `run-eval-tests.ts` hardcoded `["grafikcem","maskulenkod"]` hesap listesi KALDIRILDI — hesaplar testlerin kendi accountId'lerinden çözülür, canlı profil `getRuntimeProfile` (ADR-031 DB source-of-truth). goldenSet'teki iki seeded hesap FIXTURE olarak kalır (production keşfiyle karışmaz). Canlı generate case'leri YALNIZ `--live` + güvenlik kapıları geçerse koşar; aksi halde BLOCKED (anahtar mevcut diye sessiz harcama YASAK — bu pass'te anahtar mevcuttu ve tek canlı çağrı yapılmadı). Her koşu EvalRun'a yazılır; per-case maliyet EvalCaseResult'ta; UsageLog gated primitive'ten `budgetClass: evaluation` ile düşer (üç kayıt tutarlı). Thread sözleşmesi golden'a eklendi: hesap başına deterministik iyi/kötü `MODE: thread_contract` vakası (validateThreadSegments; LLM'siz; skor eşiği DEĞİL yapısal sözleşme). Seed idempotent uygulandı: 4 yeni, 51 mevcut atlandı (55 toplam ≥40 korundu); ilk koşu 4/4 PASS, $0.

**Curator production wiring (§E):** `POST /api/opportunities/curate` (same-origin/cron guard, 256KB body tavanı, Zod, 200-aday sınırı) registry executor üzerinden opportunity-curator koşar; LLM istemciden ÇAĞRILMAZ. Çift kapı: `ENABLE_AGENT_CURATION=1` VE `OPENROUTER_KEY_ROTATED_AT` mevcut — ikisi de yoksa ağ çağrısı OLMADAN AgentBlockedError → executor deterministik fallback. Response dürüst: `method: agent|deterministic` + `fallbackReason`; "agent" YALNIZ gerçek model çağrısı schema+sourceId-binding doğrulamasından geçtiğinde döner; fallback asla "agent üretti" diye sunulmaz. Client (`useOpportunities`) server kürasyonunu dener; route düşerse LOKAL deterministik yol yaşar. `Opportunity.curationMethod` fırsattan handoff'a taşınır (`useHandoffs` hard-coded "deterministic" KALDIRILDI); Fırsatlar kartı + HandoffBand "agent seçimi" rozetini yalnız gerçek agent başarısında gösterir. Yeni sidebar sayfası YOK; dark editorial dil korundu.

**Canonical operator readiness (§F):** `operatorReadinessService`'in bağımsız DB sorgularıyla ürettiği ikinci "hazır/değil" gerçekliği emekli edildi: `OperatorActionReadiness` (level ok|warn|blocked + canGenerate + todayNeedsGeneration + blockers/warnings) canonical health contract girdilerinden SAF türetilir ve `SystemHealthContracts.operatorAction` olarak taşınır. OperatorReadinessGate artık SystemHealthProvider'ın TEK fetch'inden beslenir — 15 sn'lik bağımsız polling + duplicate DB sorguları KALDIRILDI; gate, Sistem sekmesi ve topbar aynı canonical durumdan türediği için çelişkili status İMKÂNSIZ. "Kuyruk tamamlandı" gate'i kızartmaz. `GET /api/settings/operator-readiness` KALDIRILDI (tek tüketicisi gate'ti); `operator-scan-now` POST'u ve servisi (onun motoru olarak) yaşıyor. Kavramlar tek `ready:boolean`'a düzleştirilmedi.

**Trace gözlemi (§G):** Executor artık her koşuda `traceStatus: persisted|failed|skipped_policy` döndürür ve trace yazım hatasında secret'sız structured log (`trace_write_failed` + agentId + subject + errorClass) basar. DÜRÜST SINIRLAMA: process-local `lostTraceCount` global KPI olarak KULLANILMAZ (serverless'ta cross-instance toplanamaz; ana DB düşükken aynı DB'ye "yazamadım" kaydı yazılamaz; ayrı metrics backend YOK). UI dili "gözlenen trace kapsaması": son N eval koşusunun EvalCaseResult.traceStatus'larından persisted/(persisted+failed) — skipped_policy "beklenen" sayılmaz; hiç koşu yoksa null (sahte %100 yok). `pipelineTraceRepo.countByPipelineSince` bounded + indeks-dostu.

**Cron (§H):** Yeni Vercel cron YOK. Deterministik registry contract eval haftalık Pazartesi learn cron'unun MEVCUT slotuna katlandı; idempotency = bu haftaki cron-tetiklemeli registry_contract koşusu varsa atla; fail-open — eval hatası learn ingestion'ı bozmaz, sonuç `registryEval` alanında görünür. Canlı/ücretli eval cron'dan DEFAULT ÇALIŞMAZ (yalnız manuel CLI + kapılar; Phase 2E'de default disabled).

**UI (§I):** Profil > Sistem'e "Agent değerlendirmeleri" bölümü: son deterministic/live koşular, passed/failed/skipped, tazelik, model/harcama, gözlenen trace kapsaması; durumlar hiç-koşmadı/blocked-external/stale(>8g)/partial/passed. Eval stale/blocked ana uygulama outage'ı gibi SUNULMAZ (bölüm en fazla "uyarı"; açıklama metni "üretim kesintisi değildir"). Costs: evaluation bütçe/ay harcaması + son koşu maliyeti + production kürasyon harcaması AYRI kalemler (budgetClass/purpose sınıflandırması); hero KPI kalabalığı eklenmedi. API'ler guard'lı + bounded.

**Canlı smoke (§J) — BLOCKED-EXTERNAL:** `liveSmoke.ts` dört kapıyı (OPENROUTER_KEY_ROTATED_AT + AI_EVAL_SPEND_ENABLED=true + PHASE2E_LIVE_EVAL_APPROVED=true + 0<PHASE2E_LIVE_MAX_USD≤0.50) + katalog + evaluation bütçesini preflight eder; kapılar eksikse HİÇBİR OpenRouter çağrısı yapmadan blocked_external EvalRun kaydeder. Kapılar açıkken: 1 curator live case (executor) + 1 thread smoke (`runDraftPipeline`, evaluation budget, açık thread niyeti, NO-PERSIST: QueueItem yazılmaz, publish yok, X/IG'ye yazılmaz) — assertion'lar: gerçek provider (mock değil), writer+judge provenance, canonical THREAD ≥2 segment ≤280, hash/order tutarlı, readiness kaydı, tavan aşılmadı. BU PASS'TE ÇALIŞTIRILMADI: rotasyon marker'ı yok → canlı aktivasyon BLOCKED-EXTERNAL; "Phase 2 tam canlı doğrulandı" İLAN EDİLMEDİ.

**Güvenlik olayı ve kurtarma (bağlayıcı ders):** Pass başında `prisma migrate diff --shadow-database-url`'e production DATABASE_URL yanlışlıkla geçildi — Prisma shadow olarak verilen DB'yi SIFIRLAR; production Neon boşaldı. Kurtarma: kullanıcı onayıyla Neon PITR (Preview data ile 2026-07-17 09:15 UTC doğrulandı → restore); satır sayıları + `_prisma_migrations` + 12 segmentli thread + readiness dağılımı read-only doğrulandı; boş durum `production_old_2026-07-17T09:15:00Z` branch'inde. KURAL: `--shadow-database-url`'e asla gerçek-veri URL'i verilmez; diff'ler bundan sonra elle SQL + `migrate deploy` ile üretilir. Production DB parolası rotasyonu kullanıcı aksiyonu olarak bekliyor (URL terminale düz metin yazıldı).

## ADR-035 — Phase 3A: DB güvenlik korkuluğu + own-account Instagram gözlemi + insan-onaylı DNA apply (2026-07-17)

**Bağlam:** Phase 2E kapanışı sonrası aynı gün; migrate-diff kazasının kod-seviyesi korkuluğu zorunlu hale geldi. Phase 3A hedefi: bağlı Instagram hesabının GERÇEK içeriklerinden caption/hashtag/format yapısını deterministik gözlemlemek; hiçbir gözlemi insan onayı olmadan identity/DNA kuralına çevirmemek. Yeni tablo AÇILMADI — gözlem hesaplanan read model'dir, apply mevcut CaptionDna/SeriesProfile kolonlarına yazar (migration yok → veri riski yok).

**DB güvenlik korkuluğu (§1, bağlayıcı):** `src/lib/db/urlSafety.ts` — fail-closed saf sınıflandırıcı (yalnız localhost/127.0.0.1/`*.local`/file: ephemeral; diğer her host production-benzeri), `assertSafeShadowUrl` (production-benzeri veya ana-DB-host'lu shadow → throw), `assertSafeDbPushTarget`, `scanMigrationSqlForDestructiveOps` (DROP/TRUNCATE/ALTER..TYPE/koşulsuz DELETE statik taraması). `npm run db:push` artık guard'lı wrapper'dan geçer ve production-benzeri DATABASE_URL'i REDDEDER (canlı kanıtlandı: gerçek env'de exit 2, yalnız sanitize host fingerprint basıldı). Yeni akış `npm run db:migrate`: statik tarama → öncesi read-only snapshot → `migrate deploy` → ikinci deploy "No pending" idempotency → sonrası snapshot + satır-kaybı kontrolü. Prosedür + olay runbook'u: `docs/cemos-rebuild/DB-SAFETY.md`. Testler yalnız SAHTE URL kullanır; hata mesajları/fingerprint credential içermez.

**Single-IG binding sözleşmesi (§2):** IgMedia/IgComment/IgInsightSnapshot GLOBAL kalır (bu oturumda spekülatif multi-account şeması YOK). `resolveSingleInstagramBinding`: tam 1 hesaba bağlı binding → o hesap global IG verisinin açık sahibi; 0 → `config_required` (tahmin yok); >1 farklı hesap → fail-closed `multi_binding_blocked` (çözüm Phase 3 sonrası additive migration: IgMedia.accountId); aynı hesabın composio+meta satırları tek hesap (composio tercihli). Pasif/eksik Account → `account_unresolved`.

**Gözlem katmanı (§3):** `instagramDnaObservationService` — saf, deterministik, LLM'siz, ücretsiz, SIFIR yazma. Kaynak: bağlı hesabın IgMedia corpus'u (≤100 benzersiz; mediaId + normalize içerik-hash dedup; boş caption hariç; sondaki hashtag bloğu gövde istatistiğinden ayrılır). Çıktı: evidenceCount, dateRange, mediaType/hook/CTA dağılımları, uzunluk min/max/medyan, paragraf düzeni, emoji oran+politika, hashtag count-range/placement/casing/core/rotating, sampleSufficiency (MIN_EVIDENCE=10), performans kanıtı üç dürüst durumda (`insights_available` yalnız IgInsightSnapshot gerçekten varsa; `engagement_partial` = yalnız like/yorum + "virallik kanıtı değildir" uyarısı; `unavailable` = metrik YOK, sıfır değil), kaynak sayaçları, warnings. dnaDistillService'in saf yardımcıları yeniden kullanıldı (X-corpus'u ile IG-corpus'u karışmaz — kaynaklar ayrı).

**Governance + apply (§4):** Gözlem otomatik hiçbir şeye dönüşmez. `POST /api/instagram/dna-observation/apply`: istemci YALNIZ alan seçimi + expectedVersion gönderir; uygulanan DEĞERLERİ sunucu güncel gözlemden türetir (sahte değer enjeksiyonu imkânsız). selected-fields-only; boş seçim no-op; yetersiz/kanıtsız alan → 422; stale expectedVersion → 409, ancak değerler zaten uygulanmışsa idempotent `alreadyApplied` (duplicate version yok); cross-account apply → 422 fail-closed. CaptionDna apply provenance="operator" yazar → dnaDistillService'in `skipped_operator_owned` invariant'ı bu satırı bir daha EZEMEZ. SeriesProfile hedefi yalnız seri hashtag override'ı (version+promptVersion bump, mevcut PUT /api/series ile aynı disiplin); hesap kuralı CaptionDna'da kalır (sorumluluk ayrımı). `GET /api/instagram/dna-observation` gözlem / öneri-değerleri / ONAYLI CaptionDna'yı açıkça ayrı bloklarda döner. Her iki route guard'lı + 256KB + Zod + route-guards matrisinde.

**UI (§5):** Yeni ekran/nav YOK — mevcut Plan→Seriler yüzeyine `InstagramDnaSection` eklendi: kaynak/son-sync tazeliği, dağılımlar, dürüst performans satırı, onaylı-DNA ayrımı ("gözlem mekanik yapıdır — onaylanmadan üretim kuralı OLMAZ"), alan-bazlı checkbox'lı apply drawer (hiçbir alan otomatik seçili değil, önce/sonra önizleme, sürüm görünür, 409 kurtarma). Durumlar: loading / config_required / multi_binding_blocked / account_unresolved / no_media / insufficient (apply kapalı) / stale sync / ready / conflict / error.

**Generation grounding (§6):** Üretim YALNIZ onaylı DNA okur — davranış değişikliği yapılmadı, sözleşme testlerle SABİTLENDİ: seri hashtag override'ı carousel prompt'una girer; override boşken hesap HashtagDna'sına sessiz fallback YOK; gözlem tip düzeyinde bile prompt kurucuya giremez; identity bloğu yalnız istenen handle'ın DNA'sını okur (cross-account fallback yasak). Gerçek carousel/Reels üretim akışı Phase 3B'dir.

**Canlı aktivasyon — BLOCKED-EXTERNAL:** `COMPOSIO_INSTAGRAM_CONNECTED_ACCOUNT_ID` + `COMPOSIO_INSTAGRAM_ACCOUNT_HANDLE` env'de yok ve `AccountPlatformBinding` 0 satır → canlı Composio discovery/smoke/sync ÇALIŞTIRILMADI (sıfır Instagram çağrısı). Kullanıcı env'i tamamlayıp `POST /api/instagram/sync` (veya Entegrasyonlar'dan) çalıştırınca binding oluşur ve gözlem gerçek veriyle dolar. Claude Code user-scope Composio MCP kaydı YAPILMADI (API key'i komut satırına literal geçirmeden güvenli kayıt yolu yok; CemOS runtime köprüsü önceliklidir). Sınıflandırma: **"Phase 3A production contract tamamlandı; live Instagram activation BLOCKED-EXTERNAL."** Phase 3 BİTMEDİ; 3B–3E ayrı oturumlar.

## ADR-036 — Phase 3B: onaylı DNA → carousel/Reels üretimi + edit + insan onayı (2026-07-18)

**Bağlam:** Phase 3A gözlem katmanını kurdu; 3B üretimi bu ONAYLI zemine bağlar. Rotation marker'ı hâlâ yok → migration production'a uygulanamaz kuralı + "uygulanmamış kolon bekleyen kod yasak" birlikte **SIFIR-MİGRATION tasarımını** zorladı: üretim provenance'ı PipelineTrace "provenance" stage'inde (typed additive alanlar: seriesKey/seriesVersion/promptVersion/sourceHandoffId/contentHash/account — stagesJson JSON blob, DDL yok); onaylanmışlık TÜRETİLİR (TrainingExample.metricsJson `"dossierId":"<id>"` containment); optimistic concurrency mevcut `updatedAt`; idempotent onay Postgres advisory-lock transaction'ı (`pg_advisory_xact_lock(hashtext(id))`).

**Ürün kapısı (§A):** `getInstagramGenerationGate` — Faz 2E eval kapılarından AYRI (eval onayı ürün onayı DEĞİL): OPENROUTER_KEY_ROTATED_AT + INSTAGRAM_GENERATION_ENABLED=true + INSTAGRAM_GENERATION_LIVE_APPROVED=true + 0<INSTAGRAM_GENERATION_MAX_USD≤0.50. Kapalıyken SIFIR OpenRouter (key-status dahil) + SIFIR verifyWebsite + SIFIR ReelDossier/UsageLog/TrainingExample yazımı; route'lar typed blocked (yalnız ENV adları). `.env.example` default kapalı; kullanıcı env'ine dokunulmadı, onay flag'i uydurulmadı.

**Hesap + onaylı DNA grounding (§B):** hardcoded `grafikcem` VOICE KALDIRILDI — hesap DB'den çözülür + `getRuntimeProfile(requireGenerationReady)`; başka hesaba fallback yok; seri aynı hesapta aktif + version/promptVersion request-başı snapshot (uyuşmazlık → conflict). Grounding yalnız ONAYLI kaynaklardan: identity bloğu (voice+MemoryFact+onaylı CaptionDna/HashtagDna) + ≤5 onaylı/edited seri örneği; topic/örnek/kanıt untrusted fence; gözlenen-onaysız Instagram DNA'sı prompt'a tip düzeyinde bile giremez (3A testleri geçerli).

**Carousel generator (§C):** `generateCarouselEpisode` (purpose `ig_carousel`, mevcut `buildCarouselPrompt` ile) — strict Zod'suz hiçbir üretim kaydı yazılmaz; deterministik doğrulama (slide aralığı/ardışık numara/≤20 kelime/normalize bounded hashtag/banned-repetition/past-topic/kanıtsız fiyat-ücretsiz-istatistik); `near_copy` FAIL-CLOSED (persist yok); araç kanıtı LLM'den ÖNCE, kanıt açılmıyorsa LLM HARCAMASI YOK; 24 saat konu-dedup → aynı handoff retry'ı duplicate dossier üretmez; tek UsageLog (pipeline-runner).

**Reels sertleştirmesi (§D — SÖZLEŞME DEĞİŞİKLİĞİ):** `reelDossierFor` typed outcome union döner; VOICE dinamik; 4 aşamanın HER çıktısı strict Zod'dan geçer; geçmeyen/patlayan aşama üretimi KESER — **yarım dossier yazılmaz** (eski create-then-update fail-open partial davranışı bilinçli kaldırıldı; ödenen maliyet UsageLog+flushed PipelineTrace'te dürüstçe görünür). Per-pass tavan: 4×tahmin ≤ kapı; her aşama öncesi kalan bütçe kontrolü. Evidence gate + readiness'in KOD kararı olması AYNEN korunur. GET listesi hesap-scoped + hesap doğrulamalı.

**Editoryal readiness (§E):** `creativeReadiness` saf modül — ÜÇ AYRI kavram: üretim durumu / site-kanıt doğrulaması (`finalReadiness`) / insan onayı. "Site açılıyor" ≠ "içerik onaylandı"; "model üretti" ≠ "yayına hazır" — UI kopyasında da açık. Araç adlı içerik kanıtsız blocked; bayat kanıt stale uyarısı; kanıtsız somut iddia onayı bloklar.

**Edit/onay (§F/§G):** PATCH optimistic-concurrency (stale→409), sahiplik fail-closed, verification alanları düzenlenemez, server canonical (slayt renumber + hashtag normalize), readiness HER zaman sunucuda. Onay: sahiplik+güncel sürüm+creative ready+kanıt tazeliği+seri promptVersion YENİDEN kontrol; advisory-lock ile concurrent çift onayda dahi TEK TrainingExample (platform=instagram, seriesKey DOLU — kolon Sprint 4'ten beri boştu, CreateTrainingExampleSchema+repo artık taşıyor; outputContent=canonical JSON; bounded provenance metrics; label operatör-edit tespitiyle edited/positive — tespit provenance contentHash karşılaştırması). pastTopicsJson append version bump'sız (anti-repetition hafızası, DNA sözleşmesi değil). Operatör edit'i → best-effort FeedbackEvent (editDistance DÜRÜST null — orijinal metin saklanmıyor, yalnız hash); embedding fail-open.

**UI (§H) + slot sözleşmesi (§I):** Yeni ekran/nav YOK. Seriler'e `CarouselStudio` (aday konudan "Bölüm üret" prefill + üç-durum rozetli review drawer + slide editörü + Kaydet/Onayla); Takvim'de dossier'siz slot `SlotDossierActions` ("Dossier hazırla" + AYRI açık "Slota bağla" — slot sahte hazır görünmez), bağlı dossier `DossierDetailPanel` tam detay. `POST /api/reels/plan/slot/[id]/attach`: atomik claim (yalnız boş slot), idempotent re-attach, cross-account fail-closed — `ReelPlanSlot.dossierId` İLK KEZ gerçekten yazılıyor (önceden ölü kolondu). Başarısız üretim handoff'u tüketmez (handoff zaten consumed-candidate; üretim yalnız provenance referansı taşır).

**Canlı smoke (§J) — YAPILMADI:** rotation + üretim kapıları kapalı → SIFIR OpenRouter/site çağrısı; canlı carousel+Reels smoke BLOCKED-EXTERNAL. Kapılar açılınca sıra: bütçe preflight → tek carousel → kalan bütçe uygunsa tek Reels → edit preview; GERÇEK approval kullanıcı onayı gerektirir. Sınıflandırma: **"Phase 3B production contract tamamlandı; live generation activation BLOCKED-EXTERNAL."** Phase 3 BİTMEDİ; 3C–3E ayrı oturumlar.

## ADR-037 — Phase 3C: rakip/ilham kütüphanesi + dürüst deterministik yapısal analiz (2026-07-18)

**Bağlam:** Kullanıcı beğendiği rakip Reels/carousel'leri CemOS'a kaydedip "neden çalışıyor" açıklaması istiyor; Meta business_discovery bağlantısı YOK (META_IG_USER_ID absent) ve OpenRouter rotation/üretim kapıları kapalı. Ürün Meta'sız kullanılamaz hale GELMEMELİ. Keşif, İlham yüzeyinin gerçek açıklarını doğruladı: `accounts[0]` gizli varsayımı, `window.prompt`, iki ayrı request'le orphan ContentItem riski, board ownership kontrolü yok, manuel IG URL `platform="manual"`, BoardItem dedup yok, ücretsiz analiz ile ücretli Idea üretimi karışık, LLM çıktısı runtime doğrulamasız, outlier skoru bayat baseline'la + EPSILON bölmeli şişik çarpan, insufficient çarpan sayı gibi gösteriliyor, Boards/Content GET'leri guard'sız. **SIFIR migration** (mevcut `BoardItem.metaJson` versioned Zod zarfı; probeStatus String kolonu yeni `config_required` değerini DDL'siz taşır).

**Capture sözleşmesi (§A):** `captureInspiration` — aktif accountId zorunlu; board sahipliği fail-closed (cross-account VE paylaşılan `accountId=null` board'a capture reddi); `canonicalizeInstagramUrl` saf canonicalization (yalnız http/https + gerçek IG host'ları; `/reel|/reels|/p|/tv` + `/{user}/…` varyantları; query/fragment atılır; `share/` fetch'siz çözülemez → dürüst red; scraping/fetch YOK); externalId `shortcode_<code>` (Meta numeric media-ID uzayıyla çakışmaz) yoksa `urlhash_` fallback; ContentItem upsert + BoardItem TEK transaction + `pg_advisory_xact_lock` (Phase 3B deseni) → çift tık/retry duplicate üretmez, başarısız transaction orphan bırakmaz; varsayılan "Instagram İlham" panosu idempotent; manuel metrik YALNIZ zarfda `provenance="operator_observed"`+observedAt ile yaşar — ContentItem.metricsJson'a yazılmaz, provider metriğiyle karışamaz. `recomputeBaseline` artık `sourceType="external"` filtreler (manuel capture medyanı kirletemez). Zarf her okumada `parseInspirationMeta` fail-closed (bozuk/yabancı → null; `rawMetadataJson` çöplüğe dönüşmez).

**Deterministik analiz (§B):** `analyzeInspirationStructure` — SAF (DB/ağ/LLM yok), strict Zod, versioned `inspiration_structure.v1`, ÜCRETSİZ. Çıktı gözlenen-gerçek / yapısal-hipotez AYRIMI + hook/açılış/CTA/hashtag/satır yapısı/değer vaadi/kanıt-durumu/aktarılabilir ilkeler/kopyalanmayacaklar/kopyalama riski/confidence/evidenceBasis/limitations. Dürüstlük kuralları KODDA: caption yoksa caption yapısı uydurulmaz; media URL varlığı piksel analizi SAYILMAZ (sınırlama satırı zorunlu); "neden çalıştı" dili YALNIZ provider outlier kanıtı yeterliyken (`insufficient=false` + çarpan ≥1.5) — aksi "neden çalışabilir/hipotez"; manuel metrik tek başına "çalıştı" kanıtı değil; kaynak caption untrusted (deterministik analizör talimat uygulayamaz; çıktıya ≤80 karakter kanıt parçası dışında verbatim kopya girmez). Persist: `analyzeService` sahiplik fail-closed + versioned çıktı `BoardItem.metaJson` zarfında KALICI (analysisStatus bayrağında kaybolmaz); zarfsız legacy kayda ContentItem'dan dürüst bootstrap.

**Watchlist/outlier (§C):** Meta business_discovery TEK otomatik rakip kaynağı kalır; Composio own-account köprüsü rakip verisi İÇİN KULLANILAMAZ (toolkit'te business_discovery yok — ADR-032). Scraping/HTML parse/gizli endpoint/browser-automation YASAK kalır. `addWatchAccount`: handle normalize+validate; Meta yapılandırılmamışken `not_configured` artık "private hesap olabilir" YALANI değil dürüst `config_required` probeStatus'u. `syncIgCompetitors` İKİ AŞAMALI: (1) medya upsert + pencere değerleri, (2) format-medyan baseline'ı GÜNCEL değerlerle yaz, (3) skorlar TAZE baseline ile (eski akış ilk sync'te bayat/sıfır medyanla EPSILON bölmeli şişik çarpan basıyordu). `insufficient` (örneklem <5 veya medyan≤0) → DB'de multiplier=0 + insufficient=true; API katmanı bunu `null` döner (sahte çarpan gösterilmez); explanationJson window/sampleSize/metricProvenance="meta_business_discovery"/formula/computedAt taşır. Sync yapılandırma yoksa `skipped="meta_config_required"` fail-fast (sıfır çağrı, sahte hata listesi yok). Watchlist GLOBAL — API `scope:"global"` + UI "CemOS ortak rakip listesi" (hesap-scoped algısı yaratılmaz).

**Ücretsiz/ücretli ayrımı (§D):** "Yapısal analiz" = ücretsiz deterministik; "Fikre dönüştür" = AYRI insan eylemi, ücretli. `reverseEngineerToIdea` sertleşti: Instagram içeriği için ADR-036 `getInstagramGenerationGate` ZORUNLU — kapı kapalıyken typed `ReverseEngineerBlockedError` (yalnız ENV adları), SIFIR ağ/key-status/Idea/UsageLog/analysis-state; model çıktısı runtime Zod (`ReverseEngineerAnalysisSchema`) — geçersiz çıktı `ReverseEngineerInvalidOutputError` + Idea YAZILMAZ; 24 saat idempotency (IdeaSource contentItemId+account+promptVersion) → retry duplicate maliyet/Idea üretmez, mevcut Idea `reused` döner. Çıktı Idea'dır, QueueItem/draft DEĞİL; Instagram fikri X/Bugün akışına gitmez — mevcut `sourceKind="radar"` OpportunityHandoff ile Seriler/Takvim'e açık eylem sunulur (yeni workflow modeli İCAT EDİLMEDİ; üretim/onay Phase 3B kapılarında kalır).

**UI (§E) + güvenlik:** Yeni ekran/nav YOK — mevcut Kütüphane→İlham (`lib-ilham`) yüzeyi Inspiration Intelligence çalışma alanına dönüştü, tek dev TSX yerine `src/components/library/ilham/` altında bölünmüş bileşenler (header/capture/grid/detay/rakip-özet + tek-GET `useIlhamWorkspace`). Aktif hesap Takvim deseniyle explicit Select; `window.prompt` kalktı (pano drawer'ı); arama+format+provenance+analiz filtreleri; detayda gözlenen-gerçek vs hipotez, "neden çalıştı/çalışabilir" ayrımı, metrik provenance, sample size, kopyalama riski; sahte thumbnail/chart/KPI yok (format ikonu). Durumlar: loading/empty/error/no-account/no-board/manual-only/provider-blocked/never-synced/stale/insufficient/deterministic-ready/AI-blocked/AI-ready. Route'lar: `GET /api/inspiration` + `POST /api/inspiration/capture` + `POST /api/inspiration/analyze` guard'lı + bounded + strict Zod + typed hata kodları; önceden GUARD'SIZ kalan `GET /api/boards`, `GET /api/boards/[id]`, `GET /api/content` operatör guard'ına alındı; hepsi route-guards matrisinde.

**Canlı durum — KANITA GÖRE DÜZELTİLDİ:** Oturum direktifi "Meta business_discovery izni/bağlantısı yok" diyordu; gerçek kanıt AKSİNİ gösterdi: META_ACCESS_TOKEN + META_IG_USER_ID env'de MEVCUT (@next/env yüklemesiyle doğrulandı; ilk PowerShell env taraması yanlış-negatifti), watchlist 24/24 `probeStatus=ok`, `lastSyncAt=2026-07-18T06:39Z` (production günlük cron BUGÜN business_discovery sync'i çalıştırmış), 1017 ContentOutlierScore mevcut. **Meta rakip senkronu FİİLEN ÇALIŞIYOR — BLOCKED-EXTERNAL DEĞİL.** (Not: mevcut outlier satırları ESKİ tek-aşamalı kodun ürünü; iki-aşamalı dürüst skorlar bu branch deploy edilince ilk sync'te yeniden yazılır. Bu oturumda deploy YOK, elle canlı Meta çağrısı da YAPILMADI — kanıt read-only DB'den.) Phase 3A own-account gözlemi AYRI konu ve hâlâ blocked (AccountPlatformBinding=0, Composio env'leri yok). Rotation + INSTAGRAM_GENERATION_* yok → canlı AI uyarlaması ÇALIŞTIRILMADI (sıfır OpenRouter, $0). Manuel ilham yakalama + deterministik yapısal analiz Meta'dan bağımsız TAM çalışır. Sınıflandırma: **"Phase 3C production contract tamamlandı; manuel ilham yakalama, deterministik yapısal analiz ve Meta rakip senkronu (mevcut canlı akış) hazır. Canlı AI uyarlaması BLOCKED-EXTERNAL (rotation + üretim kapısı env'leri)."** Phase 3 BİTMEDİ; 3D–3E ayrı oturumlar.

## ADR-038 — Phase 3D: doğrulanmış site/araç → production-grade dossier ↔ Takvim (2026-07-18)

**Bağlam:** Takvim'de bir Reels slotu açan kullanıcı ham konu değil production-grade dossier görmeli: araç/URL/redirect/kanıt tazeliği, yaratıcı içerik, insan onayı ve takvim bağı — hepsi AYRI kavramlar. Keşif gerçek açıkları doğruladı: `verifyWebsite(persist:true)` DB yazımı başarısız olsa bile kanıt döndürüyordu (fail-soft persist → named-tool verificationId'siz "ready" olabilirdi), ham exception `reason`'da UI'a sızıyordu, snapshot reuse/idempotency yoktu, manuel re-verify endpoint'i yoktu, dossier durumu kopyalanmış `verificationEvidenceJson`+expiry'den okunuyordu (gerçek WebsiteVerification satırı hiç yeniden okunmuyordu), `alternativesJson` fiilen ölü/şemasızdı, Takvim yalnız yeni üretilmiş local `generatedId`'yi bağlayabiliyordu, aynı dossier birden fazla slota bağlanabiliyordu, seri-slot ↔ dossier provenance uyumu kontrol edilmiyordu, detach yoktu, `drafted` statüsü "yayına hazır" algısı yaratabiliyordu. **SIFIR migration** — mevcut `WebsiteVerification`, `ReelDossier.verificationId/verificationEvidenceJson/alternativesJson/expiry`, `ReelPlanSlot.dossierId`, `PipelineTrace` alanları yeterli.

**Verifier sözleşmesi (§A):** Tier-1 deterministik/LLM'siz kalır. Başarısızlık artık TYPED kod: `invalid_url|ssrf_blocked|redirect_blocked|redirect_hop_limit|robots_disallowed|timeout|unreachable|persistence_failed|unknown_failure` — ham exception/iç host UI'a sızmaz (sabit Türkçe mesaj haritası). HTTP 4xx/5xx (404/451 dahil) başarısızlık DEĞİL `opens:false` typed kanıttır; 451 → `regionRestricted:true`, onun DIŞINDA Türkiye erişimi İDDİA EDİLMEZ. robots.txt okuma BOUNDED (64KB stream kesme). **Yük taşıyan invariant:** `persist:true` iken DB yazımı başarısızsa sonuç `persistence_failed` — yalnız bellekteki HTTP sonucu kanıt SAYILMAZ; `computeReadiness` artık `verificationId` PARAMETRELİ: named-tool, kalıcı satır + strict kanıt + opens + taze olmadan ASLA ready olamaz (dossier-generator, carouselGenerator, dossierReviewService aynı kuralı kullanır). SSRF suite aynen: http/https-only, URL credential reddi, literal+DNS private/loopback/link-local/metadata reddi, redirect otomatik takip YOK (her hop guard'dan), hop≤5, timeout 10s, secret/cookie header gönderilmez.

**Snapshot/reuse defteri (§B):** `WebsiteVerification` append-only kanıt defteri — eski satır ASLA overwrite edilmez, her canlı doğrulama YENİ satır (audit korunur). `verifyWebsiteWithReuse`: `forceRefresh:false` iken aynı `url` için ≤24h taze + strict-parse edilebilir + expiry gelecekte + satır/zarf tutarlı son snapshot reuse edilir (`reused:true`); bozuk/bayat/uyumsuz snapshot fail-closed atlanır → canlı doğrulama. Kullanıcının "Yeniden doğrula" eylemi DAİMA force-refresh.

**Re-verification lifecycle (§C):** `POST /api/reels/dossier/[id]/verify` (guard + 64KB + strict Zod + optimistic concurrency). URL SERVER-SIDE çözülür (primaryToolJson / alternatives zarfı) — istemci keyfi URL veremez; endpoint fetch-proxy DEĞİLDİR. LLM YOK, generation gate GEREKMEZ. Ağ doğrulaması transaction DIŞINDA; yazım `pg_advisory_xact_lock("reel_verify:<id>")` + in-tx expectedUpdatedAt yeniden kontrolüyle KISA transaction'da — yarışı kaybeden sonuç mevcut durumu EZEMEZ (stale/409). Başarı: yeni snapshot + `verificationId`+`verificationEvidenceJson`+`expiry`+`finalReadiness` ATOMİK güncellenir. Başarısızlık: HİÇBİR alan yazılmaz (eski kanıt/audit korunur, bayat kanıt "taze" yapılmaz); her deneme bounded/redacted PipelineTrace izi (`reel_dossier_reverify`, typed kod, URL yok) bırakır.

**Production-state read model (§D):** `computeDossierProductionState` SAF + tek kaynak; UI ve API AYNI çıktıyı kullanır (`GET /api/reels/dossier/[id]` → `production` bloğu). ALTI katman ayrı: generation / primary evidence / alternative evidence / creative / human approval / calendar. Primary evidence YALNIZ gerçek WebsiteVerification satırından türetilir (kopyalanmış JSON girdi değildir): verificationId yok→`missing`, satır yok→`missing`, strict parse fail→`failed`, satır URL'i ≠ primary tool URL→`missing(url_mismatch)` fail-closed, opens=false→`failed`, expiry<now→`stale`, aksi `ready`; araç yoksa `no_tool_required`. Overall: `evidence_failed|evidence_missing|evidence_stale|creative_needs_edit|series_contract_changed|awaiting_human_approval|attached_not_ready|approved|production_ready`. **production_ready = kanıt(ready|no_tool) + creative ready + insan onayı + seri sözleşmesi geçerli + TAM BİR aktif slot**; birden çok slot `multi_slot_attachment` blocker'ı. Stored `finalReadiness` yalnız snapshot/audit. Tier-1 sinyalleri (signup/free/limit/TR) `unknown` KALIR — keyword'den free-tier türetilmez.

**Alternatif zinciri (§E):** `alternativesJson` = versioned strict Zod zarfı (`schemaVersion:"1"`, ≤4 aktif; legacy/bozuk → fail-closed boş + dürüst `parseFailed`). Her alternatif KENDİ WebsiteVerification satırıyla doğrulanır (verify route target=alternative; primary kanıt alanlarına DOKUNULMAZ — kanıt kopyalama yasak); manuel "verified" işareti YOK (evidence durumu `alternativeEvidenceState` ile türetilir: unverified/ready/stale/failed). Add/archive `POST /api/reels/dossier/[id]/alternatives` — advisory lock + ownership + concurrency; duplicate URL reddi; archive non-destructive (status+archivedAt, fiziksel DELETE yok, idempotent). Alternatif primary'nin yerini SESSİZCE ALAMAZ (script eski aracı anlatabilir) — güvenli yol "bu araçla YENİ dossier üret" prefill'i; o üretim ADR-036 kapısına tabidir, kapalıyken yalnız ENV adları gösterilir ve mevcut dossier DEĞİŞMEZ.

**Takvim attach/detach (§F):** Attach artık MEVCUT account-scoped dossier listesinden de yapılır (yalnız yeni-üretilen local id değil). Sözleşme: cross-account fail-closed; seri slotu → dossier PROVENANCE'ı (PipelineTrace seriesKey) slot seriesKey'iyle eşleşmeli (başlık/pillar string tahmini DEĞİL; provenance'sız dossier seri slotuna bağlanamaz); aynı dossier ikinci aktif slota bağlanamaz (`pg_advisory_xact_lock("slot_attach:<dossierId>")` + in-tx kontrol → 409 `dossier_already_attached`); atomic claim yarışı 409; idempotent alreadyAttached. **Bağlama PLANLAMA eylemidir**: yanıt `productionReady`+`overall`+`blockers` döner; `drafted` = "dossier bağlandı", yayına hazır DEĞİL — UI dürüst uyarı gösterir. `POST .../detach`: expectedDossierId fail-closed, `done` slot çözülmez, dossier FİZİKSEL SİLİNMEZ, slot `planned`'a döner, idempotent; taşıma = detach + attach (iki açık eylem).

**Tier-2 sınırı (§G):** Playwright/browser render doğrulaması Vercel serverless'a EKLENMEDİ — güvenli bounded operator-worker sözleşmesi yok; sahte "tamamlandı" gösterilmez. Tier-1 production contract tamamlandı; Tier-2 sinyalleri `unknown` kalır (451 istisnası). UI kopyası bunu açıkça söyler ("sayfa render edilmedi, ekran görüntüsü alınmadı").

**UI (§H):** Yeni ekran/nav YOK. Ortak `DossierProductionPanel` (API `production` bloğunu AYNEN render eder — istemci readiness hesaplamaz): 6-katman checklist, kanıt kartı (girilen/final URL, redirect zinciri, son kontrol, expiry, taze/bayat/başarısız/eksik, url-mismatch uyarısı), dürüst unknown satırları, "Yeniden doğrula", alternatif listesi (ekle/doğrula/arşivle/yeni-dossier), gate-blocked kutusu. TakvimDossierPanels: mevcut-dossier seçici + attach-warning ("bağlandı ama YAYINA HAZIR DEĞİL" + blocker kodları; drawer bilinçli açık kalır) + açık detach. CarouselStudio review drawer'ına aynı panel eklendi.

**Canlı durum:** Production'da ReelDossier=0 ve WebsiteVerification=0 → canlı re-verification MUTASYONU koşacak gerçek dossier yok; akışlar hermetic unit+route+e2e ile kanıtlandı (mock fetch/route — gerçek dış site çağrısı YAPILMADI, production'a fixture YAZILMADI). OPENROUTER_KEY_ROTATED_AT + INSTAGRAM_GENERATION_* hâlâ absent → canlı AI dossier üretimi BLOCKED-EXTERNAL ($0; OPENROUTER_API_KEY bu oturumda PRESENT görüldü ama rotation kanıtı olmadan kapı bilinçli kapalı). Meta durumu DEĞİŞMEDİ (ADR-037: çalışıyor). Sınıflandırma: **"Phase 3D production contract tamamlandı; deterministik site doğrulama, kanıt yenileme, alternatif araç zinciri ve dossier↔Takvim üretim durumu hazır. Canlı AI dossier üretimi BLOCKED-EXTERNAL; production'da gerçek dossier bulunmadığından canlı re-verification mutasyonu yapılmadı."** Phase 3 BİTMEDİ; 3E ayrı oturum.

## ADR-039 — Phase 3E: aylık Reels planı + güvenli takvim yerleşimi + uçtan uca plan sağlığı (2026-07-19)

**Bağlam:** Kullanıcı bir ay için Reels planı kurarken gerçek yayın günleri, 3–5 sütun, aktif seriler (bölüm sayısıyla), sezonluk konular ve mix dağılımını DB'ye yazmadan önce görebilmeli; mevcut işlenmiş slotların korunacağını, çakışmaları ve tekrarları anlayabilmeli; planı bilinçli uygulayıp AYRI bir eylemle aktive edebilmeli; slotları dossier üretim durumuyla takip edebilmeli. Keşif gerçek açıkları doğruladı: `POST /api/reels/plan` doğrudan DB'ye yazıyordu (preview yok), optimistic concurrency yoktu, upsert+`deleteMany(status:"planned")`+createMany tek transaction DEĞİLDİ ve **planned slotları FİZİKSEL SİLİYORDU** — bu, `consume-plan`'in ürettiği `planned` handoff slotunu yeniden montajda silip `OpportunityHandoff.resultRef`'i ORPHAN bırakıyordu; `notesJson` yalnız `string[]` uyarıydı (revision/fingerprint/plan girdisi yok); idempotent no-op yoktu; SeriesProfile ownership/aktiflik/format doğrulanmıyordu, `seriesKey` ham slug UI'a sızıyordu; repetition yalnız son 7 slotta pillar sayıyordu (seri/konu/araç/hook/geçmiş yok); Şubat 30/31 kabul ediliyordu; `ReelPlan.status` draft/active/archived kullanılmıyordu; slot move/skip/restore yoktu; consume-plan gün doluluğunu/ay sınırını kontrol etmiyordu; plan sağlığı yalnız `expiry<now` stale bayrağıydı ve health sistemine bağlı değildi. **SIFIR migration** — mevcut `ReelPlan.status/mixJson/notesJson/updatedAt`, `ReelPlanSlot.status/updatedAt/dossierId/seriesKey`, `OpportunityHandoff.resultRef`, `WebsiteVerification`, `ReelDossier`, `PipelineTrace` yeterli.

**Assembler derinleştirme (§6):** Saf/deterministik/LLM'siz/`Date.now`-siz kalır. Ayın GERÇEK gün sayısı `calendarGrid.daysInMonth` ile doğrulanır (Şubat 30/31 reddi). Yeni ayrı SAF modül `repetition.ts`: çok boyutlu histogram (pillar/seriesKey/normalized topic/canonical tool URL/hook shape) — yeni slotlar + yakın geçmiş (mevcut ay + önceki ~90 gün ReelPlanSlot/ReelDossier + SeriesProfile.pastTopics) sinyalleri üzerinden; mevcut `normalizeTurkish`/`isNearDuplicate` util'leri REUSE (embedding zorunlu değil). Korunan slotların günleri yeni base slot üretiminden DIŞLANIR ve sinyalleri histograma girer. `bannedRepetition` EXACT normalize eşleşme = hard blocker; near-duplicate + `pastTopics` yakınlığı = uyarı. Seri slotları görünen ad taşır (topicHint = "Seri bölümü: <ad>", ham seriesKey DEĞİL). Seasonal konu yetmezse uydurma YOK — dürüst uyarı. Mix hedefi yeni slotlar üzerinden, küçük slot sayısında matematiksel imkânsızlık dürüst warning.

**Preview → apply → activate (§7):** `POST /api/reels/plan/preview` SIFIR DB write — eklenecek/güncellenecek/korunacak/skipped slot diff'i, çakışmalar, histogram, uyarılar, hard blocker'lar, fingerprint (sha256, sıra-bağımsız canonical girdi), `expectedUpdatedAt`. `POST /api/reels/plan/apply`: fingerprint bütünlüğü (preview≠apply girdisi → 409), optimistic concurrency (`expectedUpdatedAt` tx-dışı ön + tx-içi yeniden kontrol → yarış EZMEZ), `pg_advisory_xact_lock("reel_plan:<acc>:<month>")`, TEK transaction, idempotent no-op (create/update/skip=0 → revision bump yok). **NON-DESTRUCTIVE reconcile** (`reconcileSlots`, saf): assembler-yönetimli (planned/skipped, korunmasız) slotlar güne göre eşlenir — aynı günde varsa güncellenir, yoksa oluşturulur; yeni planda olmayan yönetilen `planned` slotlar `status="skipped"` olur (FİZİKSEL DELETE YOK). KORUNAN slotlar (drafted/done, `dossierId!=null`, veya consumed `OpportunityHandoff.resultRef`-referanslı) asla silinmez/skipped yapılmaz → handoff slotları artık ORPHAN OLMAZ. `notesJson` = versioned strict Zod zarfı (`plan_notes.v1`: revision/fingerprint/canonical girdi özeti/warnings/hardBlockers/histogram özeti/appliedAt/source/method); legacy `string[]` OKUNUR. Seri DB'den çözülür (`resolveSeriesForPlan`): ownership+aktiflik+format (carousel/reel/single) doğrulanır, görünen ad/format/pastTopics/banned DB TRUTH'tan (client override edemez), pillar plan sütunlarından olmalı. Lifecycle `POST /api/reels/plan/lifecycle`: server state machine (draft↔active↔archived; geçersiz geçiş 422); apply OTOMATİK active YAPMAZ; aktivasyon açık kullanıcı eylemi + uyarı varsa açık `acknowledgeWarnings`; archive non-destructive; archived→draft geri alınabilir; istemci status'u doğrudan yazamaz. Legacy `POST /api/reels/plan` artık aynı non-destructive reconcile'a delege eder (destructive `deleteMany` KALDIRILDI).

**Handoff/slot güvenliği (§8):** `consume-plan` sertleşti: gün ayın gerçek sınırında (Şubat 30 → `invalid_day`), plan archived → `plan_archived`, aynı günde BOŞ assembler slotu varsa yeni satır yerine O slot reuse edilir (başka handoff'a aitse gasp etmez), gün dolu (drafted/done/dossier/başka-handoff) → 409 `day_occupied`; consume claim + slot yazımı `pg_advisory_xact_lock("plan_day:<plan>:<gün>")` tek transaction (slot yazımı başarısızsa handoff consumed KALMAZ; retry idempotent; resultRef orphan olmaz). Yeni slot ops: `move` (gün değişir, dossier bağlantısı KORUNUR, done taşınamaz, hedef gün dolu → 409, ay-gün doğrulanır, tx+advisory-lock+in-tx recheck), `skip` (non-destructive status; done atlanamaz; idempotent alreadySkipped; atomic updateMany + done guard), `restore` (skipped → planned; dossier varsa drafted) — hepsi account ownership + optimistic concurrency + atomic claim. Dossier/handoff hiçbir işlemde silinmez. `done` = "operatör tarafından tamamlandı" (IG publish iddiası DEĞİL; PublishedPost/Meta kaydı üretilmez).

**Plan health contract (§9):** Saf tek-kaynak `deriveInstagramPlanHealth` Phase 3D `computeDossierProductionState` çıktısını REUSE eder (ikinci readiness sözlüğü İCAT ETMEZ). `planHealthService` aktif (yoksa en güncel) planı okur, bağlı dossier'lerin production durumunu `getDossierProductionState` ile türetir, canlı collision sayar, notes zarfından uyarıları alır; fail-soft → `unknown`. Anlam korkulukları: plan yokluğu ALTYAPI HATASI DEĞİL (not_configured, kırmızı yapmaz); draft/archived nötr; bayat kanıt OUTAGE DEĞİL (warn, error değil); `production_ready` yalnız TÜM 3D kapıları geçince; veri alınamıyorsa "healthy" UYDURULMAZ (unknown). Taşıdığı: configured/planStatus/month, active/protected/skipped/dossiersiz/attached_not_ready/production_ready sayıları, evidence missing/stale/failed, awaiting approval, series changed, collisions, repetition, mix deviation, overdue/today/next-7-days unready, nextActionable, blockers, warnings, status ok/warn/error/unknown.

**Health entegrasyonu (§10):** Üç canonical altyapı domain'i (infrastructure/pipelineFreshness/todayReadiness) BOZULMADI; Instagram plan sağlığı `contracts.instagramPlanning` olarak ADDITIVE eklendi (infrastructure'ı EZMEZ — AYRI ürün/görev sözleşmesi). `SystemHealthProvider` TEK `/api/health` fetch kaynağı kalır; Takvim şeridi ve Sistem bölümü AYNI canonical sonucu okur (duplicate health türetimi YOK). `deriveTopbar` önceliği: infra error > infra warn > pipeline > bugün akışı > **aktif plan bugün/geride kalan** > **aktif plan yaklaşan** > yok; not_configured/draft/archived plan topbar uyarısı ÜRETMEZ. Profil→Sistem'e "Instagram içerik planı" bölümü (ayrı sözleşme olduğu açıkça yazılı, Takvim'e deep-link).

**Takvim/Sistem UI (§11):** Yeni ana nav/ekran YOK. `PlanBuilder` (Takvim builder drawer'ı): pillars/sıklık/aktif-seri(DB adı, ham slug DEĞİL, Toggle+bölüm+pillar)/sezonluk → Önizle (sıfır-write preview paneli: eklenecek/güncellenecek/korunacak/skipped/çakışma/mix/histogram/uyarı/hard-blocker) → Planı uygula (fingerprint+expectedUpdatedAt; hard blocker varsa disabled) → AYRI aktive/arşivle/geri-al (uyarı ack toggle'ı); `window.prompt` YOK. `PlanHealthStrip`: canonical `instagramPlanning`'i okur (Sistem ile aynı kaynak; dekoratif KPI/sahte yüzde yok; yapılandırılmamışsa gizli; ay uyuşmazlığı dürüst not). `SlotOpsBar` (slot drawer): taşı(hedef gün)/atla/geri-al; done → "operatör tamamladı" nötr. Instagram filtre metni düzeltildi: Meta business_discovery = rakip OKUMA çalışıyor; IG YAYIN/ZAMANLAMA ayrı write yeteneği ve CemOS içinden desteklenmiyor; Reels planı IG'ye yayın yapmaz, üretim planıdır. ReelDossier=0 gerçek DB'de slot drawer dürüst "dossier bekliyor" (sahte hazır içerik yok). SystemTab yeni "Instagram içerik planı" bölümü (üç ALTYAPI sözleşmesinden AYRI ürün/görev sözleşmesi olduğu vurgulu).

**Uçtan uca (§12):** Hermetic Phase 3 journey testi (saf read-model zinciri): plan preview reconcile → doğrulanmış+onaylı dossier attach → 3D production_ready → 3E plan health READY → kanıt expiry simülasyonu → plan health WARN (outage değil) → reverify (taze kanıt) → READY; deterministik, SIFIR dış çağrı. "Mock/deterministik geçti" ASLA "canlı doğrulandı" DEĞİLDİR. Cross-account/concurrency/retry servis+contract testlerinde (planReconcileService.db, planSlotService, consume-plan.contract, dossierProductionService).

**Canlı durum:** Production'da ReelDossier=0/WebsiteVerification=0; mevcut 1 draft ReelPlan (10 planned slot, 0 dossier). Tüm mutasyonlar hermetic mock/harness ile kanıtlandı — production'a plan/slot/dossier/verification/fixture YAZILMADI; gerçek browser doğrulaması read-only. SIFIR OpenRouter ($0), SIFIR gerçek dış site/Meta çağrısı bu oturumda. OPENROUTER_KEY_ROTATED_AT + INSTAGRAM_GENERATION_* hâlâ absent → canlı AI üretimi BLOCKED-EXTERNAL. Meta business_discovery çalışıyor (ADR-037, değişmedi). Phase 3A own-account Composio binding + Tier-2 render doğrulaması ayrı ve blocked. Sınıflandırma: **"Phase 3E production contract tamamlandı; aylık plan preview/apply/lifecycle, non-destructive takvim yerleşimi, güvenli slot/handoff operasyonları ve tek-kaynak plan sağlığı hazır. Phase 3'ün Instagram içerik zekâsı production contract'ı (3A–3E) tamamlandı; ancak canlı AI üretimi, own-account Composio aktivasyonu ve Tier-2 render doğrulaması ilgili dış bağımlılıklar nedeniyle TAM CANLI DEĞİLDİR."** Sonraki faz: Phase 4.

## ADR-040 — Phase 4A: kabiliyet dağıtımı + bilgi yoğunluğu (üç TOP-LEVEL, gizli değil) (2026-07-19)

**Bağlam:** Kullanıcı kararı (2026-07-19): rebuild teknik olarak temiz ama uygulama görsel/işlevsel olarak fazla boş/çıplak — sidebar ortası/dibi boş, güçlü özellikler sekme/Profil/Cmd+K arkasında saklı, Takvim hücreleri yalnız turuncu nokta, kartlar büyük çerçevede az bilgi, 1440/1920 ana içerik alanı değerlendirilmiyor. Referans yapısal görsel (marka değil): sidebar bölümleri, bilgi yoğunluğu, ikon disiplini, kart anatomisi, çalışma alanı kompozisyonu. (Not: bu oturumda referans görsel yolunda bulunamadı — yazılı yapısal spec otoriter alındı.)

**Karar:** CemOS **yalnız üç TOP-LEVEL görev alanı** korur (Bugün/Plan/Kütüphane); fakat mevcut kabiliyetler hiyerarşik alt-navigasyon, ayrı "Araştırma" grubu, gerçek durum özetleri ve bağlamsal çalışma alanlarıyla GÖRÜNÜR olur. **Minimalizm, işlevlerin saklanması veya büyük boş alanlar anlamına gelmez.** Eski kesin "ana navigasyonda yalnız üç öğe görünür" kabulü artık "yalnız üç TOP-LEVEL alan" olarak okunur (ADR-005/019 revize). Tema/marka sıfırlaması YOK: dark editorial, Plus Jakarta Sans, AppIcon/Lucide, terracotta, near-black rail korunur.

**Teslim (Phase 4A — capability distribution + information density):**
- **Sidebar yeniden dengelendi** (`Sidebar.tsx`): marka → hesap kartı → 3 top-level alan (aktif alan sidebar'da alt hedeflerini AÇAR: Plan→Takvim/Fırsatlar/Seriler · Kütüphane→Tümü/İlham/Öğrenme) → **hiyerarşik "Araştırma" grubu** (Haberler/YouTube/Viral Radar/Keşif/X Kaynakları — katlanabilir ama masaüstünde keşfedilebilir; artık yalnız Fırsatlar/Cmd+K arkasında değil) → "Araçlar" (Toolbox) → **"Şimdi" özeti** → Profil.
- **"Şimdi" modülü** (`SidebarNowModule.tsx`): Profil üstünde, YALNIZ canonical `SystemHealthProvider` verisinden (TEK fetch reuse; ikinci readiness sözlüğü YOK; N+1 YOK; sahte sayı YOK): yayına hazır taslak / karar bekleyen iş / aktif planın hazır-toplam / en yüksek öncelikli sistem sinyali. Veri yoksa dürüst yükleniyor/alınamadı.
- **navConfig ADDITIVE genişletildi**: `researchNavItems()` yalnız SUNUM katmanı — sınıflandırma fonksiyonları (resolveAreaForTab/isAdvancedTab) DEĞİŞMEDİ (advanced ekranlar hâlâ hiçbir birincil alana çözülmez; drift-guard testleri sağlam). `screenRegistry`+`navConfig` tek kaynak; paralel registry kurulmadı. Capability map: `PHASE-4-CAPABILITY-MAP.md`.
- **Takvim ay hücreleri** (`TakvimTab.tsx`): yalnız nokta DEĞİL — her planlı slot kompakt satır (readiness renk noktası + gerçek konu/başlık; X için saat). Renk = dossier/slot durumu (ready/needs_verify/not_ready); dossier'sız planlı slot dürüst NÖTR (içerik uydurulmaz); skipped üstü-çizili faint. `aria-label` `${gün} — ${sayı} öğe` DEĞİŞMEDEN (e2e kontratı: phase3b/3d/3e).
- **Sistem ekranı** (`SystemTab.tsx`): tek uzun stack kart → dashboard grid. Altyapı full-width detay kartı (2-kolon infra); kalan beş sözleşme (akış/bugün/Instagram planı/agent eval/maliyet) ilişkili-ama-bağımsız responsive grid kartları. content-wide'a genişletildi.
- **İçerik genişliği**: `--content-wide` 1280 → **1440** (Takvim/Kütüphane/araştırma/Sistem 1440/1920'yi daha iyi kullanır; okuma yüzeyleri standard kalır).

**Supersede notları:**
- **Collapse = gizle** (06 §7 / 05 A2): "3 item icon-rail'i hak etmez" GEÇERLİ (top-level yine 3), ANCAK "Araştırma" grubu artık sidebar'da hiyerarşik + keşfedilebilir olduğundan güçlü ekranlar rail dışında saklı DEĞİL. İkon-rail collapse hâlâ yok; grup collapse (Araştırma) eklendi.
- **Takvim ay = yalnız yoğunluk noktası** (05 C1 L290): SUPERSEDED — gerçek veri varsa hücre kompakt içerik gösterir (dürüst; dossier yoksa nötr). Kanıt clutter'ı değil, operasyonel sinyaldir.

**Kapsam (yalnız 4A):** Phase 4B (unified library + capture/search/board), 4C (Learn intake → transcript/Pack → atomik not/zihin haritası/görev/review), 4D (Obsidian export canlı doğrulama + ABSORBED legacy emekliliği) BAŞLAMADI. Mevcut Learn/Obsidian/Boards altyapısı (learning/*, components/learn/*, obsidian*, /api/learn/*) yeniden İCAT EDİLMEDİ — 4A yalnız Öğrenme'yi sidebar Kütüphane alt-nav'da görünür kıldı. **Phase 4 tamamlandı İLAN EDİLMEDİ.**

**Kanıt/kaynak:** typecheck 0 · lint 0 err / 5 pre-existing warn (hepsi Phase4A DIŞI) · verify:catalog OK (9 preset) · unit **2018/2018** (202 dosya, +3 navConfig) · build 0 · e2e **129 passed + 1 pre-existing faz1d first-nav flake (retry-green; "flakesiz" iddia edilmiyor), 0 failed** (+6 phase4a). NOT: ilk full e2e koşusu 7 failure verdi → kök neden `SidebarNowModule`'ün kısmi `/api/health` payload'ında crash'i (defensive optional chaining ile düzeltildi) + 2 shell-smoke assertion'ı ADR-040 IA'sına güncellendi; yeniden koşuda temiz. Gerçek browser read-only (Neon dev): 1024/1280/1440/1920 yatay taşma 0; Şimdi modülü gerçek veri (Yayına hazır 2 · Karar bekleyen 2 · Taslak plan 0/10 · Sistem sinyali); Takvim hücreleri gerçek slot konuları; Sistem dashboard grid; Araştırma toggle aria-expanded çalışır. Uygulama kaynaklı console error 0 (yalnız dış Kaspersky extension + Neon cold-start ilk-yük 500'ü, reload'da temiz). Kanıt: `shots/cemos-rebuild/phase4a-density/` (REAL-DB-READONLY etiketli). SIFIR migration; SIFIR dış/ücretli çağrı; production'a fixture YAZILMADI; PUSH/deploy YOK. Legacy semboller (`useXAgentStore`, `"xagent-store"`, `XAgentApp.tsx`, HTTP User-Agent, ABSORBED alias'lar) korundu. Sonraki: **Phase 4B**.

---

### ADR-041 — Faz 4B: Unified Library — kanonik save-to-board sözleşmesi + araştırma köprüsü
**2026-07-19 (kullanıcı Phase 4B onayı).** Kütüphane artık kopuk "arama listesi" + "İlham panosu" gibi davranmıyor; tek kanonik save/capture/board sözleşmesi + araştırma → kütüphane köprüsü. Model DUPLİKE EDİLMEDİ — mevcut `ContentItem`/`Board`/`BoardItem` omurgası + `normalizer`/`ingestContent` REUSE.

**DB — tek additive değişmez (migration `20260719120000_add_board_item_content_unique`):** `BoardItem`'a `@@unique([boardId, contentItemId])`. PostgreSQL unique index NULL'ları AYRIK sayar → `contentItemId=null` serbest (url/not/görsel) satırlar etkilenmez; yalnız gerçek `contentItemId`'li duplicate `(boardId, contentItemId)` çifti reddedilir. **"Tek kanonik içerik / pano" değişmezi artık DB düzeyinde** — tüm ekleme yollarının (addItem/captureInspiration/saveToBoard) TEK dedup kaynağı. Kod-öncesi read-only kanıt: 0 BoardItem satırı, 0 duplicate grup → index temiz uygulandı. `db:migrate` (safe-migrate-deploy) ile Neon'da: statik-scan additive → önce/sonra snapshot EŞİT (satır kaybı yok) → ikinci deploy "No pending" (idempotent) → `migrate status` up-to-date. Gerçek DATABASE_URL shadow olarak KULLANILMADI; destructive komut YOK. Post-apply doğrulama (warm): index PRESENT (`BoardItem_boardId_contentItemId_key`), Board/BoardItem hâlâ 0 (oturumdan SIFIR production yazımı).

**Kanonik save-to-board (`src/lib/boards/saveToBoard.ts`) — TEK reusable sözleşme:** `resolveBoardForSave` (board yok/arşivli/scope typed) — hesap-scoped panoya BAŞKA hesap VEYA accountId'siz istek fail-closed (`board_scope_mismatch`); paylaşılan (accountId=null) pano açıkça herkese açık; içerik account-agnostik (ContentItem hesaba ait değil, kapsam BOARD'a ait). `saveContentToBoard` idempotent + atomik: ContentItem varlık doğrulaması → tx içi `pg_advisory_xact_lock(board_save:<board>:<content>)` → findFirst reuse → yoksa create; **P2002 (unique index) → 500 DEĞİL, re-read ile dürüst `alreadySaved`** (cross-path yarış backstop'u). boardId yoksa paylaşılan default "Kaydedilenler" panosu (advisory-lock'lu get-or-create). `boardMembershipFor` toplu üyelik (N+1 YOK). captureInspiration KENDİ meta-merge akışını korur ama aynı scope/dedup semantiğini paylaşır.

**Araştırma → ContentItem köprüsü (`src/lib/boards/saveFromSource.ts`):** client YALNIZ typed kaynak REFERANSI gönderir (`{kind,id}`); sunucu YETKİLİ origin satırını yeniden yükler ve eşleşen typed `fromX` normalizer'ı çalıştırır — kırpılmış/güvenilmez client DTO alanları ContentItem kurmakta ASLA kullanılmaz (uydurma kanonik kayıt yok). `ingestContent` (platform, externalId) idempotent upsert → aynı kaynağın tekrar yakalanması duplicate ÜRETMEZ, metadata provenance-koruyarak uzlaşır. Kinds: contentItem (doğrudan) · news (`fromNewsItem`, id) · ytVideo (`fromYtVideo`, videoId@unique) · sourcePost (`fromSourcePost`, id) · igMedia · repo. Desteklenmeyen araştırma şekilleri (pipeline sayaçları, verdict özetleri) ContentItem'a ZORLANMAZ — dürüstçe `unsupported_source`.

**Unified route (`POST /api/library/save`) — Tümü + 5 araştırma ekranı için TEK sözleşme:** guard (isOperatorOrCronAuthorized) + Zod (gerçek `SaveSourceSchema` — geçersiz kind DB'den önce reddedilir) + typed kod → HTTP eşlemesi (source_not_found 404 / scope_mismatch 403 / archived 409 / content_not_found 404); created 201, alreadySaved 200. `POST /api/boards/[id]` içerik için saveContentToBoard'a delege (scope+dedup), serbest url/not için resolveBoardForSave guard'lı addItem. `/api/boards?savable=1` = paylaşılan + aktif hesap panoları (başka hesap HARİÇ — picker scope'la hizalı). Yeni mutasyon route'ları route-guards matrisinde (+3).

**Search & performans:** `/api/library/search` görünen sayfaya `savedBoards` üyeliğini TEK toplu sorguyla iliştirir (N+1 yok); `take=min(offset+limit, TAKE_CAP=500)` (sığ sayfa az çeker) + deterministik total sıralama (createdAt-desc + id tie-break → sayfalar arası duplicate/eksik yok, testli); derin sayfa cap'i aşınca dürüst `capped` bayrağı (sessiz kırpma yok). LibTumuTab stale-response guard (seqRef — filtre/sayfa hızlı değişince yavaş yanıt yeniyi EZEMEZ).

**UI (dark editorial + ADR-040 yoğunluk):** reusable `SaveToBoardButton` primitive (Tümü drawer + 5 araştırma satırı + İlham paylaşır) — **menü PORTAL ile document.body'ye (fixed konum, zIndex>drawer) render edilir** → Drawer'ın overflow/scrim stacking'i menü tıklamasını yemez (canlı e2e'de yakalanıp düzeltildi); pessimistic (sunucu sonucuyla uzlaşır — hata başarı gibi görünmez); zaten kayıtlıysa "Kayıtlı" + pano adı; hızlı kaydet + pano seç/oluştur. LibTumuTab: içerik satırında "Kayıtlı" rozeti (arama payload'ından), drawer save + non-content dürüst not ("prompt/pattern/anahtar/viral panoya kaydedilmez — kanonik içerik içindir"). İlham: bağlamsal sağ rail (`IlhamContextRail` — aktif pano/kayıt-pano-analiz sayaçları/son analizler/hızlı yakalama; yalnız gerçek workspace verisi; ≤1200px'de ana akışın altına düşer, `.ilham-workspace` grid). Araştırma köprüsü: news/youtube/viral-radar/source-intel/discovery-OutlierHighlights satırlarına ortak `SaveToBoardButton` (discovery pipeline sayaçları save-eligible DEĞİL — dürüst).

**Gate:** typecheck 0 · lint 0 err / 5 pre-existing warn (hepsi Phase4B DIŞI: coverage/scripts/ViralLibraryTab) · verify:catalog OK (9 preset) · unit **2054/2054** (+36: saveToBoard 18 / saveFromSource 6 / library-save route 7 / librarySearch +5) · build 0 · e2e **137 passed, 0 failed** (+7 phase4b; bu koşuda retry/flake tetiklenmedi — bilinen faz1d first-nav flake'i "yok" İDDİA EDİLMİYOR). Görsel kanıt `shots/cemos-rebuild/phase4b-unified-library/` (01/02/05/06 REAL-DB-READONLY — gerçek Neon, GET-only, save TIKLANMADI; 03/04/07/08 HARNESS route-mock — dosya adında açık). 1024/1280/1440/1920 taşma 0; app console error 0.

**Yapılmadı:** Phase 4C/4D, canlı AI üretimi ($0), gerçek dış çağrı (Meta business_discovery zaten çalışıyor — rakip senkronu), production'a save/capture/fixture yazımı (hepsi hermetic/read-only), push, deploy. Legacy semboller (`useXAgentStore`/"xagent-store"/`XAgentApp.tsx`/User-Agent) korundu.

---

### ADR-042 — Faz 4C: Learn intake → grounded Pack → atomik not/zihin haritası/görev/içerik fikri/review
**2026-07-19 (kullanıcı Phase 4C onayı).** Doğru tanım: **"Learn intake, resumable grounded Pack, atomik not/zihin haritası/görev/içerik fikri ve review production contract'ı tamamlandı."** Phase 4D (Obsidian canlı doğrulama + ABSORBED legacy fiziksel emekliliği) BAŞLAMADI. "Phase 4 tamamlandı", "Obsidian canlı doğrulandı", "canlı AI üretimi doğrulandı", "NotebookLM özeti orijinal transkript olarak doğrulandı", "production mutation ile test edildi" İDDİA EDİLMEZ. Mevcut altyapı (`learning/*`, `components/learn/*`, `/api/learn/*`, 9 Prisma Learn modeli) yeniden İCAT EDİLMEDİ — ~%90 hazırdı; iş: notes/graph/tasks'ı gerçek üretim aşamasına çevirmek + v2 artifact zarfı + üç kaynak türü + review idempotency + status read-model + UI.

**KRİTİK AÇIK KAPATILDI:** `pipeline/stages.ts` `PASSTHROUGH_STAGES` = {notes, graph, tasks} (no-op) idi; UI etiketleri var ama iş yoktu. **Artık BOŞ** — notes/graph/tasks GERÇEK aşama + yeni `content_ideas` aşaması eklendi; `concepts` `notes`'tan ÖNCE (veri akışı: notlar/grafik/görev/fikir kavramları tüketir).

**Pipeline v2 (`PIPELINE_VERSION`/`PROMPT_VERSION` v1→v2):** bump → `@@unique([sourceId, pipelineVersion])` YENİ pack satırı; v1 pack'ler OKUNUR kalır (getPackDetail legacy notesJson=`[]` → hasArtifact=false, dürüst "legacy paket" notu), otomatik/toplu reprocess YOK. Her yeni aşama: Zod çıktı + repair tavanı + model rolü (`STAGE_ROLES` genişledi: graph/tasks/content_ideas) + UsageLog/trace + gerçek maliyet + `assertBudget` gate + retry cap. **Çift-ücret koruması:** her artifact aşaması başında `artifact.stages.includes(stage)` → LLM başarılı ama advance düşmüşse retry re-charge ETMEZ (concepts/assessment'ın üstünde ek garanti). Orchestrator'ın mevcut lease (`claim` stale+startedAt CAS), resumable persist, STAY-partial, BudgetExceededError→429, TranscriptUnavailableError, MAX_STAGE_ATTEMPTS altyapısı KORUNDU.

**Basis-farkı (dürüstlük garantisi):** `SourceBasis` = transcript | summary, `basisForKind` (notebooklm_summary→summary). Yeni grounding türü `summary_supported`. Tüm prompt'lar basis-farkında: summary basis'te `groundingRuleFor` "NotebookLM özeti — videoyu izlemedin, `source_supported` KULLANMA" der. QA basis-farkında (`computeQaReport(basis)`): meşru grounded tür = `groundedTypeForBasis(basis)`; yanlış-temel güçlü iddia (summary pack'te source_supported = "videoda doğrulandı" yalanı) FLAG'lenir + coverage'a SAYILMAZ. **NotebookLM iddiaları asla "videoda doğrulandı" gibi görünemez.**

**v2 Pack artifact zarfı (`src/lib/learning/artifact.ts`, `LearnPack.notesJson` içinde — yeni tablo ailesi YOK):** Zod-doğrulu `{version:"v2", sourceBasis, atomicNotes[], graph{nodes,edges}, tasks[], contentIdeas[], stages[]}`. `parseArtifact` → v2 | legacy | **invalid** (parse/şema hatası SESSİZCE boş sayılmaz). `graphToMermaid` typed graph'tan DETERMİNİSTİK + escape'li + dangling-edge atlar (ham LLM Mermaid ÇALIŞTIRILMAZ). Node'lar concept/note'tan türer; edge'ler label→id çözülür (olmayan/self/duplicate atlanır).

**4C-A intake (`learnService.createSource(SourceIntake)`):** youtube (videoId) | manual_transcript | notebooklm_summary (ikisi içerik SHA-256 externalId → idempotent; ham metin ID'ye/log'a yazılmaz). provider/basis/verified SUNUCU koyar — İSTEMCİ belirleyemez (`/api/learn/sources` Zod discriminated-union yalnız içerik alanları). NotebookLM `metaJson` provenance basis=summary+verified=false. Zaman-kodsuz metin (manuel/NotebookLM) `plainTextSegments` ile pseudo-segment'e bölünür (startSec=0 → UI zaman damgası GÖSTERMEZ; eski 0-chunk açığı kapandı); metadata fetch yalnız youtube.

**4C-E status read-model (`src/lib/learning/status.ts`):** TEK kanonik `deriveLearnState` → inbox|processing|transcript_required|budget_blocked|needs_review|ready|failed; ikinci readiness sözlüğü YOK. Çelişki engellendi (job done ama pack yok; source ready ama qa_failed; budget genel hata; transcript-yok manuel yol kaybı). Dashboard + jobView bundan türer.

**4C-H review idempotency (tek additive migration `20260719140000_add_learn_review_attempt_idempotency`):** `LearnReviewAttempt.idempotencyKey String?` + `@@unique` (PostgreSQL NULL-ayrık → key'siz denemeler serbest). `reviewService.grade` ATOMİK (`$transaction`: schedule ilerletme + attempt insert tek tx) + İDEMPOTENT (aynı key ikinci kez → deduped, ladder İKİ KEZ atlamaz; eşzamanlı yarış P2002 → 500 değil, mevcut durum). Mastery attempt'lerden türer (idempotent). Client: kart başına idempotencyKey + senkron çift-tık guard + in-flight disable + POST hatası İLERLETMEZ. DB: additive-only, önce=sonra Learn satır sayıları (sources 5/packs 3/schedules 40/attempts 0 EŞİT → SIFIR kayıp, SIFIR yazım), index PRESENT, shadow-url YOK, destructive YOK.

**UI (dark editorial + ADR-040):** `LearnPackView` 7 sekme (Genel/Atomik notlar/Kavramlar/Zihin haritası/Uygula/Kartlar-Quiz/Kaynak); provenance chip (zaman damgası YALNIZ timed provider'da, aksi "kaynak/özet #idx"); NotebookLM summary-basis kalıcı uyarı; not-ready pack Obsidian/hazır-bilgi eylemlerini GİZLER; zihin haritası = erişilebilir ilişkisel görünüm (birincil) + Mermaid kodu (opsiyonel `<details>`); içerik fikirleri dürüstçe "öneri, yayınlanmış değil" + kopyala (otomatik Plan/Idea terfi YOK). `LearnProcessingView` mount'ta GET (reload-resume) + tek advance loop + backoff + budget/transcript/failed ayrı; gerçek notes/graph/tasks/content_ideas aşamaları görünür; ayrılma "iptal edildi" İDDİA ETMEZ. `LibOgrenmeTab` ÜÇ AÇIK kaynak modu (her biri açıklama+limit; NotebookLM "doğrulanmış transkript değildir"); userState grouping/rozet; pack'i olan kaynak (needs_review dahil) paketi açar. `Card` artık `data-testid` iletir (additive).

**Gate:** typecheck 0 · lint 0 err / 5 pre-existing warn (hepsi 4C DIŞI: coverage/prune-prompts/ViralLibraryTab) · verify:catalog OK (9 preset) · unit **2093/2093** (+39: artifact 15/status 9/intake 7/reviewService 4/qa +3/pipeline +1) · build 0 · e2e **145 passed + 1 pre-existing faz1d first-nav flake (retry-green; "flakesiz" İDDİA EDİLMEZ), 0 failed** (+9 phase4c). Görsel kanıt `shots/cemos-rebuild/phase4c-learning/` (01 REAL-DB-READONLY = gerçek Neon GET-only: 3 hazır paket/40 bugün tekrar/3 gerçek ready pack [2 v1 raw-URL + 1 başlıklı], canonical "Hazır" rozeti, 3 kaynak modu — save/mutasyon TIKLANMADI; 02-13 HARNESS route-mock — dosya adında açık). 1024/1280/1440/1920 taşma 0; app console error 0 (yalnız Neon cold-start).

**Yapılmadı:** Phase 4D, canlı ücretli AI (OpenRouter/Gemini/Supadata — $0; env varlığı canlı onay sayılmadı; pipeline ücretli kısmı deterministik fixture'la doğrulandı), production Learn tablolarına test kaydı (hermetik/read-only), gerçek Obsidian GitHub/local export, canonical içerik-fikri→Plan/Idea terfisi (paket içinde dürüst öneri; sonraki bağlantı `Idea`/`IdeaSource`/OpportunityHandoff üzerinden — sahte entegrasyon yok), push, deploy. Legacy semboller korundu.

### ADR-043 — Faz 4D: Obsidian export production contract + tipli durum makinesi + ABSORBED legacy fiziksel emekliliği
**2026-07-19 (kullanıcı Phase 4D onayı).** Doğru tanım: **"Phase 4D production contract tamamlandı; Obsidian canlı kanal doğrulaması BLOCKED-EXTERNAL."** Şunlar İDDİA EDİLMEZ: "Phase 4 tamamen tamamlandı", "Obsidian canlı doğrulandı", "canlı GitHub commit'i yapıldı", "gerçek vault'a yazıldı", "production mutation ile test edildi". Mevcut altyapı (`learning/{obsidian,obsidianWriter,githubVault}`, `/api/learn/packs/[id]/obsidian`, `integration_suggestions` aşaması) yeniden İCAT EDİLMEDİ — v1 export vardı; iş: deterministik + basis-farkında + v2-tam bundle, tipli durum makinesi, sertleştirilmiş kanallar, kalıcı audit, gate'li auto-export, dürüst UI + legacy emeklilik.

**KRİTİK AÇIKLAR KAPATILDI (v1 export kusurları):** (1) `buildObsidianBundle(pack, nowIso)` her export'ta `created: now()` gömüyordu → NON-DETERMİNİSTİK (her export = yeni içerik = gereksiz commit). (2) Yalnız video notu + kavram notları — v2 artifact (atomik not/graph/görev/fikir/QA) YOKTU; basis-farkı yoktu (NotebookLM özeti "YouTube video" gibi görünürdü). (3) Yerel yazıcı doğrudan `fs.writeFile` overwrite — atomic değil, unmanaged/başka-pack dosyası SESSİZCE ezilirdi. (4) GitHub `putFile` içerik aynı olsa bile PUT ederdi (gereksiz commit); errorClass ayrımı yoktu. (5) GET route `status==="ready"` KONTROL ETMEDEN export ediyordu → not-ready pack direct route ile aktarılabilirdi. (6) Kalıcı audit/idempotency modeli yoktu.

**Deterministik + basis-farkında v2 bundle (`obsidian.ts` yeniden yazıldı + `packExport.ts` + `obsidianManifest.ts`):** `assemblePackExport` TAM v2 DTO'yu repolardan derler (yalnız repo+artifact import → `learnService`/orchestrator döngüsü YOK). `buildObsidianBundle(PackExport)` SAF + deterministik (now() GÖMÜLMEZ; stable `pack.createdAt`): MOC ana notu, kaynak notu, atomik notlar, paylaşımlı çok-pack kavram notları, Mermaid MOC (typed graph'tan — ham LLM Mermaid YOK), uygulama görevleri, içerik fikirleri (öneri), flashcard/quiz, QA + provenance özeti. Her dosya managed frontmatter taşır (pack id / pipeline+prompt versiyon / source basis / QA verdict+coverage / source kind). **NotebookLM = summary framing; sahte timestamp ÜRETİLMEZ; collision-safe pack klasörü (`başlık__packIdShort`).** `obsidianManifest`: managed-marker + YAML/wikilink/markdown injection kaçışları (codepoint kontrol-strip) + deterministik manifest hash + paylaşımlı-kavram merge (başka pack'lerin blokları KORUNUR → önceki kaynak kaybı yok, already_current tespiti).

**Tipli durum makinesi (`obsidianExport.ts`) — "null = her şey" KALKTI:** kanal (zip/local_vault/github_vault) başına `ChannelResult`: state (not_configured/not_ready/prepared/succeeded/already_current/partial/conflict/failed) + written/unchanged/failed/conflict + errorClass + güvenli hedef etiketi + manifest hash. **Secret/token/TAM yerel path ASLA tutulmaz** (targetFingerprint = hedefin secret-olmayan hash'i).

**Sertleştirilmiş kanallar:** `localVault.ts` — realpath vault kökü, symlink/junction kaçış koruması (realpath dizin kök-içi mi), atomic temp-yaz+rename (yarım markdown yok), managed-marker conflict (unmanaged/başka-pack ÜZERİNE YAZMAZ), already_current skip. `githubVault.ts` — read-only preflight, unchanged-content skip (aynı içerik → PUT YOK → gereksiz commit yok), errorClass haritası (auth/forbidden/rate_limited/not_found/conflict/unprocessable/network/timeout), partial ≠ whole-success, token loglanmaz, deterministik pack-kimlikli commit mesajı. Contents API dosya-başına-commit gerçeği belgelendi.

**Kalıcı audit + idempotency (`LearnExportAttempt` modeli + additive migration `20260719160000_add_learn_export_attempt`):** kanal başına (pack+targetFingerprint+manifest) TEK satır (`@@unique` composite upsert → aynı manifest retry duplicate write/attempt ÜRETMEZ = already_current idempotency) + `idempotencyKey String? @@unique` (NULL-ayrık client çift-gönderim koruması). **Neden `notesJson` içine sıkıştırılmadı:** file-by-file audit + kanal başına farklı hedef + query'lenebilir geçmiş (son export durumu UI'ye) + idempotency invariant DB-düzeyinde olmalı; notesJson pack artifact'ı taşır, export durumu değil. `exportService.ts` TEK audited yol: assemble→ready-gate→bundle→kanal→attempt kaydı; idempotencyKey verilmişse önce kontrol (deduped). ESKİ audit-suz `exportPackToVault`/`exportPackToGithub` adaptörleri + `obsidianWriter.ts` SİLİNDİ (tek yol garantisi).

**Gate'li auto-export:** `integration_suggestions` aşaması artık YALNIZ `OBSIDIAN_AUTO_EXPORT=true` iken export eder (aksi: örtük dış yazma YOK — bundle yalnız API/UI talebiyle). Açıkken bile: yalnız ready pack + configured kanal, her deneme attempt kaydeder, fail-soft (aşama tamamlanması ASLA "export başarılı" DEMEZ — gerçek durum attempt contract'ından).

**API + UI:** `GET /api/learn/packs/[id]/obsidian` artık not-ready pack'i sunucuda 409 reddeder + deterministik bundle. `POST /api/learn/packs/[id]/export` (operator-auth + Zod + idempotencyKey → tipli audited sonuç); `GET` = channelStatuses + son export. `LearnExportPanel`: üç kanal AYRI + configured/unconfigured (yalnız env NAME) + son sonuç durumu + retry; ZIP indirme "yerel/GitHub sync DEĞİL"; "başarılı" yalnız gerçek yazma/already_current. `/api/integrations` Obsidian'ı yerel + GitHub olarak ikiye ayırdı (Vercel fs kalıcı değil / Obsidian Git çeker notları).

**Legacy emeklilik (parity-audit sürücülü):** 10 runtime-DARK component fiziksel silindi (grep ile 0 static/dynamic/string-id importer): DailyQueue/Keyword/Prompt/Pattern/Learn/FlowRadar/SourceIntelligence/ViralLibrary tab'ları + orphan agent/FlowList + agent/TweetCard. **Veri güvenliği önce:** ViralLibraryTab'ın localStorage→DB savedTweets drenajı saf/testli `savedTweetsMigration.ts`'e taşındı, AppShell mount'ta persist-hydration-farkında koşar (idempotent POST id-upsert; başarısızsa retry). **Bilinçli KORUNDU:** InstagramTab + CompetitorRadarSection + ReelsDossierSection (IG rakip-radar/reels UI'sinin canonical evi YOK — silmek 2 ek feature dosyasını cascade-orphan ederdi; re-home bekliyor). **Değişmez:** TAB_ALIASES, store persist migrate v9, deep-link normalizasyonu, `useXAgentStore`, `"xagent-store"`, `XAgentApp.tsx`, HTTP User-Agent, tüm `/api/*` + Prisma modelleri (UI silmek veri modelini silmez). Eski id'ler (daily-queue/instagram/viral-library/keyword-library/prompt-library/pattern-library/learn-dashboard) 3-katman (TAB_ALIASES → normalizeTabId → AppShell unknown-guard) ile canonical ekrana çözülür.

**Canlı sınır (BLOCKED-EXTERNAL):** Bu prompt credential oluşturma/değiştirme veya belirsiz repoya dış write yetkisi VERMEZ. Env yok / target belirsiz / token yok / dış write onayı gerektiğinden gerçek yerel vault yazımı + gerçek GitHub commit YAPILMADI (`OBSIDIAN_VAULT_PATH`/`OBSIDIAN_GITHUB_REPO`/token unconfigured). Kod + test + hermetik doğrulama tamamlandı; canlı kanal doğrulaması operatör env/target/onay bekliyor. Code repo push yasağı ≠ Obsidian vault export (ikisi de yapılmadı, karıştırılmadı).

**Gate:** typecheck 0 · lint 0 err / 4 pre-existing warn (hepsi 4D DIŞI: coverage lcov ×3 + prune-prompts; 4C'nin ViralLibraryTab uyarısı silme ile GİTTİ) · verify:catalog OK (9 preset) · unit **2139/2139** (+46: obsidian 13/obsidianManifest 14/localVault 7/githubVault 7/exportService 5) · build 0 · e2e **+8 phase4d** (export panel/kanal durumları/succeeded/already_current/partial/ZIP/eski deep-link çözümü/savedTweets göçü). DB: additive migration Neon'da uygulandı — önce=sonra Learn satır sayıları EŞİT (SIFIR kayıp/yazım), `LearnExportAttempt` + 4 index PRESENT, ikinci deploy no-op, shadow-url YOK, destructive YOK.

**Yapılmadı:** gerçek yerel vault yazımı, gerçek GitHub commit, canlı export idempotency doğrulaması (hepsi BLOCKED-EXTERNAL), IG rakip-radar/reels + pattern-curation + daily-queue detay-drawer re-home (backend korundu, UI canonical'a taşınmadı), push, deploy. Legacy semboller korundu.
