# IMPLEMENTATION-STATE

## TRUTH DELTA 2 — 2026-07-23 (yeni oturum re-verify + PR-A kapanışı, plan §14 adım 2-3)

**KÖK NEDEN SINIFI KESİNLEŞTİ — Neon DATA-TRANSFER (EGRESS) KOTASI, compute CU-saat DEĞİL:** salt-okunur probe (prisma `SELECT 1`, gerçek exit 1): `ERROR: Your project has exceeded the data transfer quota. Upgrade your plan to increase limits.` Plan §8b "compute kotası tükendi" hipotezi bu kanıtla daraltıldı: kota sınıfı = data transfer. WP-02 egress kapanışının doğrudan doğrulaması. OP-1 hâlâ gerekli (aşım miktarı, reset tarihi, parola rotasyonu). Plan yükseltme/ödeme kararı yalnız OPERATÖR (§8d ağacı — ölçümsüz ödeme YOK). Kota reset'ine dek DB kapalı kalır; 7 günlük ölçüm erişim dönünce başlar.

**PR-A KAPANDI + DEPLOY KAPISI 1 GEÇİLDİ (2026-07-23 ~07:45 UTC):**
- PR #5 açıklaması 8-commit gerçeğine güncellendi (eski "tam 5 commit / yeni kod yok" ifadesi düzeltildi) → **merge commit `918d4fc`** (repo konvansiyonu: merge commit, PR #2/#3/#4 gibi). HEAD `c503787` değişmediğinden yeşil CI yeniden tetiklenmedi (verify ×2 + db-integration + Vercel Preview READY kanıtı mevcuttu).
- **Prod SHA kanıtı:** `cemos-woad.vercel.app` → `dpl_9geZ4HCZ` READY target=production, `githubCommitSha=918d4fc` = merge SHA. Rollback hedefi kayıtlı: `dpl_8WmeP7Ms` (main@`45875ad`).
- **Salt-okunur HTTP smoke (LIVE):** `/`→307 `/giris` · `/giris` 200 · `/api/health` `/api/costs` `/api/learn/sources` → 401 `{"ok":false,"code":"unauthenticated"}` · unauthenticated gövdelerde neon.tech/prisma/postgres/5432 sızıntısı **0** · yeni deployment'ta runtime error/warning logu **0**.
- **Authenticated UI smoke (operatör Chrome oturumu, salt-okunur gezinme):** Bugün dürüst degraded ("Üretim altyapısı hazır değil — Veritabanına erişilemiyor", "Taslaklar yüklenemedi" + Yeniden dene) · **Maliyet sahte-$0 YOK (CANLI doğrulandı)** · **Öğrenme sonsuz-spinner YOK (CANLI doğrulandı)** · Kütüphane dürüst hata durumu · tüm gezinme boyunca console error **0**. Flow Radar 502 sözleşmesi mutation gerektirdiğinden smoke'ta koşulmadı (VERIFIED-HERMETIC).

**YENİ CANLI BULGU (PR-B WP-01 hedefi, ekran kanıtlı):** Sistem→Altyapı→Meta (Instagram) kartı HAM Prisma hatası + çıplak Neon hostname gösteriyor (`Invalid prisma.integrationCredential.findUnique()… Can't reach database server at ep-long-sun-…neon.tech:5432`) — healthService `metaToken.message` yolu redaksiyonsuz ve `redactSecrets`'ta bare-host kalıbı yok (SEC-M1'in canlı kanıtı; authenticated yüzeyde, ama sözleşme "ham Prisma/provider mesajı istemciye gitmez"). Minör: Öğrenme üst KPI şeridi degraded'de "0 hazır paket" gösteriyor (fake-zero kalıntısı) — WP-01 tek-global-bant işinde ele alınacak.

**Branch durumu:** `fix/db-resilience-egress` → `origin/main@918d4fc` üzerine rebase edildi (tree temiz, yalnız `?? shots/`); PR-B delta'sı bu commit'le başlar.

---

## PR-B — WP-01 + WP-02 KAPANIŞI (2026-07-23, aynı oturum; 14 atomik commit)

**WP-01 (DB-down dürüst degradasyon):** `dbUnavailableError` saf sınıflandırıcı (Prisma init/name + P1001/P1002/P1008/P1017/P2024 + canlı kota mesajı + socket + cause-zinciri) · `dbErrorResponse` (budgetErrorResponse aynası → sabit secret'sız `503 {code:"db_unavailable", retryable:true}` + breaker + rate-limitli redakte log) · instance-yerel circuit breaker (3→30sn, üstel ↦5dk, half-open) · `fail()` choke-point coercion (kaçan site yine 503; beklenmeyen 500=0 ağı) · **95 route / 118 catch-site sweep** (4 paralel Sonnet ajan, ana-thread grep+tam-suite doğrulaması) · `redactSecrets` SEC-M1 bare-host kalıpları · healthService: breaker-aware probe + canlı-kanıtlı Meta-kartı sızıntısı sabit mesajla kapandı + `degraded/dbCircuit/generatedAt` · `/api/health` DB-down'da **200+degraded** · UI: `deriveDbAvailability` tek sinyal + `DbUnavailableBanner` tek global bant + last-known-good (degraded damgayı ilerletmez) · **feedback resume-semantiği**: ViralPattern id'si `reason` JSON bağı (migration YOK), replay tamamlanmışsa ikinci ücret YOK, yutulmuş extraction tx-scoped advisory lock altında sürer (eşzamanlı kaybeden atlar), yine düşerse bağ yazılmaz → retry açık; 6 senaryo testli.

**WP-02 (event-driven health + egress):** 5dk periyodik polling SİLİNDİ → mount/focus/visible/manuel/mutation-revalidate (60sn guard; DB-down'da `retryAfterSeconds` bastırması; görünürde ≤30dk güvenlik ağı; gizlide 0) · ham health context'e → Settings/Integrations/Discovery kendi `/api/health` fetch'i YOK (navigation-burst kapandı) · plan-health fan-out yalnız `?deep=true` · `/api/costs` ay görünümü **1 groupBy + 1 dar 4-kolon select** (ay-tam-satır findMany yok; sözleşme birebir, Σ line-items=ay toplamı testli) · findMany yeniden-doğrulaması: 7 bayraktan 5'i zaten kapalı/domain-bounded (kanıtla SKIP), kanıtlı 2 büyüyen listeye `take:500` · vitest **yapısal dummy DATABASE_URL (connect_timeout=1) + uzak-host fail-closed THROW**; guard'ın ifşa ettiği **21 gizli gerçek-prisma dokunuşu** gerçek mock'larla kapandı (sıfır gevşetme; suite 13.4sn).

**GATE (gerçek exit):** typecheck 0 · lint 0 · verify:catalog OK · verify:acceptance OK · verify:ai-economics OK · unit **246 dosya / 2423 test** · build 0 (`ƒ Proxy`+`ƒ /giris`) · migration **0 yeni** (`git diff origin/main -- prisma/` boş) · **E2E TAM `--retries=0`: 8 shard = 21+20+25+15+23+17+24+16 = 161/161, her shard foreground EXIT 0, SIFIR flaky** (arkaplan tam-koşu 10dk harness tavanında kesildi → kanıtlı shard desenine dönüldü; kesilen koşunun yetim dev-server'ı 3211'de temizlendi).

**Sıradaki:** push → PR-B → CI+preview → temiz-bağlam code/security delta review → yeşilse merge + deploy kapısı 2 + prod smoke (yetki continuation promptunda). PR-C backlog: CODE-L1 budget-status TTL memo · SEC-L1 embeddings tam reservation (WP-06) · WP-04 hesap ayrımı (bu PR'a sığmadı — bilinçli).

---

## TRUTH DELTA — Opus uygulama başlangıç doğrulaması (2026-07-22, FINAL-OPERATIONAL-CLOSURE-PLAN v2 §14 gereği)

Salt-okunur re-verify sonucu; planın referans gerçeklerinden SAPMALAR:

**Doğrulandı (delta yok):** branch `fix/prod-audit-egress-security@98a94fa` (5 commit, tree yalnız `?? shots/` + untracked plan dosyası) · prod `main@45875ad` = `dpl_8WmeP7Ms` READY · açık PR yoktu (PR #5 bu oturumda açıldı) · default branch `main` · Neon hâlâ erişilemez (18:03'te dahi `PrismaClientInitializationError`).

**DELTA 1 — Production env envanteri TAM (VAR/YOK, `vercel env ls` — değerler okunmadı):** Planın "eksik olabilir" varsaydığı env'lerin HEPSİ Production'da VAR: `OPENROUTER_KEY_ROTATED_AT` (1 gün önce eklenmiş — rotasyon kapısı artık kapalı değil olabilir), `OPENROUTER_API_KEY` (1g), `COMPOSIO_CONSUMER_API_KEY`+`COMPOSIO_INSTAGRAM_CONNECTED_ACCOUNT_ID`+`COMPOSIO_INSTAGRAM_ACCOUNT_HANDLE`+`COMPOSIO_INSTAGRAM_TOOLKIT_VERSION`+`INSTAGRAM_DATA_PROVIDER` (9 saat önce), `OBSIDIAN_GITHUB_TOKEN`+`OBSIDIAN_GITHUB_REPO`+`OBSIDIAN_GITHUB_DIR` (20 gün), OIDC dörtlüsü, `DATABASE_URL`, `CRON_SECRET`, `SOCIALDATA_API_KEY`, `FAL_KEY`, `SUPADATA_API_KEY`, `GEMINI_API_KEY`, `META_*`, tüm bütçe/limit değişkenleri. **Sonuç: OP-2 yalnız KREDİ sorusuna düşer; OP-5 yalnız hedef ONAYINA düşer (GitHub env'leri hazır); OP-3 bağlama env'leri hazır (ACTIVE onayı canlı sync'te test edilir).** Not: VAR ≠ değer geçerli — canlı doğrulama ilk gerçek çağrıda.

**DELTA 2 — Vercel cron'ları CANLI ateşleniyor:** 2026-07-22 18:03'te `/api/cron/learn` koştu (`CRON_SECRET` çalışıyor), DB'de öngörülen biçimde başarısız oldu; bütçe gate'i dürüst davrandı ("Budget durumu okunamadı, mining atlanıyor"). Cron zamanlaması için ek operatör aksiyonu GEREKMİYOR; DB dönünce cron'lar kendiliğinden üretmeye başlar.

**Sapma bildirimi:** PR-A'ya (plan: "yalnız 5 commit") bir adet salt-docs commit'i eklendi (bu truth delta + plan dosyasının kendisi). Gerekçe: hotfix'i yöneten otoritatif plan repo'da PR ile birlikte kayda girer; runtime riski sıfır. İtiraz halinde commit düşürülür.

---

## AUTH RETIREMENT → "Sign in with Vercel" (OIDC) — ADR-049 (2026-07-21)

**Branch `feature/cemos-rebuild`** (önceki HEAD `e8d729b` üstüne; PUSH/PR/deploy YOK; tree yalnız `?? shots/`). Kullanıcı kararı: uygulama-içi parola emekli; **Hobby'de kal ($0), uygulama-içi "Sign in with Vercel" (OIDC) kapısı.** Vercel plan kapısı resmî docs ile doğrulandı: Deployment Protection "All Deployments" (production domain koruması) = **Pro/Enterprise**; Hobby production'ı public bırakır → authentication authority uygulama-içi OIDC kapısıdır.

**Yapılan:**
- **Emekli:** parola scrypt (`hashPassword`/`verifyPassword`), `throttle.ts` (+test), `scripts/hash-access-password.ts`, `POST /api/auth/login`, `ACCESS_PASSWORD_HASH`, `?setup=1`, `session.test` parola vakaları.
- **Yeni:** `src/lib/auth/vercelOidc.ts` (PKCE/state/nonce/allow-list/next-guard — saf, unit-test) + `GET /api/auth/authorize` + `GET /api/auth/callback` (+ callback route testi). Kimlik `/userinfo` + allow-list (**FAIL-CLOSED**) → başarılıysa `cemos_session` (session.ts HMAC reuse). Vercel access/refresh token'ları saklanmaz.
- **Değişen:** `proxy.ts` (yalnız `SESSION_SECRET`; setup kaldırıldı), `requiredSecrets` (`ACCESS_PASSWORD_HASH`→OAuth client id/secret + allow-list; `SESSION_SECRET` kalır), `/giris` (parola formu → "Vercel ile giriş yap" server-component link), `.env.example`, `CEMOS.md §7`, ADR-049, acceptance matrix, screen-spec A7. E2E harness: password-login → imzalı `cemos_session` cookie minting; `access-gate.spec` yeni sözleşme.
- **Korundu:** `SESSION_SECRET`, "Çıkış" logout, `/giris` route, `/api/auth/{authorize,callback,logout}`, tüm 2. katman guard'lar (`isOperatorOrCronAuthorized`, `CRON_SECRET`, budget/provider/SSRF/redaction). **`AuthAttempt` tablosu inert (DROP YOK — destructive).**

**GATE (bu HEAD, gerçek exit):** typecheck **0** · lint **0** · verify:catalog **OK** · verify:acceptance **OK** (18 ekran / **82 mutation guard'lı** / 4 cron) · verify:ai-economics **OK** · unit **2285** (233 dosya) · build **0** (`ƒ Proxy` + `/api/auth/{authorize,callback}` + `/giris`). E2E: bu koşuda doğrulanır. **SIFIR migration.**

**[USER-ACTION] (deploy önkoşulu):** Vercel "Sign in with Vercel" App (callback `https://cemos-woad.vercel.app/api/auth/callback`, scope `openid email profile`) → prod+preview env `NEXT_PUBLIC_VERCEL_APP_CLIENT_ID`+`VERCEL_APP_CLIENT_SECRET`+`AUTH_ALLOWED_VERCEL_USERS` (ör. `alicembozma@gmail.com`); `SESSION_SECRET` KALIR. Sonra deploy + smoke + OpenRouter bounded golden (≤$0.50) + Composio read-only.

---

## LAST NON-EXTERNAL CLOSURE (2026-07-21) — düzeltilebilir non-external P0/P1/P2 = 0; launch işlemleri bekleniyor

**Branch `feature/cemos-rebuild` @ `1c47b3b`** (önceki Final Proof `eb2117d` üstüne **4 commit**; `0127213` üstüne kümülatif 23). Push/PR/deploy YOK. Tree temiz (`?? shots/` — dokunulmadı, yalnız bu oturumun walkthrough kanıtı eklendi, commitlenMEDİ). Bu oturum: 4 bağımsız red-team ajanı (provider-accounting / redaction / dead-route / PostgreSQL-executability) + 1 builder (redaction wraps) → ana-thread kanıtlı doğrulama + düzeltme. Canlı ücretli AI **$0**; dış round-trip YOK (yalnız verify:catalog free /models read); YENİ migration YOK.

**Kapatılan 6 zorunlu closure:**
- **[1] Provider accounting degraded-tail (`ca0c579`)** — önceki "muhasebe bypass-siz" iddiası ücretli-tail'lerde ÇÜRÜTÜLDÜ. Invariant: bir ücretli çağrı BAŞLADIKTAN sonra maliyet sonucu HER ZAMAN kaydedilir (reconciled/estimated/unknown) — asla sessiz $0. `UsageLog.meta.costOutcome` (migration YOK; bütçe okuyucuları `estimatedCostUsd`'i topladığından degraded satırı hem `/api/costs`'ta görünür hem ilgili tavana sayılır). **F2/P2** generateGated: LEDGER-FIRST-then-settle (ledger yazımı başarısızsa reservation OPEN kalır = fail-safe over-count, TTL'de kendini iyileştirir; başarı+billed-failure yolları). **F1/P1** transkript: Gemini/Supadata gönderimde faturalanır ama yalnız ≥200-char usable'da kaydediliyordu → çağrılan HER ücretli sağlayıcı ledger'lanır (usable→estimated, reached-ama-unusable→unknown; `learnConfig.transcriptCostRows` saf karar). **F3/P2** fal: reached-ama-URL-yok → unknown. **F4/P3** embeddings: her 200 ledger'lanır. Testler: transcriptCostRows unit + generateGated fail-safe + imageService.degraded + vector-memory.degraded.
- **[2] Kalan log redaction (`1c47b3b`)** — `redactSecrets` 3 açık için sertleştirildi (bare Meta EAA token / non-DB URL userinfo / yerel fs-path OS-username). HIGH DB-persist (CronRun.error: news-pool/run·process, youtube/sync; bridgeSync warnings→resultJson) + KÖK NEDEN 5 kopya `errMsg` helper (igClient/instagramService/youtubeService/ytClient/mcpClient → `redactError(e)`, her console+dönen-.error call-site maskeyi devralır) + MEDIUM stderr (generate-morning/draft-pipeline/draftService/ytOwnPerformance/engagementLearning/aiSpendReservation/feed-the-goat) + LOW (cron/news, pipeline errorMessages). Korunan (dürüst güvenli): client-side console, name-only requiredSecrets, key'i HEADER'da taşıyan public fetch (supadata/HN/Reddit). redactSecrets +3 pattern testi.
- **[3] BudgetSystemUnavailableError → 503 (`ce7550d`)** — iki ayrı bütçe hatası tek kaynaklı `budgetErrorResponse` ile 16 gated route'ta AYNI mapping: over-budget→402 `{code:"budget"}` (kalıcı, retry değil), authority-doğrulanamıyor→503 `{code:"budget_unavailable", retryable:true}` (harcama YOK, geçici; sabit secret-siz Türkçe mesaj, detail sızmaz). 429→402 iki outlier (youtube/briefs, learn/advance) kanonikleştirildi (429 gerçek rate-limit'e kaldı). Client `code`'a bakar → sonsuz retry loop yok, mevcut 503 `generation_blocked` ile çakışmaz. Helper unit + route-level 402&503 testi.
- **[4] Group-C dead routes (`ca6e697`)** — bağımsız denetim + ana-thread doğrulamayla TÜM tüketici sınıflarında (UI/server/cron/MCP/test) 0-tüketici kanıtlanan 5 route silindi: competitors, prompt-library, source-posts (top-level; canlı flow-radar send-to-queue DEĞİL), pattern-library/[id]/{increment-usage,adjust-score}. Coupled: route-guards.test (3 satır) + paylaşılan pattern-library/[id]/route.test (2 describe + mock trim; canlı PATCH testi yaşıyor). Ölü helper/repo YOK (her biri ≥1 canlı tüketicili). Mutation route 84→82. **KORUNDU (kanıtla):** boards/[id] (roadmap-belirsiz İlham board-detail; guard'lı) + news-pool/run (CRON_SECRET dış-tetik + staged pipeline süperseded DEĞİL — rg-empty ≠ ölü).
- **[5] PostgreSQL integration — CI-READY / NOT-RUN-LOCALLY (dürüst, PASS SAYILMADI)** — probe: yerel psql/pg_ctl/postgres YOK, Docker daemon/CLI YOK, embedded-pg devDep YOK, diskte portable PG YOK → gerçek-PG'nin her yolu install/download/uzun-süreçli-server ister (üçü de yasak → operatör alanı). Ephemeral guard unit-test'li (prod/boş URL THROW). CI workflow geçerli+wired. **Yeni kanıt:** `vitest list --config vitest.integration.config.ts` (opt-in flag'le) 5 dosyadaki **14 gerçek-PG itest vakasının hepsini** ADLARIYLA listeler (collect edildi, execute EDİLMEDİ — guard beforeAll'da; flag'siz HİÇBİRİ listelenmez → collection-guard kanıtlı). Gerçek yeşil/kırmızı koşu meşru biçimde CI-only.
- **[6] Manual desktop walkthrough — TAMAMLANDI (REAL, read-only)** — `.env.local`'da ACCESS_PASSWORD_HASH/SESSION_SECRET YOK → `proxy.ts` `isLocalDevRuntime()` dev-setup bypass'ı → `next dev` şifresiz gezilebilir. 1024/1440/1920'de ana IA + ekranlar (Giriş, Bugün, Plan, Kütüphane, Toolbox, Profil menü + Maliyet, Viral Radar) gerçek Neon verisiyle render; **console 0 error / 0 warning (48 mesaj)**; **1024 & 1440'ta yatay taşma 0** (`scrollWidth ≤ viewport`, 0 taşan eleman); "Şimdi" modülü dürüst degraded ("Durum alınamadı." → gerçek sayılara toparlandı). Prod mutation TETİKLENMEDİ (DB-güvenlik). 10 REAL-etiketli PNG → `shots/cemos-rebuild/final-nonexternal-closure/` (commitlenMEZ). Mutation-ağır yolculuklar E2E 159 + unit ile kapsanır.

**TAM GATE YEŞİL (gerçek exit kodları):** typecheck **0** · lint **0** · verify:catalog **OK** (9 preset canlı) · verify:acceptance **OK** (18 ekran / **82 mutation route hepsi guard'lı** [84→82] / 4 cron) · verify:ai-economics **OK** · migration scan **14 additive / 0 destructive** (yeni yok) · unit **2273** (232 dosya) · build **0** (temiz `.next`, `ƒ Proxy (Middleware)` + `ƒ /giris`) · **E2E 159/159 — 8 deterministik shard (`--shard=k/8`), her biri foreground gerçek EXIT 0** (21+20+25+15+23+21+18+16=159; her test tam bir kez; SKIP yok; pipe/tail/background maskeleme yok; **1 flaky** shard-2 CemOS'un-bildikleri profil, retry'da geçti — ayrı raporlandı, bilinen kalıcı flake).

**Residual (dürüst, non-external, çekirdeği bloke ETMEZ — hepsi P3):**
- Accounting **F5** (OpenRouter 200-with-unparseable-BODY, gövde nadir — usage.cost aynı gövdede) + **F6** (SocialData estimate-only + `healthService` deep-probe'un kaydetmediği küçük billable lookup) + embeddings için tam reserve/pre-gate (provider-key cap backstop var). Magnitude ihmal edilebilir; dürüst P3.
- boards/[id] + news-pool/run: kanıtla KORUNDU (silinmedi), belirsizlik operatör/roadmap kararı.
- Genuinely-public-URL log satırları (supadata x-api-key HEADER'da, HN/Reddit) bilinçli redakte edilmedi (mesajda secret yok).

**Yalnız operatör (external, YETKİLENDİRİLMEDİ):** Neon parola rotasyonu · Vercel prod env (`CREDENTIAL_ENC_KEY`/`ACCESS_PASSWORD_HASH`/`SESSION_SECRET` — yoksa instrumentation 500) + Deployment Protection · OpenRouter kredi (tek canlı golden) · Composio/Meta binding · Obsidian target · bounded live benchmark (USD tavanı) · push/PR/deploy. **NOT operational-core-live (canlı sağlayıcı round-trip YOK) / NOT production-launched (deploy YOK).**

---

## FINAL PROOF (2026-07-21) — kesilen sertifikasyon tamamlandı; tam 159 E2E GERÇEKTEN koştu, non-external P0–P2 çekirdeği kapandı; launch onayı bekleniyor

**Branch `feature/cemos-rebuild` @ `6efaf92`** (önceki cert `e17ebc4` üstüne **7 commit**; `0127213` üstüne kümülatif 18). Push/PR/deploy YOK. Tree temiz (`?? shots/` — dokunulmadı). Bu oturum: 4 bağımsız red-team ajanı (AI-accounting / redaction / dead-route+SSRF / silent-failure) → ana-thread kanıtlı düzeltmeler + false-positive geri çekme. Canlı ücretli AI **$0**; dış round-trip YOK; YENİ migration YOK.

**TAM GATE YEŞİL (gerçek exit kodları):** typecheck **0** · lint **0** · verify:catalog **OK** (9 preset canlı) · verify:acceptance **OK** (18 ekran / **84 mutation route hepsi guard'lı** [98→84, 14 ölü route silindi] / 4 cron) · verify:ai-economics **OK** · migration scan **14 additive / 0 destructive** (yeni yok) · unit **2261** (228 dosya, +net) · build **0** (temiz `.next`, `ƒ Proxy (Middleware)` + `ƒ /giris` dynamic).

**BİRİNCİ ZORUNLU SONUÇ — TAM E2E GERÇEKTEN TAMAMLANDI (önceki cert'i düzeltir):** Playwright **159 test / 20 spec**, deterministik **8 shard** (`--shard=k/8`), her biri foreground + gerçek exit: shard 1–8 = 21+20+25+15+23+21+18+16 = **159**, hepsi **EXIT 0** (`--list` toplamıyla birebir; her test tam bir kez; SKIP yok; pipe/tail/background exit-maskeleme YOK). Ayrıca `/giris` değişiminden sonra access-gate **3/3** yeniden doğrulandı. ⇒ "regresyon riski nil" iddiası artık tam-suite koşusuyla desteklenir.

**İKİNCİ ZORUNLU SONUÇ — real-PG integration: CI-READY / NOT-RUN (dürüst):** yerelde PG/Docker/Testcontainers/embedded-pg **YOK** (probe: psql/pg_ctl/docker absent, 5432 dinlemede yok) → itest'ler prod Neon'a ASLA koşmaz. Offline kapatılan: `db-integration.yml` statik doğrulandı (postgres:16 service + localhost DATABASE_URL + DB_INTEGRATION=1 + `migrate deploy`→`test:db-integration`); 5 itest yapısal sağlam; **ephemeral guard artık unit-test'li** (`guard.test.ts` — remote/prod URL THROW, yalnız localhost/opt-in geçer → "prod'u asla mutate etme" sözleşmesi kanıtlı, DB'siz PASS). Gerçek-PG koşusu CI'da; burada **PASS SAYILMADI**.

**Bu oturumda kapatılan (kanıtlı, non-external):**
- **SSRF DNS-rebind (marquee)** — önceki "undici yok → yarım fix" residual'ı ÇÜRÜTÜLDÜ: rotalar Node runtime (Prisma import), `node:https` + custom `lookup` ile socket doğrulanmış IP'ye PIN'lenir (SNI/Host/cert hostname'de kalır). `assertSafePin` + `makePinnedFetch` (Happy-Eyeballs `all:true` aware, body-capped); `verifyWebsite` + `toolbox/refresh` her hop pin'ler (redirect re-validate+re-pin). `pinnedFetch.test` REBIND'i kanıtlar (gerçek DNS'de çözülmeyen hostname yalnız pin ile bağlanır). Enjekte `fetchImpl` (testler) verbatim.
- **Dürüst degraded news** — `buildDailyDigest` başarısız üretimde BOŞ digest upsert ediyordu (health `Boolean(row)`→yeşil); artık başarısızsa persist YOK + health content-aware + `newsStagesDegraded` cron'u PARTIAL yapar + `syncRepoRadar` başarısız enrichment'ı "active" yazmaz.
- **Redaction** — `redactSecrets` artık Google `AIza…` (Gemini/YouTube) + URL cred query-param (`?key=`/`access_token=`/`fb_exchange_token=`) maskeler; DB-persist ham hata sink'leri sarıldı (`NewsItem.errorMessage`, `CronRun.resultJson` [miningError/stage summaries], `LearnProcessingJob.lastError`, `YtChannel.lastError`, gemini key-in-URL log).
- **`/giris` hydration mismatch** — client `typeof window` dallanması her yanlış-parolada React hydration hatası veriyordu → Server Component (searchParams sunucuda; JS'siz; access-gate 3/3, mismatch gitti).
- **Sahte-başarı kuyruğu**: `imageService.callFal` sessiz yutma → redacted log; `send-to-queue` GERÇEK içeriği `usedMock:true` etiketliyordu → `false`.
- **14 kanıtlı-ölü route + 6 coupled test SİLİNDİ** (queue/[id]/{approve,reject,mark-published,schedule,regenerate} · vector-memory/* · growth/feedback · content/search · voice-profiles · published-posts · youtube/discover; her biri UI/server/cron/MCP tüketici-yok kanıtıyla).

**Residual (dürüst, non-external, çekirdeği bloke ETMEZ):**
- Ücretli-sağlayıcı **degraded-tail muhasebesi** (Agent-A GAP-2/3, FP 0.15–0.35): Gemini/Supadata transkript + OpenRouter embeddings HTTP-ok'ta faturalanıp downstream-fail'de (≥200-char / malformed-200) ledger'a yazılmayabilir. Spine bypass-siz + `/api/costs` reconcile eder; bu yalnız degraded kuyruk. Fatura kesinliği belirsiz → mis-accounting riskiyle DEĞİŞTİRİLMEDİ.
- **Console-only redaction** (~13 site: cron/generate-morning, engagementLearning, ytOwnPerformance, aiSpendReservation, supadata) — Vercel log, çoğu Prisma/network (Bearer header, URL değil); düşük gerçek-secret.
- **Group-C ölü route** (competitors/prompt-library/source-posts + adjust-score/increment-usage) — KANITLA ölü ama `route-guards.test.ts` / paylaşılan `pattern-library` test cerrahisi gerektirir → ayrı sweep. `boards/[id]` + `news-pool/run` = belirsiz, KORUNDU.
- `BudgetSystemUnavailableError` route'larda honest-500 (503-kod değil; harcama YOK; kozmetik).

**Yalnız operatör (external, YETKİLENDİRİLMEDİ):** Neon parola rotasyonu · Vercel prod env (3 sır, yoksa instrumentation 500) + Deployment Protection · OpenRouter kredi (tek canlı golden) · Composio/Meta binding · Obsidian target · bounded live benchmark (USD tavanı) · push/PR/deploy. **NOT operational-core-live / NOT production-launched.**

---

## PRE-LAUNCH ADVERSARIAL CERTIFICATION (2026-07-20) — non-external P0–P1 = 0, düzeltilebilir P2 kapandı; launch onayı bekleniyor

**Branch `feature/cemos-rebuild`** (Owner/Perfection Pass `0127213` üstüne **10 commit**, HEAD bu doc commit'i). Push/PR/deploy YOK. Tree temiz (`?? shots/`). Yeni 8-subagent adversarial dalga (DB-concurrency / AI-accounting / security-SSRF / dead-route / product-false-success / cron-provider / test-realism / dormant-IG) → ana-thread kanıtlı düzeltmeler + false-positive geri çekme. Canlı ücretli AI **$0**; dış round-trip YOK.

**TAM GATE YEŞİL:** typecheck **0** · lint **0** · verify:catalog **OK** (9 preset, canlı) · verify:acceptance **OK** (18 ekran / **98 mutation route hepsi guard'lı** / 4 cron) · verify:ai-economics **OK** · unit **2257** (228 dosya) · build **0** (temiz `.next`, `ƒ Proxy (Middleware)` üretildi) · e2e **shell-smoke 8/8 real-server PASS** (bu koşu, 2.0m); tam 159-suite bu ortamda arka-plan süre-limitiyle KESİLDİ (fail DEĞİL — prior tam suite 159 passed); değişen yüzeye e2e assertion'ı YOK + silinen route'a e2e referansı YOK → regresyon riski nil.

**RUNTIME AUTH KANITI (yeni — önceki pass'ler dosya/manifest varlığına dayanıyordu):** `next start` prod sunucu + curl ile PROXY ENFORCEMENT canlı doğrulandı → `/giris`=200 (allowlist), `/api/health`=**401** `{"ok":false,"code":"unauthenticated"}` (proxy.ts:58), `/dashboard`=**307 /giris?next=** (proxy.ts:60-63), `/`=307. AYRICA sırlar yokken `instrumentation.js` başlangıçta THROW → yapılandırılmamış deploy her isteğe **500 fail-closed** (açık erişim DEĞİL). ⇒ güvenlik-denetiminin "tüm route auth'suz erişilebilir (P0-1)" iddiası ÇÜRÜTÜLDÜ; gerçek residual yalnız build-kıran vitest-config typo idi (düzeltildi). Boş `middleware-manifest.json` red-herring (Next 16 webpack proxy'yi başka mekanizmayla kaydeder; runtime doğru çağırır).

**Kapatılan (kanıtlı, non-external):**
- **P0/P1 sahte-başarı** (`draftService.ts`): 402/timeout'ta `createMockBenchmark` sabit pazarlama tweet'i tweet/auto niyette GERÇEK QueueItem olarak persist ediliyordu (yeşil cron + "hazır" Bugün) → ANY `usedMock` artık dürüst blocked (`pipeline_failed:<class>`). Owner pass'in D-closure'ının kaçırdığı ikiz motor.
- **Budget fail-CLOSED** (mandate): reservation altyapı hatasında (tablo yok / DB unreachable / status okuma hatası) → `BudgetSystemUnavailableError` (harcama YOK; eski fail-open `{id:null}` KALKTI); cold-start retry; kritik bölüm TEK `tx` bağlantısı (DB-agent self-starvation P2 kapandı).
- **Tüm ücretli AI muhasebeleşiyor**: Gemini/Supadata transkript UNGATED + silent-$0 idi → learn-budget gate + `recordTranscript`; OpenRouter embeddings + xConnector discovery scan + `recordScan` provider-tag + Fal/transcript Costs line-item reconciliation. Migration YOK.
- **Cron dürüstlüğü**: Sistem paneli liveness ledger (402→kırmızı; split-brain kapandı) · learn-cron budget-skip → partial · operator-scan-now 0-taslak dürüst (UI blanket-success yalanı kalktı).
- **Ürün**: series `accounts[0]` non-determinism (stable `orderBy`) · SSRF toolbox/refresh per-hop `assertSafeUrl`+manual-redirect · redaction (openrouter provider body + Prisma repo logları).
- **Real-Postgres itest katmanı** (`*.itest.ts` + `vitest.integration.config.ts` + CI `postgres:16` + double-guard): reservation overshoot · cron single-flight · viral atomic clamp/no-lost-update · feedback P2002 idempotency · OperatorSetting upsert. **YEREL PG/Docker YOK → CI'da koşar, SKIP-guarded, prod Neon'a ASLA dokunmaz (dürüst blocker; DoD "PASS sayma" izlendi).**
- **6 kanıtlı-ölü legacy route SİLİNDİ** (`/api/{generate,scan,benchmark,drafts,flow,news}`; 0 kod tüketicisi, acceptance yeşil kaldı).

**Migrations:** A (`OperatorSetting`) + C (`AiSpendReservation`) ZATEN prod Neon'da (Owner pass; guarded, before=after satır kaybı YOK, iki-deploy "No pending"). Bu certification'da YENİ migration YOK. `migrate status` = up to date.

**Residual (dürüst, non-external, backlog — çekirdeği bloke ETMEZ):**
- SSRF DNS-rebind TOCTOU (`verifyWebsite`): undici import EDİLEMİYOR (MODULE_NOT_FOUND doğrulandı) → node:https lookup-pin rewrite tested-verifier'ı riske atar; toolbox/refresh sertleştirildi ama verifyWebsite'te sub-saniye rebind penceresi açık (auth-gated + tek-operatör + metadata zaten bloklu → düşük gerçek maruziyet). Yarım-fix EKLENMEDİ.
- `redactError` broad rollout (~15 message-only `console.*` site: instagramService / cron routes) — Prisma parolayı zaten maskeler; düşük gerçek-secret riski.
- integration-DB testleri YEREL koşulamaz (Docker/PG yok) → CI-only.
- Kalan kanıtlı-DELETE-safe route'lar (queue/[id]/* superseded, vector-memory/*, adjust-score lost-update) — shared test-file coupling → ayrı sweep.
- `BudgetSystemUnavailableError` route'larda 503-kod yerine honest-500 (harcama YOK; kozmetik).

**Yalnız operatör (external):** Neon parola rotasyonu · Vercel prod env (`CREDENTIAL_ENC_KEY`/`ACCESS_PASSWORD_HASH`/`SESSION_SECRET`) + Deployment Protection · OpenRouter kredi (tek canlı golden) · Composio/Meta binding · Obsidian target · bounded live benchmark (USD tavanı) · push/PR/deploy. **NOT operational-core-live (canlı sağlayıcı round-trip YOK) / NOT production-launched (deploy YOK).**

---

## OWNER / PERFECTION PASS (2026-07-20) — non-external P0–P2 KAPANDI, launch onayı bekleniyor

**Branch `feature/cemos-rebuild` @ `1934468`** (baseline `7b82bdc` üstüne 16 commit). Push/PR/deploy YOK. Tree temiz (`?? shots/`). Adversarial 8-subagent read-only audit → ana-thread kanıtlı düzeltmeler. Canlı ücretli AI **$0**; dış çağrı yalnız read-only.

**TAM GATE YEŞİL:** typecheck **0** · lint **0** · unit **2252** (+32) · build **0** · e2e **159 passed** (8.7m).

**Closure A + C migration UYGULANDI** (prod Neon, guarded, her biri before=after satır sayımı [queueItems 68 / accounts 2 / newsItems 1669 / usageLogs 4220 / …] **SIFIR kayıp** + iki-deploy "No pending"): `20260720120000_add_operator_setting` + `20260720130000_add_ai_spend_reservation`. `migrate status` = "up to date". Read-only kredi-şifreleme denetimi: `IntegrationCredential` **0 satır** (plaintext-at-rest = 0; env-token modeli).

**Kapanan P1 (7 commit):**
- `bedfaae` **B cold-start**: `generateJsonGated` artık `getModelProfile()`'ı estimate+call ÖNCESİ hydrate eder → soğuk instance operatörün durable profilini kullanır (Settings ziyareti gerekmez). Sahte "instrumentation re-hydrates" yorumu + "yeni sunucu örneklerinde geçerli" UI yalanı düzeltildi.
- `bcebfdf` **D canned fallback**: writer başarısızlığında sabit pazarlama metni + sahte "publish"/75 kritik KALKTI → dürüst rethrow (route 402/5xx + UI blocked/error zaten var); kritik başarısızlığı = dürüst degraded "rewrite". `generateDraftsFallback` ölü işaretlendi.
- `0bb83f3` **OperatorReadinessGate**: birincil "Üret" butonu artık gerçek `draftsCreated/draftsBlocked` sayar + kuyruğu tazeler (`onGenerated`→`fetchDrafts`) + app Toast (0 taslak → dürüst "üretilmedi"; eski `alert("başarıyla tamamlandı")` yalanı kalktı).
- `ffbc239` **Learn prompt injection**: 8 builder ham transkript/özet/başlığı `wrapUntrustedData` ile çitler + `UNTRUSTED_DATA_NOTICE` system'de (repo'da fence'siz TEK ingestion yoluydu).
- `239a03e` **F provider honesty**: Entegrasyon paneli liveness ledger'ı env-presence health'in ÜSTÜNDE tutar (pure `integrationDisplay` modülü) → 402-tükenmiş OpenRouter / hiç-çağrılmamış sağlayıcı artık yeşil "bağlı" DEĞİL; yalnız gerçek DB probe green yapabilir.
- `cd605a4` **proxy fail-open**: sırlar yoksa + runtime prod algılanmıyorsa (NODE_ENV tanımsız custom server/Docker) TÜM app açılıyordu → `isLocalDevRuntime()` (yalnız pozitif dev/test; asla Vercel/tanımsız) proxy + cron + snapshot'a bağlandı.
- `95fcc12` **morning single-flight**: `runDailyForAccount` count-then-act yarışı (cron+retry → çift LLM + duplicate draft) → `cronRunRepo.startIfIdle` (advisory-locked atomic, fail-open) generate-morning'e bağlandı.

**Gate (95fcc12):** typecheck **0** · lint **0** · unit **2240** (+20). Tam build + e2e P2 dalgası sonunda toplu koşulacak.

**Ek P1/P2 KAPANDI (16 commit toplam):** morning-gen single-flight (`cronRunRepo.startIfIdle` advisory-locked atomic) · proxy fail-closed default (`isLocalDevRuntime`; proxy+cron+snapshot) · Learn 8-builder prompt-injection fencing · feedback server-side idempotency · timing-safe token compare · atomik viral-score · daily-cron partial dürüstlüğü + healthService yüzeyi · DigestSection/SettingsTab swallow düzeltmeleri · **closure C** reservation · **closure F** provider honesty · **closure H** IPv6-gap.

**Residual (dürüst, non-external, backlog — çekirdek release'i bloke ETMEZ):**
- SSRF DNS-rebind TOCTOU — undici modül olarak import edilemiyor (Node global fetch'te lookup hook yok); node:https ile pinned-fetch rewrite'ı iyi-test-edilmiş verifier'ı değiştireceği için ertelendi. Yarım-fix EKLENMEDİ.
- Gemini transcript (`learning/gemini.ts`) + OpenRouter embeddings (`vector-memory.ts`) UsageLog'a yazmıyor → budget fraksiyon-cent under-read (düşük-$ accounting deliği; embeddings hata → local hash dürüst fallback).
- `redactError`/logger rollout ~20 ham `console.error/warn` sitede eksik (hijyen; secret yalnız server log'una, HTTP yanıtına DEĞİL — `apiResponse.fail` zaten redact eder).
- Dead-route sweep (~25 kanıtlı DELETE-safe route) — RC-churn riskiyle ertelendi; hepsi guard'lı, akış kırmıyor.
- `generateDraftsFallback` fiziksel silme (unreachable + DEAD işaretli).

**Yalnız operatör aksiyonu (external):** Neon parola rotasyonu · Vercel prod env + Deployment Protection · OpenRouter kredi (tek canlı golden) · Composio/Meta binding · Obsidian target · bounded live benchmark (USD tavanı) · push/PR/deploy.

---


> Sonraki oturumun kesin handoff'u. Context daralırsa buradan devam. Güncelleme: 2026-07-19, **PHASE 4C: LEARN INTAKE → GROUNDED PACK → ATOMİK NOT/ZİHİN HARİTASI/GÖREV/İÇERİK FİKRİ/REVIEW TAMAMLANDI (ADR-042); Phase 4A (ADR-040) + 4B (ADR-041) da tamam. Push/deploy YOK. Kritik açık kapatıldı: notes/graph/tasks artık PASSTHROUGH DEĞİL (gerçek üretim) + content_ideas eklendi; pipeline v2 (v1 pack'ler okunur, reprocess yok); v2 artifact zarfı (notesJson); üç açık kaynak türü (youtube/manuel/NotebookLM — provenance sunucu-set, NotebookLM summary-basis "videoda doğrulanmadı"); review idempotency (tek additive migration); kanonik status read-model. Phase 4D (Obsidian export production contract + tipli durum makinesi + hardened local/GitHub kanallar + kalıcı audit + gate'li auto-export + ABSORBED legacy fiziksel emekliliği) TAMAMLANDI (ADR-043); Obsidian CANLI kanal doğrulaması (gerçek vault yazımı / gerçek GitHub commit) BLOCKED-EXTERNAL (env/target/onay yok). "Phase 4 tamamen tamamlandı" / "Obsidian canlı doğrulandı" / "canlı AI doğrulandı" / "production mutation ile test edildi" İLAN EDİLMEDİ. Phase 3 (3A–3E) production contract BİTTİ ama canlı AI üretimi + own-account Composio + Tier-2 render dış-bağımlı BLOCKED. Phase 4D sıradaydı; şimdi **PHASE 5A: CANONICAL FEATURE RE-HOME + PRODUCT HARDENING TAMAMLANDI (ADR-044).** Emekliye ayrılan ekranların benzersiz eylemleri canonical IA'ya taşındı (YENİ top-level YOK): Bugün feedback/rescore (idempotent + dürüst blocked/degraded + telemetry.judged bug fix), Kütüphane pattern arşivle/viral kaldır + save-pattern idempotency, Plan competitor watchlist "hesap ekle" + outlier deep-link (mapRadar url drop fix); 3 ölü IG legacy component (InstagramTab/CompetitorRadarSection/ReelsDossierSection + cascade-orphan TimelineLane) FİZİKSEL emekli (instagram→plan-seriler alias + /api/instagram/* backend + Prisma modelleri KORUNDU); first-navigation flake kök-nedenden çözüldü (AppShell data-shell-ready + [data-skeleton] settle + global-setup tarayıcı/veri warm-up — waitForTimeout/retry YOK); Tier-2 entegrasyon satırı + PHASE-5B-ACTIVATION-PLAN.md. Parity matrisi kanıt-temelli ÖNCE çıkarıldı (PHASE-5A-REHOME-PARITY.md). TEK additive migration (FeedbackEvent.idempotencyKey NULL-ayrık unique) prod Neon'a uygulandı (öncesi=sonrası satır kaybı YOK, iki-deploy "No pending"). Gate: typecheck 0 / lint 0err-4preexisting (YENİ 0) / catalog OK 9 preset / unit **2150** / build 0 / e2e F-hedefli-10x retries=0 **10/10** + tam suite **153 passed** (shell-smoke first-nav flaky'si guard'la 3/3 fixed) + 8 HARNESS shot. **CANLI dış yazma/AI/ödeme/Neon rotasyonu/push/PR/deploy YOK** — kod turnkey, credential operatör bekliyor. Kapsam dışı (5B): training-center geçmiş görünümü, X-Idea/create-draft köprüsü. Şimdi **PHASE 5B: KALAN ÜRÜN BORÇLARI + ENVANTER DENETİMİ TAMAMLANDI (ADR-045; 2026-07-20).** (5B-A) Eğitim Merkezi öğrenme geçmişi TEK read model'e katıldı (`ProfileMemoryTab`; aday-vs-validated pattern + feedback detay + eğitim külliyatı + öğrenme etkinliği kartı) + gerçek eksik değer `FeedbackEvent.neutralizedAt` "yanlış sinyali etkisizleştir/geri al" (fail-closed, ham kayıt silinmez) + `POST /api/memory/signals`; `training-center` alias morning→profile-memory. (5B-B) Öğrenme content_idea → X taslağı (`draftBridge`+`POST /api/learn/packs/[id]/draft`+LearnPackView "Taslağa dönüştür"): sunucu-otoriteli metin, deterministik $0, `QueueItem.originKey` NULL-ayrık idempotency (çift-tık duplicate YOK), status "new" (auto-publish YOK), Bugün deep-link (`focusDraftId`→`focusSeed`); orphan create-draft aynı idempotency ile sertleşti; İlham'ın Instagram→Seriler/Takvim ayrımı KORUNDU. (5B-C) 4 yolculuk e2e (memory neutralize 2 + Learn→draft 1 yeni). (5B-E) salt-okunur envanter denetimi + haber-cron yorum yalanı düzeltildi + aktivasyon doküman işaretçileri. **İKİ additive migration** (`20260720100000_add_feedback_neutralized_at` + `20260720110000_add_queue_item_origin_key`) guarded workflow'la prod Neon'a — satır kaybı YOK, iki-deploy "No pending". Gate: typecheck 0 / lint 0err-4preexisting (YENİ 0) / catalog OK / unit **2184** (+34) / build 0 / e2e **156 passed** (+3). **CANLI ücretli LLM/Obsidian/Composio/Meta/X/Tier-2 round-trip + Neon rotasyonu + push/PR/deploy YOK.** Görsel shot'lar 5D geçişine batch'lendi. Şimdi **PHASE 5C: OUTCOME-AWARE KALİTE KALİBRASYONU = DÜRÜST DURUM YÜZEYİ TAMAMLANDI (ADR-046; 2026-07-20).** Üç paralel harita: eval/outcome/memory/reels-gate sistemleri ZATEN yüksek standartta kurulu (Phase 2E + Sprint 7/9); eksik = "kalibre edildi" yalanını reddeden dürüst durum yüzeyi. (5C-A/C) `calibrationStatus` read model + `GET /api/quality/calibration` + Sistem `CalibrationStatusCard`: gerçek sonuç örneklemleri (eşleşen PerformanceSnapshot/IgInsightSnapshot/validated-vs-aday pattern) eşiklere (COLD_START_MIN_SAMPLE=5, MIN_SUPPORT=3) karşı sayılır → near-empty tablolarda `outcomeCalibrated=false` + açık "KALİBRE DEĞİL"; normalizedScore HAM olduğu + eşiklerin provisional olduğu + canlı beslemelerin BLOCKED-EXTERNAL olduğu dürüstçe bildirilir (sahte kalibre YOK). (5C-D) `deriveInstagramPlanHealth` konsolide `meetsBar` verdisi (mevcut counts'tan türetildi, ikinci readiness sözlüğü YOK) + `PlanHealthStrip` yüzeyi; per-dossier gate + honesty guard'ları REUSE (ADR-036/038/039 bozulmadı). (5C-B) marka-hafızası provenance = sistemin EN GÜÇLÜ parçası, doğrulandı (kod yok); aday-pattern pre-validation etkisi 5B-A'da dürüst etiketli, gate-from-output backlog. Hiçbir eşik gevşetilmedi; Cohen's-κ hook labeled-data yokluğunda bilinçli unwired. **SIFIR migration.** Gate: typecheck 0 / lint 0err-4preexisting (YENİ 0) / catalog OK / unit **2195** (+11) / build 0 / e2e (kalibrasyon kartı + meets-bar şeridi + Takvim guard yeşil). Canlı LLM/round-trip YOK. Şimdi **PHASE 5D: RELEASE-CANDIDATE HARDENING TAMAMLANDI (ADR-047; 2026-07-20).** İki paralel denetim + dep audit + görsel doğrulama. **Güvenlik:** CRITICAL/HIGH YOK, hardcoded secret YOK; erişim sınırı GERÇEK oturum kapısı (`proxy.ts`: scrypt+HMAC cookie+throttle, prod fail-closed) — Vercel Deployment Protection'ın üstünde. İki MEDIUM choke-point'te kapatıldı: `apiResponse.fail()` secret/gövde redaction (~119 route) + openrouter kaynak sınırı; forge-safe `wrapUntrustedData` 3 adversarial-metin sitesine (reverseEngineer/comment/dm). Residual: SSRF DNS-rebind TOCTOU + postcss-via-Next moderate (istismar-edilemez) + Neon rotasyonu (USER-ACTION). **Dead-code:** import-graph 0-tüketici → 8-dosya agent/host kümesi + training-center route(+test) + orphan JSON SİLİNDİ; weekly-learning-report alias morning→profile-memory düzeltildi; kalan legacy route'lar (mark-published dahil, `DraftReviewCard` testid'i çürütüldü) kanıt-temelli POST-RELEASE-BACKLOG'a ertelendi. Type-escape/silent-catch/placeholder temiz; createMockBenchmark gated+usedMock. **Perf:** ölçülmüş darboğaz yok → mikro-opt yok. **Görsel:** 12 REAL-DB-READONLY shot (1024+1920 taşma 0, dürüst durumlar). **SIFIR migration.** Gate: typecheck 0 / lint 0err-4preexisting (YENİ 0) / catalog OK / unit **2181** (−14 silinen training-center testi, +2 redaction) / build 0 (temiz .next) / e2e **158 passed**. Sonraki: **RELEASE GATE** (konsolide onay isteği — push/deploy operatör kararı; canlı round-trip/push/PR/deploy YAPILMADI).**

## Aktif durum

- **Faz: PHASE 5F — AI ECONOMICS, LIVE READINESS & FINAL CLOSURE — KOD/TEST TARAFI TAMAMLANDI (2026-07-20; `325bbf2` üstüne **9 commit** → HEAD `533327a`).** Sınıflandırma: **CemOS AI Economics tamamlandı; launch ve bounded live benchmark onayı bekleniyor.** TAM GATE YEŞİL: typecheck **0** · lint **0/0** · verify:catalog OK (9 preset, canlı) · verify:acceptance OK · **verify:ai-economics OK** · unit **2220** · build **0** (temiz) · e2e **159 passed / 0 failed** (8.8m). Ek 5 commit (yukarıdaki 4'e): `2d404aa` feat(integrations) provider liveness §13/BUG-05 · `3760d3f` refactor(cleanup) redacted logger + shared redactSecrets §15 · `3677f1b` docs(release) drift+backlog §3/§10/§15 · `533327a` fix(settings) instrumentation edge-bundle §6-followup · (state). §10 TOCTOU = scoped P2 (dürüst tehdit modeli + AiSpendReservation spec, yarım sistem YOK); SSRF-TOCTOU = scoped P3 (undici IP-pin spec, yarım fix YOK); §5/§8/§9/§11/§12 = provisional/benchmark-gated backlog. **GOTCHA (tekrar etme):** §6 boot-hydration'ı `instrumentation.register()`'dan settingsService(→Prisma) dinamik import etti → Next instrumentation'ı EDGE için derler, `@prisma/client` `node:child_process` UnhandledScheme → dev derleme kırıldı, TÜM route 500 (e2e global-setup login yakaladı; production build TOLERE etti, dev/e2e ETMEDİ). Fix: hydration kaldırıldı (P1 fix ona bağlı değil); durable profil node-runtime `/api/settings` GET + `setModelProfile` POST ile process.env'e converge eder. **SIFIR prod DB yazımı** (OperatorSetting migration hazır+scan-clean, deploy'a bağlandı); **canlı ücretli AI/round-trip YOK** ($0); **push/PR/deploy YOK.** [ESKİ NOT — süperseded]: Amaç: AI hattını kaliteyi düşürmeden fiyat/performans + maliyet/routing/budget doğrulama + configured-broken sağlayıcı gizli-başarı engelleme + son launch kapısı. **`325bbf2` üstüne 4 commit (şimdiye):** `82d9fcf` fix(ai) pricing provenance (§7 — canlı katalog 2026-07-20 RE-DOĞRULANDI: 8/8 slug fiyatı MODEL_PRICING ile TAM EŞLEŞTİ; claude-opus-4.8 5/25 eklendi; `MODEL_PRICING_VERIFIED_AT`+`hasVerifiedPrice`; bilinmeyen slug pozitif rol-tahmini, ASLA $0; +4 test) · `720bc06` fix(settings) durable model-profile (§6 **P1 "settings success lie"** kapandı — POST `.env.local` yazıyordu = Vercel read-only FS/ephemeral; **additive `OperatorSetting` key/value tablo** [migration `20260720120000`, statik destructive-scan ADDITIVE, apply DEPLOY'a ertelendi], `settingsService` get/set [DB + env fallback + short-TTL cache + table-missing fail-open], POST yalnız commit sonrası success, GET durable okur, `instrumentation.register()` boot hydration, dürüst UI copy; +15 test) · `cb1f184` docs(ai) AI-COST-QUALITY-MATRIX (§4 — **47 prod call-site** envanteri, DIRECT-UNGATED=∅ [grep DOĞRULANDI], iki-katman routing [9 preset MODEL_PROFILE'ı yok sayar + 6 role profile-aware], §5 çelişki [preset-judge gpt-5.4-mini vs role-judge gemini], 7 dead/2 gated-off site, Tier-0 aday, provisional öneriler — benchmark yok → "en ucuz=en iyi"/"kalite düşmedi" İDDİA YOK) · `2388059` test(ai) `verify:ai-economics` (§17 — ağsız/ücretsiz statik guard: purpose attribution / primary+fallback fiyat kaynağı / writer≠judge aile / premium+editor default-off / eval-spend default-off / bilinmeyen model ≠$0 / primary katalog fiyatı maxPrice tavanı altında / budget sınıfları / verified-at). **Explorer alt-ajanı** call-site envanterini çıkardı; kritik DIRECT-UNGATED=∅ iddiası kendi grep'imle DOĞRULANDI. **Gate (şimdiye):** typecheck **0** · unit **2207** (+19: 4 pricing / 11 settings / 4 route) · migrate:scan tüm 13 additive · verify:ai-economics OK · verify:catalog beklemede (canlı, sonda). **SIFIR prod DB yazımı** (OperatorSetting migration hazır+scan-clean ama prod'a UYGULANMADI — Neon rotasyonu USER-ACTION beklerken deploy'a bağlandı); **canlı ücretli AI/round-trip YOK** ($0; live benchmark USD tavanı bekliyor — §8/§19); **push/PR/deploy YOK.** **KALAN:** §10 budget TOCTOU (reservation — assess+safe-implement veya dürüst P2 belge), §13 integration liveness (BUG-05), §3 doc drift (RELEASE-GATE HEAD/sayılar), §15 cleanup (workerService log / coverage lint / dead-route re-verify / SSRF-TOCTOU), tam regresyon + §20 final rapor. Resume: bu bölüm + `AI-COST-QUALITY-MATRIX.md`.
- **Faz: PHASE 5E — FINAL ACCEPTANCE & REPAIR — TAMAMLANDI (2026-07-20). Sınıflandırma: Final Acceptance tamamlandı; bilinen (reproduce edilebilir) P0–P2 hata YOK, launch onayı bekleniyor. NOT operational-core-live (canlı round-trip yok) / NOT production-launched (deploy yok).** Kaynak resume anchor'ları: `FINAL-ACCEPTANCE-MATRIX.md` + bu bölüm. **Başlangıç `7a5dac2` üstüne 5 commit:** `53f30b8` feat(handoffs) format-aware Learn idea routing (BUG-01/02 P1) · `538395f` fix(security) safeExternalHref 12 site (BUG-06 P2) · `a440310` test(acceptance) `verify:acceptance` script + lint 0/0 · `bd00737` test(e2e) format-handoff (phase4c 11/11). **4-subagent envanter** (surfaces/API-138/integrations/idea-handoff): erişim sınırı GERÇEK oturum kapısı (proxy+session+scrypt+HMAC+AES-GCM+guards — hepsi PASS); **P0 YOK** (104/104 mutation guard'lı, `verify:acceptance` backstop); XSS temiz (dangerouslySetInnerHTML=0). **Fixler:** BUG-01 carousel/reel Learn fikri artık X tweet DEĞİL → server-otoriteli handoff (carousel→Seriler, reel→Takvim; sourceKind+="learn"; kind-discriminated route; format-CTA UI) + e2e; BUG-02 hedef hesap sunucuda türetilir; BUG-06 tested `safeExternalHref` 12 ingested-URL sitesine + NewsPoolTab dup silindi. **Doc-resolved:** BUG-03 Idea = programatik/MCP sözleşmesi (`/api/mcp:74` okur), BUG-04 dead/dormant model (IG DM/Yorum kümesi + ScanRun/GenerationRun + seed-only; model drop=destructive/yasak), BUG-05 silent-degrade blocker-gated backlog. **BUG-07 GERİ ÇEKİLDİ** (B false-positive; gerçek fetch operator-scan-now). CEMOS.md §2/§7 doc-defektleri düzeltildi (eski nav + eski "login yok" iddiası). **SIFIR migration/DB yazımı/canlı round-trip/push/deploy.** **TAM GATE YEŞİL:** typecheck **0** · lint **0/0** (coverage silindi + prune-prompts const) · catalog **OK** · **acceptance OK** (104/104 guard) · unit **2188** (+7) · build **0** (temiz) · e2e **159 passed / 0 fail** (+1 carousel-handoff). Dead-route sweep (§8D ~25 verified DELETE-safe) RC-churn + B false-positive nedeniyle POST-RELEASE'e ertelendi. **P3 residual (dürüst):** SSRF-TOCTOU (undici IP-pin scoped task) + dead-route sweep + coverage-ignore hook-bloke. Görsel: Learn değişimi (format-CTA) phase4c responsive e2e "1024–1920 taşma 0" ile kapsanır; ayrı phase5e shot klasörü ÜRETİLMEDİ (dürüst). **SIFIR migration/DB yazımı/canlı round-trip/push/deploy.** 6 commit (`53f30b8`→`37cb055`). Sonraki: operatör onayıyla push/deploy (RELEASE-GATE §10).

- **Faz: PHASE 4D — OBSIDIAN EXPORT PRODUCTION CONTRACT + ABSORBED LEGACY EMEKLİLİĞİ TAMAMLANDI (ADR-043; 2026-07-19). Canlı kanal doğrulaması BLOCKED-EXTERNAL.** v1 export vardı; yeniden İCAT EDİLMEDİ. **v1 açıkları kapatıldı:** non-deterministik `created:now()`, v2 artifact yokluğu + basis-farksızlık, direct-overwrite yerel yazım, GitHub unchanged-PUT, not-ready direct export, audit yokluğu. **Deterministik + basis-farkında v2 bundle** (`obsidian.ts` yeniden + `packExport.ts` + `obsidianManifest.ts`): MOC/kaynak/atomik not/paylaşımlı çok-pack kavram/Mermaid MOC (typed graph — ham LLM Mermaid YOK)/görev/fikir/kart/QA-provenance; managed frontmatter (pack/pipeline/prompt/basis/QA/kind); now() gömülmez (stable createdAt); NotebookLM=summary framing + sahte timestamp YOK; collision-safe pack klasörü; paylaşımlı-kavram merge önceki kaynağı korur; manifest hash (already_current). **Tipli durum makinesi** (`obsidianExport.ts`, "null=her şey" kalktı): kanal başına ChannelResult (not_configured/not_ready/prepared/succeeded/already_current/partial/conflict/failed + sayılar + errorClass + güvenli hedef etiketi); secret/token/tam-path ASLA tutulmaz. **Sertleştirilmiş kanallar:** `localVault.ts` (realpath kök + symlink/junction kaçış guard + atomic temp+rename + managed conflict + already_current skip), `githubVault.ts` (read-only preflight + unchanged-skip [gereksiz commit yok] + errorClass haritası + partial≠success + token loglanmaz + deterministik commit). **Kalıcı audit** `LearnExportAttempt` + additive migration `20260719160000_add_learn_export_attempt` (composite unique = manifest idempotency; idempotencyKey NULL-ayrık client dedup); `exportService` TEK audited yol (eski audit-suz adaptörler + `obsidianWriter.ts` SİLİNDİ). **Gate'li auto-export:** `integration_suggestions` yalnız `OBSIDIAN_AUTO_EXPORT=true` iken; aksi örtük dış yazma YOK; "aşama tamam" ≠ "export başarılı". **API+UI:** GET obsidian not-ready→409; POST `/export` (auth+Zod+idempotencyKey→audited); `LearnExportPanel` 3 kanal AYRI + configured/unconfigured (env NAME) + son sonuç + retry; ZIP "sync değil"; `/api/integrations` Obsidian yerel+GitHub ayrı (Vercel fs kalıcı değil/Obsidian Git notları). **Legacy emeklilik:** 10 runtime-DARK component silindi (DailyQueue/Keyword/Prompt/Pattern/Learn/FlowRadar/SourceIntelligence/ViralLibrary tab + orphan FlowList/TweetCard; grep 0 importer); ViralLibrary savedTweets localStorage→DB drenajı saf/testli `savedTweetsMigration.ts`'e taşındı + AppShell hydration-farkında mount bootstrap (idempotent); **KORUNDU:** InstagramTab+CompetitorRadarSection+ReelsDossierSection (canonical evi yok, cascade-orphan; re-home bekliyor), TAB_ALIASES/store v9/deep-link/`useXAgentStore`/"xagent-store"/`XAgentApp.tsx`/User-Agent/tüm API+model. Eski id'ler 3-katmanla canonical'a çözülür. **Gate:** typecheck 0 · lint 0 err/4 pre-existing warn (4D DIŞI; 4C'nin ViralLibraryTab uyarısı silmeyle gitti) · catalog OK (9 preset) · unit **2139/2139** (+46) · build 0 · e2e **+8 phase4d** (panel/kanal durumları/succeeded/already_current/partial/ZIP/eski deep-link çözümü/savedTweets göçü). **DB:** additive migration Neon'da; önce=sonra Learn sayıları EŞİT (SIFIR kayıp/yazım), `LearnExportAttempt`+4 index PRESENT, ikinci deploy no-op, shadow-url YOK, destructive YOK. **Yapılmadı:** gerçek vault yazımı/GitHub commit (BLOCKED-EXTERNAL), IG/pattern/daily-queue detay UI re-home (backend korundu), push, deploy. Sonraki: **Phase 5**.

- **Faz: PHASE 4C — LEARN INTAKE → GROUNDED PACK → ATOMİK NOT/ZİHİN HARİTASI/GÖREV/İÇERİK FİKRİ/REVIEW TAMAMLANDI (ADR-042; 2026-07-19).** ~%90 altyapı hazırdı (`learning/*`, `components/learn/*`, `/api/learn/*`, 9 Prisma Learn modeli) — yeniden İCAT EDİLMEDİ. **Kritik açık:** `PASSTHROUGH_STAGES`={notes,graph,tasks} no-op'tu → artık BOŞ (gerçek üretim aşamaları) + yeni `content_ideas`; `concepts` `notes`'tan ÖNCE. **Pipeline v2** (`PIPELINE_VERSION`/`PROMPT_VERSION` v1→v2 → yeni pack satırı; v1 pack'ler okunur[hasArtifact=false legacy notu], toplu reprocess YOK); her yeni aşama Zod+repair+rol+trace+gerçek maliyet+budget gate+retry cap; `artifact.stages.includes` çift-ücret koruması. Orchestrator lease/resume/STAY-partial/BudgetExceeded→429/TranscriptUnavailable KORUNDU. **Basis-farkı (dürüstlük):** `SourceBasis` transcript|summary (`basisForKind`); yeni `summary_supported` grounding; prompt'lar+QA basis-farkında (`computeQaReport(basis)`) → summary pack'te `source_supported` = "videoda doğrulandı" YALANI flag'lenir+coverage'a sayılmaz; NotebookLM iddiaları asla video-doğrulanmış görünmez. **v2 artifact** (`src/lib/learning/artifact.ts`, `LearnPack.notesJson` içinde — yeni tablo YOK): Zod zarf `{version,sourceBasis,atomicNotes,graph{nodes,edges},tasks,contentIdeas,stages}`; `parseArtifact`→v2|legacy|**invalid** (parse hatası sessizce boş DEĞİL); `graphToMermaid` deterministik+escape'li+dangling atlar (ham LLM Mermaid YOK). **4C-A intake** (`createSource(SourceIntake)`): youtube[videoId] | manual_transcript | notebooklm_summary[içerik SHA-256 externalId idempotent; ham metin ID/log'a yazılmaz]; provider/basis/verified SUNUCU koyar (istemci belirleyemez — Zod discriminated-union); NotebookLM metaJson basis=summary+verified=false; zaman-kodsuz metin `plainTextSegments` pseudo-segment (0-chunk açığı kapandı, timestamp gösterilmez); metadata yalnız youtube. **4C-E** (`src/lib/learning/status.ts`): TEK `deriveLearnState`→inbox|processing|transcript_required|budget_blocked|needs_review|ready|failed (ikinci readiness sözlüğü YOK; çelişki engellendi). **4C-H** (tek additive migration `20260719140000_add_learn_review_attempt_idempotency`): `LearnReviewAttempt.idempotencyKey String?`+`@@unique` (NULL-ayrık); `reviewService.grade` ATOMİK `$transaction`+İDEMPOTENT (aynı key→deduped, ladder iki kez atlamaz; yarış P2002→mevcut durum); client kart-başı key+senkron çift-tık guard+in-flight disable+POST hatası ilerletmez. **DB:** additive-only, önce=sonra Learn sayıları (sources 5/packs 3/schedules 40/attempts 0 EŞİT → SIFIR kayıp/yazım), index PRESENT, shadow-url YOK. **UI:** `LearnPackView` 7 sekme + provenance chip (timestamp yalnız timed provider) + NotebookLM uyarısı + not-ready Obsidian/hazır-bilgi gizler + erişilebilir zihin haritası(+Mermaid `<details>`) + içerik fikri "öneri, yayınlanmış değil"+kopyala (otomatik terfi YOK); `LearnProcessingView` mount GET resume + backoff + budget/transcript/failed ayrı + gerçek aşamalar görünür; `LibOgrenmeTab` üç açık kaynak modu (NotebookLM "doğrulanmış transkript değildir") + userState grouping; `Card` `data-testid` iletir. **Gate:** typecheck 0 · lint 0 err/5 pre-existing warn (hepsi 4C DIŞI) · catalog OK (9 preset) · unit **2093/2093** (+39: artifact 15/status 9/intake 7/reviewService 4/qa +3/pipeline +1) · build 0 · e2e **145 passed + 1 pre-existing faz1d first-nav flake (retry-green; "flakesiz" iddia EDİLMEZ), 0 failed** (+9 phase4c). Kanıt `shots/cemos-rebuild/phase4c-learning/` (01 REAL-DB-READONLY = gerçek Neon GET-only: 3 hazır paket/40 bugün tekrar/3 gerçek ready pack[2 v1 raw-URL+1 başlıklı]/canonical Hazır rozeti/3 kaynak modu — save TIKLANMADI; 02-13 HARNESS route-mock — dosya adında açık); 1024/1280/1440/1920 taşma 0, app console error 0 (yalnız Neon cold-start). **Yapılmadı:** Phase 4D, canlı ücretli AI ($0; env varlığı canlı onay sayılmadı; ücretli kısım deterministik fixture'la doğrulandı), production Learn tablolarına test kaydı (hermetik/read-only), gerçek Obsidian export, canonical içerik-fikri→Plan/Idea terfisi (paket içinde dürüst öneri), push, deploy. Legacy semboller korundu. Sonraki: **Phase 4D** (Obsidian canlı doğrulama + ABSORBED legacy fiziksel emekliliği).

- **Faz: PHASE 4B — UNIFIED LIBRARY: kanonik save/capture/board sözleşmesi + araştırma köprüsü TAMAMLANDI (ADR-041; 2026-07-19).** Kütüphane kopuk "arama listesi + İlham panosu" olmaktan çıktı; TEK save-to-board sözleşmesi + araştırma→kütüphane köprüsü. **Model duplike EDİLMEDİ** (`ContentItem`/`Board`/`BoardItem` + `normalizer`/`ingestContent` REUSE). **DB: tek additive migration `20260719120000_add_board_item_content_unique`** — `BoardItem @@unique([boardId, contentItemId])` (PostgreSQL NULL-ayrık → serbest url/not satırları etkilenmez; "tek kanonik içerik / pano" değişmezi DB düzeyinde, tüm ekleme yolları için); kod-öncesi read-only kanıt 0 board/0 dupe → temiz uygulandı; `db:migrate` safe workflow (statik-scan additive, önce=sonra snapshot [satır kaybı yok], ikinci deploy "No pending" idempotent, `migrate status` up-to-date); shadow-url YOK; post-apply WARM doğrulama: index PRESENT (`BoardItem_boardId_contentItemId_key`), Board/BoardItem hâlâ **0** = oturumdan SIFIR production yazımı. **Teslim:** `src/lib/boards/saveToBoard.ts` (resolveBoardForSave scope/archived typed — hesap-scoped panoya başka hesap/accountId'siz istek fail-closed; saveContentToBoard idempotent+`pg_advisory_xact_lock`+**P2002-backstop** [unique yarışı → 500 değil, re-read `alreadySaved`]; boardMembershipFor N+1'siz; boardId yoksa paylaşılan default "Kaydedilenler") · `src/lib/boards/saveFromSource.ts` (client YALNIZ `{kind,id}`; sunucu YETKİLİ origin satırını `fromX` normalizer'la yeniden yükler — kırpılmış client DTO ContentItem kurmaz, uydurma yok; `ingestContent` (platform,externalId) idempotent dedup; desteklenmeyen şekil dürüst `unsupported_source`; kinds contentItem/news/ytVideo[videoId@unique]/sourcePost/igMedia/repo) · `POST /api/library/save` (Tümü+5 araştırma TEK sözleşme; guard+Zod[gerçek SaveSourceSchema]+typed kod→HTTP) · `/api/boards/[id]` POST içerik saveContentToBoard'a delege + serbest url/not guard'lı · `/api/boards?savable=1` = paylaşılan+aktif hesap (başka hesap HARİÇ) · `/api/library/search` görünen sayfaya toplu `savedBoards` (N+1 yok) + kararlı sayfalama (createdAt-desc+id tie-break, testli) + dürüst `capped` bayrağı; `LibTumuTab` stale-response guard. Yeni mutasyon route'ları route-guards matrisinde (+3). **UI (dark editorial + ADR-040):** reusable `SaveToBoardButton` (menü **PORTAL→document.body** fixed konum → Drawer overflow/scrim menü tıklamasını yemez [canlı e2e'de yakalandı, düzeltildi]; pessimistic sunucu-uzlaşımı — hata başarı gibi görünmez; "Kayıtlı"+pano adı; hızlı kaydet + pano seç/oluştur) · LibTumuTab satır "Kayıtlı" rozeti + drawer save + non-content dürüst not · `IlhamContextRail` (aktif pano/kayıt-pano-analiz sayaçları/son analizler/hızlı yakalama — yalnız gerçek workspace verisi; `.ilham-workspace` grid ≤1200px'de alta düşer) · 5 araştırma satırında (news/youtube/viral-radar/source-intel/discovery-OutlierHighlights) ortak save; discovery pipeline sayaçları save-eligible DEĞİL (dürüst). **Gate:** typecheck 0 · lint 0 err/5 pre-existing warn (hepsi 4B DIŞI) · verify:catalog OK (9 preset) · unit **2054/2054** (+36: saveToBoard 18/saveFromSource 6/library-save route 7/librarySearch +5) · build 0 · e2e **137 passed, 0 failed** (+7 phase4b; bu koşuda retry/flake tetiklenmedi — bilinen faz1d first-nav flake'i "yok" İDDİA EDİLMEZ). Kanıt `shots/cemos-rebuild/phase4b-unified-library/` (01/02/05/06 REAL-DB-READONLY = gerçek Neon, GET-only, save TIKLANMADI [01: Tümü 4234 gerçek içerik; 02: içerik drawer "Kaydet"]; 03/04/07/08 HARNESS route-mock — dosya adında açık [03: İlham rail 1920; 04: pano seç/oluştur menüsü]); 1024/1280/1440/1920 taşma 0, app console error 0 (yalnız Neon cold-start /api/health 500 "Şimdi: Durum alınamadı" — 4B dışı). **Yapılmadı:** Phase 4C/4D, canlı AI ($0), gerçek dış çağrı (Meta business_discovery zaten çalışıyor), production'a save/capture/fixture (hepsi hermetic/read-only), push, deploy. Legacy semboller (`useXAgentStore`/"xagent-store"/`XAgentApp.tsx`/User-Agent) korundu. Sonraki: **Phase 4C** (Learn intake → `learning/{pipeline,scheduling,prompts,reviewService}` + `components/learn/*` + `/api/learn/jobs/[id]/advance` durum makinesi).

- **Faz: PHASE 4A — CAPABILITY DISTRIBUTION + INFORMATION DENSITY TAMAMLANDI (ADR-040; 2026-07-19).** Kullanıcı kararı: uygulama fazla boş/çıplak → kabiliyetleri görünür/dengeli/operasyonel dağıt. **SIFIR migration** (yalnız UI/IA). **Teslim:** (1) `Sidebar.tsx` yeniden dengelendi — 3 TOP-LEVEL (Bugün/Plan/Kütüphane) korunur; aktif alan sidebar'da alt hedeflerini AÇAR (Plan→Takvim/Fırsatlar/Seriler · Kütüphane→Tümü/İlham/Öğrenme); **hiyerarşik "Araştırma" grubu** (Haberler/YouTube/Viral Radar/Keşif/X Kaynakları — katlanabilir, masaüstünde keşfedilebilir; artık yalnız Fırsatlar/Cmd+K arkasında değil); "Araçlar"(Toolbox); Profil. (2) `SidebarNowModule.tsx` "Şimdi" özeti — Profil üstünde, YALNIZ canonical `SystemHealthProvider` (TEK fetch reuse; ikinci readiness sözlüğü YOK; sahte sayı YOK): yayına hazır/karar bekleyen/aktif plan hazır-toplam/en yüksek sistem sinyali. (3) `navConfig` ADDITIVE `researchNavItems()` SUNUM katmanı — sınıflandırma DEĞİŞMEDİ (advanced hâlâ hiçbir alana çözülmez; drift-guard sağlam); `PHASE-4-CAPABILITY-MAP.md` (orphan denetimi: YOK). (4) `TakvimTab` ay hücreleri artık gerçek içerik (readiness renk noktası + konu/başlık; dossier'sız planlı slot dürüst nötr — uydurma yok; `aria-label` `${gün} — ${sayı} öğe` DEĞİŞMEDEN, e2e kontratı). (5) `SystemTab` tek uzun kart → dashboard grid (Altyapı full-width + 5 sözleşme responsive grid). (6) `--content-wide` 1280→1440 (operasyonel yüzeyler + Sistem 1440/1920'yi daha iyi kullanır). **Gate:** typecheck 0 · lint 0 err/5 pre-existing warn · catalog OK · unit **2018/2018** (+3 navConfig) · build 0 · e2e **129 passed + 1 pre-existing faz1d first-nav flake (retry-green), 0 failed** (+6 phase4a). İlk e2e koşusu 7 failure verdi (SidebarNowModule kısmi-health crash + 2 shell-smoke ADR-040 assertion) → düzeltildi, yeniden koşu temiz. Canlı (gerçek Neon, dev 3007, READ-ONLY): 1024/1280/1440/1920 taşma 0; Şimdi gerçek veri (Yayına hazır 2·Karar bekleyen 2·Taslak plan 0/10·Sistem sinyali); Takvim hücreleri gerçek slot konuları; Sistem dashboard grid; Araştırma toggle aria-expanded çalışır; uygulama console error 0 (yalnız dış Kaspersky + Neon cold-start ilk-yük 500). Kanıt `shots/cemos-rebuild/phase4a-density/` (REAL-DB-READONLY etiketli). **Yapılmadı:** Phase 4B/C/D, canlı OpenRouter ($0), gerçek dış çağrı, production'a fixture, push, deploy. Legacy semboller korundu.

- **Faz: PHASE 3E — PRODUCTION CONTRACT COMPLETE / TAM CANLI DEĞİL (ADR-039; 2026-07-19).** Aylık Reels planı + güvenli takvim yerleşimi + uçtan uca plan sağlığı; **SIFIR yeni migration** (ReelPlan.status/mixJson/notesJson/updatedAt, ReelPlanSlot.status/updatedAt/dossierId/seriesKey, OpportunityHandoff.resultRef, WebsiteVerification, ReelDossier, PipelineTrace yeterli). **Teslim:** derinleştirilmiş SAF assembler (ayın gerçek gün sayısı doğrulaması → Şubat 30/31 reddi; yeni `repetition.ts` çok boyutlu histogram pillar/seriesKey/normalized-topic/canonical-tool-URL/hook-shape üzerinden yeni+geçmiş sinyalleri — `normalizeTurkish`/`isNearDuplicate` REUSE; korunan-gün dışlama + sinyalleri histograma; `bannedRepetition` exact=hard-block, near+pastTopics=uyarı; seri görünen ad ham slug değil) · preview→apply→activate (`POST /api/reels/plan/preview` SIFIR-write diff+fingerprint+expectedUpdatedAt; `POST .../apply` fingerprint bütünlüğü + optimistic concurrency + `pg_advisory_xact_lock("reel_plan:<acc>:<month>")` tek-tx + idempotent no-op + **NON-DESTRUCTIVE reconcile** `reconcileSlots` — obsolete planned→skipped, korunanlar[drafted/done/dossier/handoff-resultRef] asla silinmez → **handoff slot ORPHAN olmaz**, eski destructive `deleteMany` KALDIRILDI; `notesJson`=versioned `plan_notes.v1` zarf, legacy string[] okunur; `resolveSeriesForPlan` DB-truth ownership+aktiflik+format+name+pastTopics+banned; `POST .../lifecycle` draft↔active↔archived server state machine — apply otomatik active YAPMAZ, aktivasyon açık+ack; legacy POST reconcile'a delege) · handoff/slot güvenliği (`consume-plan`: ay-gün doğrulama[invalid_day]+archived reddi+boş-slot reuse+`day_occupied`+`pg_advisory_xact_lock("plan_day:")` tek-tx, resultRef orphan olmaz; yeni `move`[dossier korunur, done immutable, day_occupied, ay-gün]/`skip`[non-destructive, done immutable, idempotent]/`restore`[skipped→planned/drafted] — optimistic concurrency+atomic claim; dossier/handoff silinmez; done="operatör tamamladı", IG publish iddiası değil) · tek-kaynak `deriveInstagramPlanHealth` (Phase 3D `computeDossierProductionState` REUSE — ikinci readiness sözlüğü YOK; plan yokluğu≠infra error, draft/archived nötr, stale≠outage, production_ready=tüm 3D kapıları, veri yok→unknown) + `planHealthService` (aktif/en-güncel plan, bağlı dossier production'ı `getDossierProductionState`'ten, canlı collision, fail-soft) · `/api/health` additive `contracts.instagramPlanning` (üç altyapı sözleşmesi korunur; `SystemHealthProvider` tek fetch; `deriveTopbar` priority infra>pipeline>bugün>aktif-plan-bugün/geride>aktif-plan-yaklaşan>yok; not_configured/draft/archived topbar üretmez) · UI: `PlanBuilder`(önizle→uygula[fingerprint+expectedUpdatedAt, hard-blocker disabled]→AYRI aktive/arşivle/geri-al+ack; DB seri adı; window.prompt YOK)/`PlanHealthStrip`(canonical instagramPlanning, sahte KPI yok, ay-uyuşmazlığı notu)/`SlotOpsBar`(taşı/atla/geri-al) TakvimTab'de; SystemTab "Instagram içerik planı" bölümü (AYRI ürün/görev sözleşmesi vurgulu); Instagram filtre metni düzeltildi (business_discovery OKUMA çalışıyor vs IG YAYIN/ZAMANLAMA ayrı write, CemOS içinden desteklenmiyor; Reels planı üretim planı, IG yayını değil). Yeni route'lar route-guards matrisinde (+6: preview/apply/lifecycle/move/skip/restore). **Gate:** typecheck 0 · lint 0 err (5 pre-existing warn — hepsi Phase3E DIŞI: coverage/scripts/ViralLibraryTab) · catalog OK (9 preset canlı) · unit **2015/2015** (202 dosya; +107) · build 0 · e2e **124 passed (+5 phase3e; bilinen faz1d first-nav flake bu koşuda tetiklenmedi — "flakesiz" iddia edilmiyor)**. Canlı (gerçek Neon, dev 3050, READ-ONLY): Takvim/Sistem 1024/1280/1440/1920 taşma 0, plan sağlık şeridi + Instagram plan bölümü canonical health'ten render; gerçek DB'de 1 draft ReelPlan (10 planned slot, 0 dossier) → slot drawer dürüst "dossier bekliyor" (sahte içerik yok). Kanıt `shots/cemos-rebuild/phase3e-production/` (REAL-DB-READONLY vs HARNESS-ROUTE-MOCK dosya adında açık). Başlangıç read-only snapshot: QueueItem 64/ContentItem 3492/WebsiteVerification 0/ReelDossier 0/ReelPlan 1/ReelPlanSlot 10 (0 dossier)/Account 2/SeriesProfile 8/TrainingExample 181/PipelineTrace 110 (3D-sonuna göre TrainingExample +4, PipelineTrace +14 = oturumlar-arası aktivite, bu oturumun yazımı DEĞİL). **Yapılmadı:** Phase 4, canlı OpenRouter ($0), gerçek dış site/Meta çağrısı (hepsi hermetic mock), production'a plan/slot/dossier/verification/fixture yazımı, canlı re-verification/apply mutasyonu (browser read-only), push, deploy.

- **Faz: PHASE 3D — PRODUCTION CONTRACT COMPLETE / CANLI AI DOSSIER ÜRETİMİ BLOCKED-EXTERNAL (ADR-038; 2026-07-18).** Doğrulanmış site/araç → production-grade dossier ↔ Takvim; **SIFIR yeni migration** (WebsiteVerification/ReelDossier.verificationId+verificationEvidenceJson+alternativesJson+expiry/ReelPlanSlot.dossierId/PipelineTrace yeterli). **Teslim:** `verifyWebsite` sertleşti — TYPED failure kodları (`invalid_url|ssrf_blocked|redirect_blocked|redirect_hop_limit|robots_disallowed|timeout|unreachable|persistence_failed|unknown_failure`; ham exception/iç host sızmaz), bounded robots okuma, **fail-soft persist KALKTI** (persist başarısız → `persistence_failed`; yalnız bellekteki kanıt named-tool'u ready YAPAMAZ — `computeReadiness` artık `verificationId` ister, tüm çağıranlar güncellendi) · `verifyWebsiteWithReuse` append-only snapshot defteri (≤24h taze strict-parse reuse; force-refresh ayrı; bozuk/bayat fail-closed) · `POST /api/reels/dossier/[id]/verify` re-verify lifecycle (server-side URL — fetch-proxy DEĞİL; LLM'siz, gate'siz; ağ tx dışında + advisory-lock'lu kısa tx + in-tx expectedUpdatedAt → yarış eskiyi ezemez; başarı atomic snapshot+dossier update; başarısızlık HİÇBİR alanı yazmaz + bounded/redacted `reel_dossier_reverify` PipelineTrace izi) · `computeDossierProductionState` SAF tek-kaynak read model (6 katman: generation/primary evidence/alternatives/creative/approval/calendar; primary evidence YALNIZ gerçek WebsiteVerification satırından — kopya JSON değil; url_mismatch/parse-fail/satır-yok fail-closed; production_ready = kanıt+creative+onay+seri+TAM BİR slot; `GET /api/reels/dossier/[id]` `production` bloğu döner, UI aynı modeli render eder) · alternatif zinciri (`alternativesJson` versioned strict zarf ≤4 aktif; her alternatif KENDİ verificationId'siyle doğrulanır, primary kanıt kopyalanamaz, manuel verified yok, archive non-destructive, sessiz promotion yok — "bu araçla yeni dossier" prefill'i ADR-036 kapılı; `POST .../alternatives` add/archive) · Takvim attach sözleşmesi derinleşti (MEVCUT dossier seçimi; provenance seriesKey eşleşmesi — string tahmini değil; `pg_advisory_xact_lock` ile aynı dossier ikinci aktif slota BAĞLANAMAZ; yanıt `productionReady+blockers` — "bağlandı" ≠ "yayına hazır", `drafted` = yalnız bağlandı) + `POST .../detach` (expectedDossierId fail-closed, done çözülmez, dossier silinmez, idempotent) · UI: `DossierProductionPanel` (checklist + kanıt kartı [girilen/final URL, redirect, checkedAt, expiry, taze/bayat/başarısız/eksik] + dürüst unknown [451'siz TR erişimi İDDİA EDİLMEZ, "sayfa render edilmedi" notu] + Yeniden doğrula + alternatif yönetimi + gate-blocked yalnız ENV adları) TakvimDossierPanels + CarouselStudio'da; Tier-2 EKLENMEDİ (güvenli operator-worker sözleşmesi yok — unknown kalır, sahte tamamlanmışlık yok). Yeni route'lar route-guards matrisinde (+3). **Gate:** typecheck 0 · lint 0 err (5 pre-existing warn) · catalog OK · unit **1908/1908** (192 dosya; +68) · build 0 · e2e **118 passed + 1 pre-existing faz1d flake retry-green (119; +10 phase3d)**. Canlı (gerçek Neon, dev 3050, READ-ONLY): Takvim/Seriler 1024/1280/1440/1920 taşma 0, temiz yüklemede console 0 (ilk yüklemede settings+handoff 500 = bilinen Neon cold-start); gerçek slot drawer'ı dürüst "dossier yok" (ReelDossier=0 → sahte seçici yok). Kanıt `shots/cemos-rebuild/phase3d-production/` (01–04 REAL-DB-READONLY; 05–08 HARNESS-ROUTE-MOCK — dosya adında açık). Başlangıç/son read-only snapshot: tüm sayaçlar eşit (WebsiteVerification 0, ReelDossier 0, ReelPlanSlot 10/0 bağlı, …) TEK fark PipelineTrace 83→96 = mevcut Fırsatlar okuma yolunun deterministik $0 `opportunity_curation` izleri (e2e faz1d gerçek spec'i + read-only browsing; Phase 3D yazımı DEĞİL, fixture DEĞİL). **Yapılmadı:** Phase 3E, canlı OpenRouter dossier üretimi (rotation+INSTAGRAM_GENERATION_* yok — $0; OPENROUTER_API_KEY env'de görünse de rotation kanıtı yok → kapı bilinçli kapalı), gerçek dış site doğrulaması (production'da dossier yok — hermetic mock), production'a fixture, push, deploy.

- **Faz: PHASE 3C — PRODUCTION CONTRACT COMPLETE / CANLI AI UYARLAMASI BLOCKED-EXTERNAL (ADR-037; 2026-07-18).** Rakip/ilham kütüphanesi + dürüst deterministik yapısal analiz; **SIFIR yeni migration** (BoardItem.metaJson = versioned Zod ilham zarfı; IgWatchAccount.probeStatus String kolonu yeni `config_required` değerini DDL'siz taşır). **Teslim:** `src/lib/inspiration/` — `canonicalizeInstagramUrl` (saf; IG host + reel/reels/p/tv + `/{user}/…`; `share/` dürüst red; fetch/scraping YOK; externalId `shortcode_*`/`urlhash_*`) · `captureInspiration` (aktif hesap zorunlu; cross-account VE paylaşılan-board fail-closed; ContentItem+BoardItem TEK transaction + advisory-lock idempotency; default "Instagram İlham" panosu idempotent; manuel metrik YALNIZ zarfda `operator_observed`+observedAt — ContentItem.metricsJson'a yazılmaz; `recomputeBaseline` artık sourceType="external" filtreli) · `analyzeInspirationStructure` (SAF/LLM'siz/ücretsiz; `inspiration_structure.v1`; gözlenen-gerçek vs hipotez; "neden çalıştı" YALNIZ provider outlier kanıtıyla [insufficient=false + ≥1.5×], aksi "neden çalışabilir"; caption yoksa caption yapısı uydurulmaz; media URL ≠ piksel analizi; untrusted kaynak ≤80 karakter kanıt parçası dışında yeniden üretilmez) + `analyzeService` persist (sahiplik fail-closed; versioned çıktı zarfta KALICI) · `syncIgCompetitors` İKİ AŞAMALI (upsert→GÜNCEL baseline→TAZE skor; insufficient → multiplier=0+flag, API'de null; explanationJson window/sample/provenance/formula/computedAt; config yoksa `skipped=meta_config_required` fail-fast) · `addWatchAccount` handle validasyonu + dürüst `config_required` (private yalanı kalktı) · `reverseEngineerToIdea` sertleşti (Instagram→ADR-036 kapısı zorunlu: kapalıyken SIFIR ağ/Idea/UsageLog typed blocked; çıktı runtime Zod — geçersiz çıktı Idea YAZMAZ; 24s idempotency — retry duplicate maliyet üretmez) · route'lar `GET /api/inspiration` + `POST /api/inspiration/capture|analyze` (guard+bounded+Zod+typed kodlar) + önceden guard'sız `GET /api/boards`, `GET /api/boards/[id]`, `GET /api/content` guard'landı (hepsi route-guards matrisinde) · UI: `LibIlhamTab` → `src/components/library/ilham/` (HeaderBar hesap Select + pano pill + pano drawer [window.prompt KALKTI] · CaptureDrawer tek atomik POST · InspirationGrid arama/format/provenance/analiz filtreleri · InspirationDetailDrawer gözlenen-vs-hipotez + "çalıştı/çalışabilir" ayrımı + metrik provenance + AYRI ücretli "Fikre dönüştür" + kapı-kapalı yalnız ENV adları + Instagram fikri Seriler/Takvim handoff'una [X/Bugün'e AKMAZ] · CompetitorSummary "CemOS ortak rakip listesi" + config-required/never-synced/stale + insufficient çarpan gösterilmez). **KANIT DÜZELTMESİ:** META_ACCESS_TOKEN+META_IG_USER_ID env'de MEVCUT; watchlist 24/24 ok, lastSyncAt 2026-07-18T06:39Z (prod cron BUGÜN sync'lemiş) → **Meta rakip senkronu ÇALIŞIYOR, blocked değil** (own-account 3A binding'i ayrı ve hâlâ 0). **Gate:** typecheck 0 · lint 0 err (5 pre-existing warn) · catalog OK · unit **1840/1840** (187 dosya; +66) · build 0 · e2e **108 passed + 1 pre-existing faz1d flake retry-green (109; +7 phase3c)**. Canlı (gerçek Neon, dev 3050, READ-ONLY): İlham yüzeyi 1024/1280/1440/1920 taşma 0, temiz yüklemede console 0 (ilk yüklemede 1 kez /api/settings 500 = bilinen Neon cold-start); pano yok dürüst empty; outlier feed gerçek prod verisiyle dolu. Kanıt `shots/cemos-rebuild/phase3c-production/` (01–04 GERÇEK DB; 05–08 HARNESS route-mock — dosya adında açık). Başlangıç/son read-only snapshot BİREBİR eşit (Board 0/BoardItem 0/ContentItem 3492/outlier 1017/…) — oturumdan SIFIR DB yazımı, fixture yazılmadı. **Yapılmadı:** Phase 3D–3E, canlı OpenRouter/AI uyarlaması (rotation+INSTAGRAM_GENERATION_* yok — $0), elle canlı Meta çağrısı (kanıt DB'den), Composio own-account aktivasyonu, push, deploy.

- **Faz: PHASE 3B — PRODUCTION CONTRACT COMPLETE / LIVE GENERATION BLOCKED-EXTERNAL (ADR-036; 2026-07-18).** Onaylı DNA → carousel/Reels üretimi + edit + insan onayı; **SIFIR yeni migration** (rotation doğrulanmadan production DDL yasak → provenance PipelineTrace "provenance" stage'inde, onaylanmışlık TrainingExample.metricsJson dossierId containment'ından TÜRETİLİR, concurrency mevcut updatedAt, idempotent onay pg_advisory_xact_lock transaction'ı). **Teslim:** `productGates.getInstagramGenerationGate` (eval kapılarından AYRI 4 koşul; kapalı=SIFIR OpenRouter/verifyWebsite/yazım; .env.example default kapalı) · `generateCarouselEpisode` (buildCarouselPrompt + strict Zod'suz persist yok + slide/hashtag/banned/past-topic/kanıtsız-iddia deterministik doğrulama + near_copy FAIL-CLOSED + kanıt LLM'den önce + 24s konu-dedup=handoff-retry-duplicate-yok) · `reelDossierFor` SERTLEŞTİRİLDİ (typed outcome; VOICE hardcoded DEĞİL — DB hesap + getRuntimeProfile(requireGenerationReady); 4 aşama strict Zod; başarısız aşama = YARIM DOSSIER YAZILMAZ [bilinçli sözleşme değişikliği — eski fail-open partial kalktı; ödenen maliyet UsageLog+trace'te dürüst]; per-pass tavan + aşama-öncesi kalan bütçe; GET listesi hesap-scoped) · `creativeReadiness` (ÜÇ AYRI kavram: üretim/site-kanıtı/insan onayı; kanıtsız iddia + kanıtsız araç blocked) · `dossierReviewService` (PATCH expectedUpdatedAt→409, verification düzenlenemez, server-canonical, onayda sahiplik+sürüm+readiness+kanıt+seri promptVersion YENİDEN kontrol; advisory-lock ile concurrent çift onayda TEK TrainingExample — platform=instagram, seriesKey ARTIK DOLU [CreateTrainingExampleSchema+repo genişledi], label edited/positive provenance-hash tespitiyle; pastTopics version-bump'sız; FeedbackEvent best-effort editDistance=null dürüst; embed fail-open) · route'lar: `POST /api/instagram/content/generate`, `GET/PATCH /api/reels/dossier/[id]`, `POST .../approve`, `POST /api/reels/plan/slot/[id]/attach` (atomik claim + idempotent + cross-account fail-closed — ReelPlanSlot.dossierId İLK KEZ yazılıyor) — hepsi guard matrisinde · UI: Seriler'e `CarouselStudio` (aday-konu prefill "Bölüm üret", üç-durum rozetli review drawer, slide editörü, Kaydet/Onayla, kapı-kapalı dürüst blocked yalnız ENV adları), Takvim'e `SlotDossierActions` ("Dossier hazırla" + AYRI "Slota bağla") + `DossierDetailPanel` (TakvimDossierPanels.tsx — 800 satır bütçesi korundu). **Canlı smoke YAPILMADI:** rotation + INSTAGRAM_GENERATION_* env'leri yok → SIFIR OpenRouter/site çağrısı, $0.00. **Yapılmadı:** Phase 3C–3E, IG/X publish/write, gerçek TrainingExample/DNA mutasyonu (onay yalnız UI'daki açık insan eylemiyle), production_old silme, push, deploy.

- **Faz: PHASE 3A — PRODUCTION CONTRACT COMPLETE / LIVE INSTAGRAM ACTIVATION BLOCKED-EXTERNAL (ADR-035; 2026-07-17).** Plan: `08-PHASE3-PLAN.md` (3A–3E; yalnız 3A uygulandı — "Phase 3 tamamlandı" İDDİA EDİLMEDİ). **DB güvenlik korkuluğu (olay sonrası):** `src/lib/db/urlSafety.ts` fail-closed sınıflandırıcı + `assertSafeShadowUrl`/`assertSafeDbPushTarget` + destructive-SQL statik tarayıcı; `npm run db:push` guard'lı (production-benzeri hedefe CANLI KANITLA reddetti, exit 2, yalnız sanitize fingerprint); `npm run db:migrate`(+`:scan`) = elle-SQL güvenli deploy workflow'u (öncesi/sonrası snapshot + ikinci-deploy idempotency + satır-kaybı kontrolü); prosedür+runbook `DB-SAFETY.md`; 6 mevcut migration additive tarandı. **Teslim:** `resolveSingleInstagramBinding` single-IG contract (0 binding→config_required; >1 farklı hesap→multi_binding_blocked fail-closed; composio>meta tercihi; pasif hesap→account_unresolved) · `instagramDnaObservationService` saf/LLM'siz/SIFIR-yazma gözlem read model'i (IgMedia ≤100 dedup'lu corpus; hashtag bloğu gövdeden ayrık; hook/CTA/format/uzunluk/paragraf/emoji/hashtag istatistikleri; MIN_EVIDENCE=10 sufficiency; performans kanıtı dürüst üçlü — insights_available/engagement_partial("virallik kanıtı değil")/unavailable("sıfır değil, YOK")) · apply governance (`dnaApplyService` + `POST /api/instagram/dna-observation/apply`): değerler HER ZAMAN sunucu-türetimli, selected-fields-only, boş seçim no-op, 422 yetersiz kanıt, 409 stale (değerler eşleşiyorsa idempotent alreadyApplied — duplicate version YOK), cross-account 422; CaptionDna apply provenance=operator (dnaDistill artık ezemez); SeriesProfile yalnız seri hashtag override (version+promptVersion bump) · `GET /api/instagram/dna-observation` gözlem/öneri/ONAYLI-DNA ayrı bloklar; iki route guard'lı+256KB+Zod+route-guards matrisinde · Seriler UI `InstagramDnaSection` (yeni ekran/nav YOK): tüm durumlar (config_required/multi_binding/no_media/insufficient-apply-kapalı/stale/ready/conflict), alan-bazlı apply drawer (otomatik seçim yok, önce/sonra önizleme, sürüm görünür) · grounding precedence testleri (seri override prompt'a girer; hesap HashtagDna'ya sessiz fallback YOK; gözlem tip düzeyinde prompt'a giremez; identity bloğu handle-izole). **DB:** YENİ MİGRATION YOK (gözlem read model; apply mevcut kolonlara) — migrate status 6 migration up-to-date; başlangıç/son read-only snapshot eşit (yalnız arka plan cron artışları). **Baseline farkı işlendi:** EvalTest DB toplamı **65** (direktifteki "55" golden-builder set sayısıdır; restore edilen 61 legacy satır + 4 yeni thread_contract seed = 65 — veri kaybı yok). **BLOCKED-EXTERNAL:** `COMPOSIO_INSTAGRAM_CONNECTED_ACCOUNT_ID`+`_ACCOUNT_HANDLE` yok + binding 0 satır → canlı Composio discovery/smoke/sync ÇALIŞTIRILMADI (sıfır IG çağrısı); OpenRouter rotasyon marker'ı hâlâ yok → Phase 2 canlı smoke de bekliyor; Neon DB parolası rotasyonu kullanıcı aksiyonu. **Yapılmadı:** Phase 3B–3E, rakip scraping, IG/X write, canlı ücretli LLM (SIFIR), otomatik identity promotion, push, deploy.

- **Faz: PHASE 2E — PRODUCTION CONTRACT COMPLETE / LIVE BLOCKED-EXTERNAL (ADR-034; 2026-07-17).** SONUÇ B: production kod + DB + deterministik eval + UI + cron + testler TAMAM; canlı bölüm (curator smoke + thread smoke + canlı golden generate) OPENROUTER_KEY_ROTATED_AT olmadığı için ÇALIŞTIRILMADI — "Phase 2 tam canlı doğrulandı" İLAN EDİLMEDİ. **Güvenlik olayı + kurtarma:** pass başında `prisma migrate diff --shadow-database-url`'e production URL yanlışlıkla geçti → Neon production SIFIRLANDI; kullanıcı onayıyla Neon PITR (Preview data 09:15 UTC doğrulaması → restore) ile TAM kurtarıldı (62 QueueItem / 12 segmentli thread / 1628 NewsItem / 3718 UsageLog / 5 migration read-only doğrulandı; boş durum `production_old_2026-07-17T09:15:00Z` yedek branch'inde). KURAL: shadow-database-url'e asla gerçek URL verilmez; migration'lar elle SQL + migrate deploy. **Teslim:** additive `EvalRun`+`EvalCaseResult` + `PipelineTrace(pipelineId,createdAt)` indeksi (migration `20260717100000_add_eval_run_history`, migrate deploy Neon + ikinci deploy "No pending" idempotent) · fixture contract katmanı (16 fixture / 13 enabled agent; expectedOutcomes+deterministic+liveAllowlisted+assertOutput; canlı allowlist YALNIZ opportunity-curator + content-creator) · hermetic registry contract runner + CLI (`scripts/run-registry-eval.ts`; ilk gerçek koşu 16/16 PASS $0, runId `cmrot3gv90000qeus2lt1lv75`; 2 case'te Neon pool timeout trace yazımını düşürdü → traceStatus=failed + `trace_write_failed` structured log DÜRÜSTÇE gözlendi) · executor `traceStatus`(persisted|failed|skipped_policy) + process-local lostTraceCount'un global KPI OLMADIĞI dokümante ("gözlenen trace kapsaması" dili; son N koşudan persisted/(persisted+failed)) · golden runner modernizasyonu (hardcoded hesap listesi KALDIRILDI → testlerin accountId'leri + getRuntimeProfile; canlı generate YALNIZ --live+kapılar, aksi BLOCKED; EvalRun/EvalCaseResult kaydı; thread_contract deterministik golden vakaları — seed idempotent 4 yeni/51 mevcut, 55 toplam; ilk koşu 4/4 PASS $0, runId `cmrot32ho0000qe30fow9wvlj`) · curator production wiring (`POST /api/opportunities/curate` guard'lı+bounded; çift kapı ENABLE_AGENT_CURATION + OPENROUTER_KEY_ROTATED_AT; method agent|deterministic DÜRÜST; client fallback; curationMethod fırsattan handoff'a; "agent seçimi" rozeti yalnız gerçek başarıda) · canonical `OperatorActionReadiness` (gate SystemHealthProvider'ın TEK fetch'inden; 15sn polling + duplicate sorgular KALKTI; `GET /api/settings/operator-readiness` KALDIRILDI — tek tüketici gate'ti; scan-now + servis motor olarak yaşıyor; çelişkili status imkânsız) · learn cron'a haftalık Pazartesi deterministik registry eval (yeni Vercel cron YOK; idempotent hafta kontrolü; fail-open; canlı eval cron'dan DEFAULT ÇALIŞMAZ) · Sistem "Agent değerlendirmeleri" bölümü (hiç-koşmadı/blocked-external/stale/partial/passed; eval stale ana outage gibi SUNULMAZ) + Costs evaluation bütçe/harcama + son koşu + kürasyon harcaması AYRI kalem · gated liveSmoke (dört kapı + katalog + bütçe preflight; kapılar eksik → OpenRouter çağrısı OLMADAN blocked_external EvalRun; thread smoke NO-PERSIST sözleşmesi typed) · `.env.example` Faz 2E kapıları + stale model slug düzeltmesi (deepseek-chat:free/claude-sonnet-4.5 → deepseek-v4-flash/claude-sonnet-5; canlı env'e DOKUNULMADI). **BLOCKED-EXTERNAL:** OpenRouter key rotasyonu (kullanıcı OPENROUTER_KEY_ROTATED_AT set edecek) → canlı curator/thread/golden-generate smoke; Composio `COMPOSIO_INSTAGRAM_CONNECTED_ACCOUNT_ID`+`_ACCOUNT_HANDLE` yok → canlı IG smoke (2C sözleşmesi hazır); Neon DB parolası rotasyonu (URL terminale yazıldı) kullanıcı aksiyonu. **Yapılmadı:** canlı ücretli LLM çağrısı (SIFIR), X API publish, Phase 3–4, push, deploy.
- **Faz: PHASE 2D ✅ KAPANDI (ADR-033; 2026-07-16).** Production contract complete; **kalibrasyon insufficient_sample → skor eşikleri PROVISIONAL** (dürüst kapanış — "kalibrasyon tamamlandı" İDDİA EDİLMEDİ); canlı ücretli model thread çıktısı TEST EDİLMEDİ (2E). **Kod-öncesi Neon audit'i (`scripts/audit-thread-readiness.ts`, read-only):** 13 thread taslağının TAMAMI mode=thread+draftType=TWEET+segmentsiz; 12'si yapısız halde yanlış "ready" görünüyordu; etiketli örnek 0 pozitif/0 negatif. **Teslim:** canonical thread sözleşmesi (`threadSegments` = publication authority; `effectiveThreadSegmentLimit=min(280,maxChars)` TEK primitive — UI `SEGMENT_MAX` literali kalktı; `isThreadDraft(draftType, mode)` mode-farkındalı) · writer strict json_schema + typed `threadSegments` + açık format niyeti (thread zorunlu/tweet kilidi/auto; repair sonrası karşılanamayan thread = judge'a harcamasız typed blocked, QueueItem YOK; sahte mock/tek-segment thread yasak) · judge `sourceDraftIndex` provenance (canonical içerik/segment WRITER adayından; reorder/fast/deadline/empty yolları segment korur; duplicate indeks imkânsız) · final editor thread'de `skipped:thread_segment_contract` · tek-create THREAD persistence (canonical content=join(segments); fitToMaxChars thread'e uygulanmaz; segment-bazlı deterministik lint, segment başına LLM lint yok; telemetry formatIntent/threadSelection/segmentCount) · entry-point parity (Source.mode/suggestedFormat=thread → gerçek thread isteği merkezî draftService'te; handoff explicit-TWEET kilidi; atomize "thread" rolü yalnız gerçek segmentli adaya) · readiness policy **1.1.0-provisional** (segment-bazlı denetim, thread metni canonical segmentlerden → stale content bypass kapalı; ≥2 segment; bilinmeyen hesap grafikcem'e düşmez, emoji yalnız exact handle) · PATCH atomik segment+editedContent senkronu (boş segment 400) · dürüst intent (thread=YALNIZ ilk segment, `intentMode=thread_first_segment`; kalan segmentler kopyalanabilir; "Paylaşıldı" bütün zincir için manuel beyan; hash=segment sırası+değeri → edit/reorder=stale/content_changed; `UsageLog.tweetCount=segmentCount`; PublishLog manualThread) · schedule/approve thread parity (`readiness_not_ready` fail-closed; birleşik uzunluk reddi yok) · **deterministik backfill Neon'a UYGULANDI:** dry-run 13 aday → apply 12 mutasyon (11 blank_blocks + 1 numbered; 12 TWEET→THREAD yükseltme) + 1 sınır-aşımı dürüst untouched; **ikinci dry-run 0 mutasyon (idempotent)**; status/content mutate edilmedi, içerik loglanmadı. **Gate:** typecheck 0 · lint 0 err (5 pre-existing warn) · catalog OK · unit **1614/1614** (164 dosya; +77) · build 0 · e2e **78 passed + 1 pre-existing flaky retry-green (79; +7 thread)** · migrate status up-to-date (5 migration; **2D'de şema değişikliği YOK** — yeni kolon eklenmedi, ADR-010 korunur). Canlı (gerçek Neon, dev 3050): Bugün bugünkü kuyruk dürüst boş; read-only API doğrulaması 60 item / 12 THREAD / 12'si geçerli segmentli; console 0; 1024/1440/1920 taşma 0. Kanıt `shots/cemos-rebuild/phase2d-thread/` (01/02/07 gerçek; 03/04/05/06 HARNESS — açık). **Yapılmadı:** 2E, Phase 3–4, gerçek X publish (tek/multi-post), canlı ücretli LLM (OpenRouter key rotasyon bekliyor — önceki pass'te kısmî sızıntı), Composio canlı smoke (kapsam dışı; key durumu için 2E), push, deploy.
- **Faz: PHASE 2C ✅ KAPANDI (ADR-031 + ADR-032; 2026-07-16).** 10 yeni commit (9 iş + 1 sayı-düzeltme; git geçmişiyle doğrulandı): `df7ff5d`(db: Account profil kolonları + AccountPlatformBinding, migration `20260716210000` saf additive, migrate deploy Neon + seed backfill — iki hesap profileStatus=active doğrulandı read-only) · `65fbeca`(profileRepository: DB runtime source-of-truth, Zod RuntimeAccountProfile, fail-closed isKnownAccountHandleDb/üretim-hazırlık/resolveCronHandles, bootstrap=seed/fixture/işaretli-degraded-fallback) · `ec59598`(tüketici migrasyonu: memory scope + feedback + registry + route doğrulamaları + cron/operator listeleri DB-otoriteli; pipeline/draft üretim girişleri DB profili; heuristik bootstrap Record'ları graceful — başka hesabın personasına düşüş YOK; Sidebar switcher /api/settings'ten, Channel=string) · `1b4acb8`(composio: server-only MCP client `connect.composio.dev/mcp`, JSON-RPC+SSE, retry/timeout/2MB/redaction; çift-katman read-only allowlist + deny-verb + canlı discovery fail-closed; DM tool'ları bilinçli DENY) · `bc3d019`(InstagramReadProvider soyutlaması: Composio birincil + mevcut Meta fallback SARILDI silinmedi; auto/composio/meta seçimi — explicit composio'da sessiz fallback YOK) · `0048699`(bridge sync: binding fail-closed + idempotent IgMedia/IgComment/IgInsightSnapshot upsert + ContentItem köprüsü + AccountPlatformBinding persist; daily cron aşaması + manuel POST /api/instagram/sync; /api/integrations composio girdisi) · `f951de0`(Entegrasyonlar UI: connected/degraded/blocked/config-gerekli + salt-okuma + fallback rozeti + manuel sync) · `0201de1`(7 hermetik e2e) · +test/docs. **Gate:** typecheck 0 · lint 0 err (5 pre-existing warn) · catalog OK · unit **1537/1537** (159 dosya) · build 0 · e2e **72/72** · migrate status up-to-date. Canlı (gerçek Neon, dev 3050): Entegrasyonlar composio kartı dürüst "yapılandırma gerekli", switcher DB'den 2 hesap, console 0, 1024–1920 taşma 0. Kanıt `shots/cemos-rebuild/phase2c-composio/` (01/02/05/06 gerçek; 03/04 HARNESS — açık). **BLOCKED-EXTERNAL:** `COMPOSIO_CONSUMER_API_KEY` güvenli hiçbir kaynakta yok → Claude Code MCP kaydı + canlı Composio discovery/smoke/gerçek sync yapılamadı (kod+test tam; slug'lar toolkit 20260708_00 dokümantasyonundan); rakip business_discovery Composio'da YOK — Meta izni blocker sürüyor. **Yapılmadı:** 2D/2E, Phase 3–4, gerçek X publish, canlı ücretli LLM, push, deploy.
- **Faz: PHASE 2B ✅ KAPANDI (ADR-029 + ADR-030; 2026-07-16).** 11 yeni commit (git geçmişiyle doğrulandı; önceki "12" yazım hatasıydı): `6cd20c2`(handoff düzeltme: 2A=7 commit + 2B haritası) · `48209fe`(MemoryEvidence ledger + canonicalKey, migration `20260716120000` additive, Neon'da) · `10f1406`(governance: OTOMATİK AKTİFLEŞME KALDIRILDI — learned fact 3 kanıtta bile yalnız review-ready; approve server-side fail-closed; atomik approve/rollback/revise; Sahiplen=operator_assertion; cross-account koruması) · `e41a676`(deterministik signal bridge: tag→canonical kural, verbatim operatör reason'ı, mekanik neden ÖĞRENİLMEZ, editDistance tek başına kural üretmez, engagement identity'ye yazılmaz; processFeedback best-effort + haftalık LLM'siz reconciliation, yeni cron yok; registry curator v1.1.0) · `856e114`(kaynaklı knowledge read model + GET /api/memory/knowledge) · `71f759a`(influence provenance: buildIdentityMemoryContext → GroundingContext.memoryFactIds → scores.groundingMemoryFactIds; yalnız aktif id, sahte influence imkânsız) · `6c573e9`(ProfileMemoryTab kaynaklı görünüm) · `7d048cc`+`16796f7`+`c466fd8`(testler) · docs. **Gate:** typecheck 0 · lint 0 err (5 pre-existing warn) · catalog OK · unit **1483/1483** (154 dosya) · build 0 · e2e **65/65** · migrate up-to-date (P1001 cold-start'lar retry ile aşıldı). Canlı (gerçek Neon): 16 aktif kural dürüst legacy rozetiyle, revise drawer, boş performans dersi dürüst; console 0, 1024–1920 taşma 0. Kanıt `shots/cemos-rebuild/faz2b/` (01/05/06/07/08/09 gerçek; 02/03/04 HARNESS — açık). Gerçek identity hafızasına test kuralı YAZILMADI; canlı ücretli LLM çağrısı YOK. **Yapılmadı:** 2C/2D/2E, Phase 3–4, gerçek X publish, canlı eval/kürasyon, push, deploy.
- **Faz: PHASE 2A ✅ KAPANDI (ADR-027 + ADR-028; 2026-07-16).** 7 yeni commit: `b300e84`(Phase-2 dilim haritası + 12-rol envanteri) · `67e5e49`(typed registry: 13 tanım, capability allowlist, fail-fast validate, curator sözleşmesi) · `0ad5f46`(executor + additive trace metadata + drawer, 29 test) · `3d4d13a`(OpportunityHandoff modeli + additive migration `20260716085323`, migrate deploy Neon, status clean) · `acf0591`(handoff servisi + 6 API route + üç yüzeyin gerçek handoff wiring'i + ReviewQueue focusSeed, 21 test) · `dff2479`(6 hermetik e2e) · docs commit'i. **Gate:** typecheck 0 · lint 0 err (5 pre-existing warn) · verify:catalog OK · unit **1448/1448** (151 dosya) · build 0 · e2e **58/58** · migrate status up-to-date. Canlı (gerçek Neon, dev 3050): handoff create→reload persist doğrulandı; Takvim prefilled panel; gerçek yüzeylerde console **0**, 1024–1920 taşma **0**. Kanıt `shots/cemos-rebuild/faz2a/` (01/03/05/06/07 gerçek Neon; 02 blocked + 04 seri + 08 trace HARNESS — açıkça fixture). **KRİTİK:** OpenRouter kredisi artık MEVCUT (ön-kontrol: limit 10, kalan ~7.70) → canlı ücretli çağrı BİLEREK yapılmadı; 2E kürasyon aktivasyonu kullanıcı onayı bekliyor. **Yapılmadı:** 2B–2E, Phase 3–4, gerçek X publish, canlı ücretli eval/kürasyon, push, deploy.
- **Faz (önceki): PHASE 1 ✅ KAPANDI** — 1A/1B/1B.5/1C/1C.1/1C.2/1D/**1D.1/1E/1F** tamam. Phase 2–4 YAPILMADI. Kapanış gate'i (2026-07-16): typecheck 0 · lint 0 err (5 pre-existing warn) · verify:catalog OK · unit **1398/1398** · build 0 · e2e **52/52** · `migrate status` up-to-date. Canlı browser: 1024/1280/1440/1920 taşma 0, console 0, hydration 0, sonsuz skeleton 0.
- **Faz 1D.1 ✅ (commit `8850faa`):** (a) settled-state kapanışı — eski kanıtlar sabit 2.6s beklemeyle skeleton yakalıyordu (Öğrenme gerçek settle ~20s = kanıt zamanlaması); ARTI gerçek kod açığı bulundu: TakvimTab'de /api/settings düşünce/boş dönünce `loading` sonsuza dek true (canlı repro: Neon cold-start 500) → dürüst ErrorState + hesap-reload retry. Semantik işaretler: `[data-skeleton]` + `[data-state=empty|error|blocked]`; kanıt/test artık networkidle DEĞİL semantik settled bekler; e2e "skeleton makul sürede KALKAR" regresyon bloğu. (b) Metrik sadakati — paylaşılan **`MetricStrip`** primitive (tek sessiz biçim); ViralRadar/SourceIntel/Öğrenme MetricGrid hero kartları + YouTubeTab elle satırı → MetricStrip. Kanıt: `shots/cemos-rebuild/faz1d1-success/` (01–10; settled süreleri loglu, gerçek Neon).
- **Faz 1E ✅ (ADR-025; commit'ler `8530245` db · `41c266d` adapters+prepare · `0a3de7b` atomic confirm · `18c8abc`+`bdec0f9` test):** PublishAttempt state machine — intent=YALNIZ prepared (server-side kalıcı; reload-persist canlı Neon'da doğrulandı); manuel onay TEK transaction (attempt+QueueItem+PublishLog+PublishedPost+UsageLog) + idempotent + contentHash/readiness yeniden-koşma kapıları; markManualPublished KALDIRILDI (PATCH bypass kapandı); XApiPublishAdapter kalıcı `payment_approval_required` blocked-external, ağ isteği YOK. **X API gerçek publish hâlâ BLOCKED-EXTERNAL (ödeme onayı yok); intent yayın DEĞİLDİR.** Migration `20260716072050_add_publish_attempt` additive, `migrate deploy` ile Neon'da, status clean.
- **Faz 1F ✅ (ADR-026; commit'ler `c9b2480` feat · `b00c88a` test):** üç sözleşmeli sağlık — infrastructure/pipelineFreshness/todayReadiness + topbar tek-actionable-sinyal (`src/lib/health/`); /api/health additive `contracts`; SystemHealthProvider tek fetch kaynağı; SystemTab üç bölüm + MetricStrip + bölüm-bazlı fail-soft + deep-link; "kuyruk tamamlandı" healthy/neutral, opsiyonel entegrasyon eksiği sistemi kırmızı yapmaz, secret değil yalnız ENV adı. 26 saf test (§6.6 matrisi). Kanıt: `shots/cemos-rebuild/faz1e1f/` (01–05; gerçek Neon: Sistem 3 bölüm dolu, prepared-reload persisted=true, X blocked-external).
- **Faz (önceki): Faz 1D ✅ TAMAM** (ADR-024) — **13 gerçek yüzey**: 8 host placeholder → gerçek + 5 REDESIGNED-ADVANCED arketipe. Kullanıcı-erişilebilir HostPlaceholder = 0. **KULLANICI ONAY KAPISI — Faz 1E/1F onaysız BAŞLAMA.** Commit'ler: `5a7f5bd`(Plan A) · `3077945`(Library B) · `2202869`(Profile C + /api/integrations) · `bfc2920`(News+YouTube D) · `512c373`(advanced E: ViralRadarScreen+SourceIntelScreen) · (+test/docs). 1D-A Takvim/Fırsatlar/Seriler (curation Faz-2-LLM, Fırsatlar eylem=nav-köprü); 1D-B Tümü(/api/library/search)/İlham(greenfield, sahte-AI-yok)/Öğrenme(env-gate); 1D-C Memory/Integrations(env-NAME-only, X-blocked); 1D-D youtube hero→quiet, news-pool+discovery zaten uyumlu; 1D-E flow-radar→ViralRadar(8-KPI kalktı, ScoreBars), source-intel→SourceIntel(ölü Tara/Flow'a-Gönder butonları kalktı, split-pane). Eski FlowRadarTab/SourceIntelligenceTab + hostScreens erişilemez (Faz 4 fiziksel emeklilik). Gate: typecheck 0 · lint 0 · unit **1358** · build 0 · e2e [gate] · migrate up-to-date (1D şema değişikliği YOK). Canlı: 13/13 render + 0 console error (gerçek Neon). Kanıt `shots/cemos-rebuild/faz1d/`.
- **Faz (geçmiş):** Faz 0/P0/1A/1B/1B.5/1C/1C.1 ✅ + **Faz 1C.2 ✅ TAMAM** (shell + DB kapanış: PG baseline, tek sistem-sağlığı, sidebar profil, kart eylem sadeliği, hesap popover, stale/readiness dili — ADR-023). Faz 1C.2 commit'ler: `65c621e`+`0acaaed` fix(db) baseline · `9d27285` fix(shell) · `4c56fea` fix(review) · docs(rebuild). Sıradaki: **Faz 1D** — ama önce **görsel onay kapısı** (ADR-021/022/023; onay gelmeden 1D yok).
- **DB (ADR-023, PG BASELINE UYGULANDI — P3019 çözüldü):** Tek postgres baseline `prisma/migrations/0_init` (71 tablo = 71 model; threadSegments + AuthAttempt dahil). `migration_lock.toml`=postgresql. 7 eski migration `prisma/migrations-archive/`'e taşındı (silinmedi). Canlı DB'ye baseline SQL ÇALIŞTIRILMADI; `migrate resolve --applied 0_init` ile bağlandı. **`migrate status` = up-to-date, P3019 YOK**; `_prisma_migrations` 0_init=applied. daily-queue gerçek Neon **200** (58 item; bugünün kuyruğu boş = dürüst empty state). Read-only doğrulandı (threadSegments=text, AuthAttempt + 3 index). Fresh-target apply çalıştırılmadı (izole PG hedefi yok; construction + "no difference" ile kanıtlı). Gelecekte `migrate deploy` artık kullanılabilir.
- **Tipografi (ADR-022):** Plus Jakarta Sans (Inter kalktı) + AppIcon tek-ikon primitive + özgün CemOS wordmark. Kanıt: `shots/cemos-rebuild/faz1c1-fidelity/`.
- **Branch:** `feature/cemos-rebuild` (`feature/ui-dark-redesign` HEAD `f8b72b8`'den). **Push YOK.**
- **Commit'ler:** `2cd7b24` Faz 0 · `fb90f33` faz1a · `c964979` handoff · `b68e32a` faz1b · `adae802`+`1146aec` handoff/1C-map · `feat(theme)` faz1b.5 dark.
- **Tema (ADR-020, kullanıcı 2026-07-15):** **desktop-only + tek tema DARK EDITORIAL** (light kararı supersede; terracotta korunur). Desktop kabul: 1024/1280/1440/1920, **1024–1920 taşma yok**; mobil best-effort. X API ödemesi ONAYLANMADI (Faz 1E intent-only).

### Faz 1B.5 tamamlanan (feat(theme)) — ADR-020
- **globals.css dark flip** (token İSİMLERİ sabit, DEĞERLER dark): bg-base `#101114`…surface `#191C21`…elevated `#20242A`; text-primary `#F2EFE8`/secondary `#B8B2A8`/muted `#918B82`; accent split solid `#A8481F`(+beyaz fg) / text-link-focus `#E4865E`; siyah gölge (glow yok); `color-scheme:dark`; `--ink` light RGB (scrollbar/chart-grid auto açık); status/chart dark-açık varyant. `chartColors.ts` dark.
- **Desktop genişlik varyantları:** `--content-reading/standard/wide` (960/1080/1280); AppShell ekran-bazlı uygular (Bugün/Toolbox/Profil=standard, Takvim/Kütüphane/advanced=wide); `PageScaffold width` prop eklendi.
- **`theme-tokens.test.ts` dark:** gerçek WCAG (metin ≥4.5:1, focus ≥3:1) + dark-tema-kilidi (yüzey relLum düşük/metin yüksek) + açık-tema literal regression (#f7f6f2…) + mor/neon ban. `:focus-visible` accent-text (dark görünür).
- **Doğrulama:** typecheck ✓ · lint 0 err ✓ · unit **1251/1251** ✓ · build ✓ · e2e **22/22** ✓ · gerçek app dark 1280/1440/1920/1024 — Bugün(gerçek kart)/Plan(wide)/Profil menü/Giris, **1024–1920 taşma 0**, console **0 error** (`shots/cemos-rebuild/faz1b5/`).
- **Kapsam notu:** MobileNav + ≤640 responsive KORUNDU (best-effort); silinmedi. Faz 1C–1F UI yalnız desktop tasarlanır (drawer=side panel, segment editor desktop, 1024 kullanılabilir; 390 screenshot YOK). Docs (06/05/03/FINAL) desktop-only + dark'a güncellendi.

### Faz 1B tamamlanan (commit b68e32a) — ADR-019
- **Yeni IA:** `navConfig` = PRIMARY_AREAS(bugun/plan/kutuphane) + ADVANCED_TABS(5, parentArea=plan) + UTILITY_TABS(toolbox) + PROFILE_TABS(5) + AREA_ALIASES + güncellenmiş TAB_ALIASES. Yardımcılar: `highlightAreaForTab`, `resolveAreaForTab`, `isAdvanced/Utility/ProfileTab`, `advanced/profileMeta`, `labelForTab`, `subTabsOfArea`, `allNavigableTabs`.
- **Shell:** `Sidebar` dar 232px (collapse KALDIRILDI), `ProfileMenu.tsx` (YENİ, yukarı popover 5 yüzey+Çıkış), `MobileNav` 3+1 + Profil sheet, `AppShell` mod modeli + workspace `SubNav` (Plan/Kütüphane + Profil) + 960px + breadcrumb, `screenRegistry` yeni host id'leri.
- **Host iskeleleri:** `host/HostPlaceholder.tsx` + `host/hostScreens.tsx` (plan-*/lib-*/profile-memory/profile-integrations — dürüst placeholder, Faz 1D doldurur). `lib/auth/clientLogout.ts`.
- **Store:** v9 migration (yalnız ABSORBED activeTab taşınır; advanced/utility/profil pass-through), `XAGENT_STORE_NAME` const (invariant "xagent-store").
- **Doğrulama:** typecheck ✓ · lint 0 err ✓ · unit **1240/1240** ✓ · build ✓ · e2e **22/22** ✓ · gerçek app 1280/390 faithful (Bugün/Plan/Profil menü/mobil 3+1), 320-1440 taşma **0**, console **0 error** (`shots/cemos-rebuild/faz1b/` — bugun-1280, plan-1280, profilmenu-1280, bugun-390).
- **E2E harness sertleştirme:** `global-setup` warmup+retry (soğuk compile), `playwright.config` workers:1/retries:1/expect 10s, Ctrl+K hydration guard. Nav helper (`helpers/nav.ts`) sınıf-bazlı yeniden yazıldı.
- **ABSORBED ekran dosyaları SİLİNMEDİ** (ViralLibraryTab, DailyQueueTab, KeywordLibraryTab, PromptKutuphanesiTab, PatternLibraryTab, InstagramTab, LearnDashboardTab) — registry'den çıktı, kullanıcı erişimi kesildi; emeklilik Faz 4. Faz 1D `lib-tumu`/`plan-seriler` bunların işlevini birleştirecek.

### Faz 1A tamamlanan (commit fb90f33)
- **Auth:** `src/proxy.ts` (Next 16 gate), `src/lib/auth/session.ts`+`throttle.ts` (+test), `src/app/giris` (native form→303), `src/app/api/auth/{login,logout}`, `ACCESS_PASSWORD_HASH`+`SESSION_SECRET` (requiredSecrets+.env.example), `scripts/hash-access-password.ts`, `AuthAttempt` Prisma modeli (additive).
- **Design system:** `globals.css` açık editorial + terracotta (token isimleri sabit), `chartColors.ts`, `theme-tokens.test.ts` (koyu-literal yasağı + gerçek WCAG kontrast), `--content-max:960px` token (uygulaması Faz 1B shell'de).
- **E2E:** `global-setup.ts` (login→storageState), `access-gate.spec.ts`, `playwright.config.ts` (gate env + reuseExistingServer:false).
- **Doğrulama:** typecheck ✓ · lint 0 err · unit **1229/1229** ✓ · build ✓ · e2e **20/20** ✓ · gerçek app açık tema render + /giris (console **0 error**, 1280+390: `shots/cemos-rebuild/faz1a/`).
- **[USER] gerekli:** `prisma db push` (AuthAttempt tablosu — yoksa throttle fail-open) + prod'da `ACCESS_PASSWORD_HASH`/`SESSION_SECRET` env (yoksa prod kapı fail-closed `/giris?setup=1`). E2E hash sabit salt "e2e-test-pass" içindir; prod'da `hash-access-password.ts` ile gerçek üret.

## Tamamlanan (Faz 0 kabul)

| Madde | Durum | Kanıt |
|---|---|---|
| Git güvenliği + branch | ✅ | `feature/cemos-rebuild` |
| Baseline (test/typecheck/lint/build/e2e/catalog) | ✅ TAM YEŞİL | 00-BASELINE §1: 1198 unit + 17 e2e + build + tc + lint + catalog |
| Canlı deploy read-only denetimi | ✅ | 00-BASELINE §2; `shots/cemos-rebuild/baseline/` (auth'suz erişim doğrulandı) |
| Resmî X API araştırması | ✅ | ADR-015 / 02 §5: REQUIRES-USER-PAYMENT-APPROVAL |
| Tasarım araştırması + accent | ✅ | 06 §2 terracotta hipotez + indigo alternatif |
| 10 Faz 0 belgesi | ✅ | `docs/cemos-rebuild/` (00,01,02,03,04,05,06,DECISIONS,IMPLEMENTATION-STATE,FINAL-IMPLEMENTATION-PLAN) |
| Tasarım onay paketi (mockup) | ✅ | `shots/cemos-rebuild/design-approval/` (12 ekran HTML + render) |
| Tam ekran sınıflandırma (SPEC-PENDING yok) | ✅ | 04 (ABSORBED/REDESIGNED-ADVANCED) + 05 (31 yüzey) |

## Baseline test sonuçları (gerçek)

```
typecheck=0  lint=0  verify:catalog=0
test: 134 dosya, 1198/1198 geçti (6.65s)
build: exit 0, 20.4s, 44 statik sayfa
e2e: 17 passed (41.9s)  [edit-gate spec shell-smoke:64 dahil]
```

## Browser kanıtları

**Baseline (canlı, koyu dashboard — değiştirilecek):** `shots/cemos-rebuild/baseline/prod-1280-morning-styled.jpeg`, `prod-390-morning.jpeg`, `prod-1280-morning.jpeg`.

**Tasarım onay paketi (YENİ açık editorial, terracotta) — `shots/cemos-rebuild/design-approval/` (12 mockup HTML + `cemos-ds.css` + 12 render):**
- `render-01-shell-bugun-1280` — desktop shell + Bugün karar kuyruğu (hero)
- `render-02-plan-takvim-1280` — Plan/Takvim (ay yoğunluk + hafta önizleme)
- `render-03-plan-firsatlar-1280` — Plan/Fırsatlar (seçilmiş fırsatlar)
- `render-04-plan-seriler-1280` — Plan/Seriler (seri DNA)
- `render-05-kutuphane-tumu-1280` — Kütüphane/Tümü (birleşik arama)
- `render-06-profil-sistem-1280` — Profil/Sistem (ADR-014 3-katman sağlık)
- `render-07-advanced-flowradar-1280` — REDESIGNED-ADVANCED Viral Radar araştırma
- `render-08-mobil-bugun` — mobil Bugün (thumb-CTA, bilinçli-aksiyon)
- `render-09-mobil-kutuphane` — mobil Kütüphane/Öğrenme
- `render-10-giris-1280` — erişim kapısı `/giris`
- `render-11-durumlar-1280` — durum galerisi (boş/hata/stale/blocked-external)
- `render-12-bugun-drawer-1280` — Bugün taslak detay drawer (kaynak/doğrulama/sinyal/audit)

Doğrulanan (görsel incelendi): 01, 06, 08, 11, 12 — hepsi açık editorial, terracotta accent, koyu/mor/KPI-hero/dekoratif-chart YOK; paylaşılan `cemos-ds.css` ile tutarlı. Kalan 7 aynı CSS'i kullanır.

## DB / migration durumu

- Faz 0: DB değişikliği YOK.
- Faz 1 additive planı (destructive DEĞİL, `prisma migrate diff` ile kanıt): `PublishAttempt` modeli, `QueueItem.threadSegments`, auth throttling tablosu. `prisma db push` yalnız kullanıcı onayıyla.

## Dış blocker'lar (BLOCKED-EXTERNAL)

- **X API gerçek publish:** pay-per-use (senaryo tablosu ADR-015: %0 link $2.25–3.60 / %50 link $16.13–25.80 / %100 link $30–48 aylık, 5-8 post/gün) → **Ali Cem ödeme onayı ALINMADI** (kullanıcı kararı 2026-07-15). Faz 1E yalnız adapter contract + PublishAttempt + IntentPublishAdapter + dürüst intent fallback; `XApiPublishAdapter` gerçek bağlantı BLOCKED-EXTERNAL.
- Vercel Deployment Protection / firewall (kullanıcı).
- `ACCESS_PASSWORD_HASH`/`SESSION_SECRET`/`CREDENTIAL_ENC_KEY` prod env (kullanıcı).
- Meta `instagram_basic`+`business_discovery` (Faz 3).
- OpenRouter kredisi (Faz 2 canlı eval).

## Açık riskler / uygulama sırasında doğrulanacak

1. `blocked` readiness kriter seti — canlı queue verisiyle ayarlanıp dondurulacak.
2. Kuyruk endpoint teyidi (1C başında hangi route MorningDashboardTab besliyor).
3. `processFeedback` union'ı (`approved` kabulü) + `publishService.ts:156` literal cast.
4. Playwright `webServer.env`/globalSetup/`reuseExistingServer` (auth E2E).
5. Türkçe glyph per-weight (Inter/Newsreader), üretim hex canlı kontrast re-verify.
6. Accent kullanıcı kararı (terracotta vs indigo) — onay kapısı.

## Sonraki kesin iş — Faz 1C (Bugün + gerçek readiness)

FINAL-IMPLEMENTATION-PLAN §1C + master prompt Faz 1C. Özet:
1. **Kuyruk zinciri teyidi:** hangi route `MorningDashboardTab`'i besliyor koddan doğrula (`/api/growth/daily-queue`).
2. **`readinessService.ts` (pure):** her çağrı `editedContent ?? content`; persist EDİLMEZ; judged=false/skor eksik/legacy → ASLA ready; `ready|needs_edit|blocked`; blocked YALNIZ ciddi doğruluk/güvenlik/policy/kaynaksız-somut-iddia; stil → needs_edit; hesap-bazlı soru-CTA/emoji; Türkçe-dışı token oranı + allowlist (kelime-özel Trajectory hack'i YOK).
3. **`threadSegments` additive (QueueItem, Zod typed):** basit segment editörü; yapısal segmentsiz thread fail-closed; metindeki "1/" kanıt SAYILMAZ.
4. **`whyToday.ts` + 5-durum doğrulama:** verified/partially_verified/source_available/unverified/stale; kart ve drawer AYNI sonucu gösterir; `SourcePost.scannedAt` fact-check tarihi DEĞİL.
5. **`publishService`:** yayın anında readiness re-run; eski zorunlu kozmetik edit-gate KALDIR; needs_edit→edit_required; blocked→422 Türkçe neden.
6. **UI:** yeni DraftReviewCard + detay drawer; ReviewQueue/MorningDashboardTab/OperatorReadinessGate yeni sisteme; **intent-only CTA dili korunur** ("X'te aç", ADR-017).
7. **Fixtures:** 6 kötü taslak (yabancı sızıntı/soru-CTA/emoji/kaynaksız-iddia/yapısız-thread/düşük-Türkçe) ready OLAMAZ; iyi fixture ready. Additive Prisma migration `prisma migrate diff` ile destructive-değil kanıtı (production'a UYGULAMA).

Gate: test/typecheck/lint + build/playwright/1280·390/console-0. Ayrı commit + bu dosyayı güncelle.

### Faz 1C kod haritası (keşif ajanı çıktısı — turnkey base)

**Veri zinciri:** `MorningDashboardTab` → `useDailyQueueData` hook (`src/components/morning/useDailyQueueData.ts`) → `GET /api/growth/daily-queue` (`src/app/api/growth/daily-queue/route.ts`) → `ReviewQueue` → `DraftReviewCard` (+ `OperatorReadinessGate`, `MorningHeroStats`). PATCH `src/app/api/growth/daily-queue/[id]/route.ts` (`{content}` kaydet · `{status:"manual_published"}` → `publishService.markManualPublished`).
- Route her item'e `scoresParsed` ekler (route.ts:105-143): `judged` (telemetry.judged), `turkishNaturalness`, `sourceFaithfulness`, `riskScore`, `hookStrengthScore`, `personaMatchScore`, `noveltyScore`, `leaks`, `leakCount`, `ctaPresent`, `payoff`, `publishScore`, `isEstimatedScore`.
- Client `MorningDraft`/`MorningDraftScores` tipi `useDailyQueueData.ts:7-36` (route'un ALT KÜMESİ).

**QueueItem (`prisma/schema.prisma:103-133`):** `content`, `editedContent?`, `status String @default("new")` (ENUM DEĞİL; new/draft/needs_edit/approved/scheduled/rejected/published/manual_published), `scores String` (JSON — `judged` yalnız `telemetry.judged`; ayrı judged kolonu YOK), `lintReport String?`, `candidatesJson`, `sourcePostId?`, `newsItemId?`, `draftType @default("TWEET")`. **`threadSegments` YOK** → additive eklenecek (Zod-typed JSON String kolon; migrate diff kanıtı, apply YOK).

**Kaldırılacak edit-gate:** server `publishService.ts:97-104` (`markManualPublished` → `edited===original||!edited` ise `throw "edit_required"`); literal cast `:156` (`as "grafikcem"|"maskulenkod"`); UI `DraftReviewCard.tsx:115` (`isEdited`), `:166` (`if(!isEdited)return`), `:398` (`disabled`), `:417-421` (uyarı metni). `extractGateNotes` (:32-44) `lintReport.issues[code==="quality_gate"]` okur.

**`applyQualityGate` (`src/lib/services/scoreSignals.ts:68-90`):** `new|needs_edit`; tetik = high leak / (judged && turkishNaturalness<`TURKISH_NATURALNESS_MIN=55`) / lint `banned_phrase|question_cta`. `SubSignals` (:10-20). Yazım `draftService.ts:254-346` (status:needs_edit→:308, scores JSON→:311-342). `Leak` (`leak-detector.ts:29-34` kind/severity low|med|high). Lint kodları (`heuristics.ts`): banned_phrase/question_cta/char_limit/emoji_density/hashtag_density/sales_smell.

**Hesap politikaları (`accounts.ts:40-56`):** yapısal emoji/soru-CTA alanı YOK — free-text `toneRules/formatRules/forbiddenRules`. grafikcem: emoji YASAK + soru-CTA yasak (:83-84,88-89); maskulenkod: emoji kuralı yok. Makine karşılığı lint `question_cta`/`emoji_density`. `AccountHandle="grafikcem"|"maskulenkod"`; `effectiveMaxChars`/`resolveFormatTier` mevcut.

**Doğrulama verisi:** `SourcePost.scannedAt` VAR (tarama zamanı — fact-check DEĞİL) `schema:98`. `NewsItem.sourceVerification` (single_source|official_only|editorial_confirmed|multi_source_confirmed) `:393` + `whyPeopleCare` `:389`. `classifySourceVerification` (`src/lib/news/sourceVerification.ts:99-116`). **whyToday/partially_verified/factCheck HİÇ YOK** (grep 0) → sıfırdan.

**Türkçe util:** `src/lib/utils/textSimilarity.ts` → `normalizeTurkish` (ğüşıöç→gusioc, punct strip), `calculateJaccard/Levenshtein`, `isNearDuplicate`. Dil-tespit util YOK.

**Mevcut readiness:** `operatorReadinessService` (`src/lib/services/operatorReadinessService.ts`) = INFRA/pipeline readiness ("bugün üretebilir mi") — per-draft readiness'ten AYRI. Yeni `readinessService.ts` (per-draft) isim çakışması YOK.

**Yeni `readinessService.ts` tasarımı (pure, self-contained input — QueueItem'dan adapte edilir):** `state: ready|needs_edit|blocked`; text=`editedContent?.trim()||content.trim()`; persist YOK. **BLOCK** (öncelikli): over-maxChars · `riskScore>=RISK_BLOCK_MIN(75, provisional)` · kaynaksız somut istatistik iddiası (`%`/`$₺`/`\d+ kat|milyon|milyar|bin`/`(19|20)\d{2}` VE !hasSource) · security lint kodu. **NEEDS_EDIT:** !judged (fail-closed) · turkishNaturalness null/legacy · judged&&<55 · high leak · yabancı-dil sızıntısı (İngilizce fonksiyon-kelime sayısı≥2, allowlist'li — kelime-özel hack YOK) · lint banned_phrase|question_cta · grafikcem&&emoji(`\p{Extended_Pictographic}`) · thread&&!threadSegments (yapısal; "1/" kanıt DEĞİL) · segment char/limit ihlali · status==="needs_edit". Aksi → **ready**. Eşikler **provisional** — canlı queue verisiyle DONDURULACAK (risk#2; OpenRouter-402 canlı üretime bağlı → o kalibrasyon dış-bağımlı). Fixtures `__fixtures__/badDrafts.ts` (6: yabancı-sızıntı/soru-CTA/emoji/kaynaksız-iddia/yapısız-thread/düşük-Türkçe) hiçbiri ready; iyi fixture ready → `readinessService.regression.test.ts`.

### Faz 1C TAMAM — dabb0bf (referans shell) + ba0068b (readiness wiring + desktop review UI)

**Çekirdek (bbda80c, pure):** `readinessService`/`threadSegments`/`whyToday` (+fixtures) — 35 test. Değişmedi, bağlandı.

**Referans hizalama (dabb0bf, ADR-021):** inverse/peach açık-ada token'ları (+@theme mirror, gerçek WCAG, açık-ada kilidi); `Surface`/`InverseCard`/`PeachCard` primitive (ton renkleri `--sf-*` ile çocuklara); Sidebar hesap bağlam kartı + **nötr grafit aktif nav** (terracotta yalnız ikon); segmented pill SubNav; `PageHeader size="hero"`; AppShell Plan/Kütüphane hero başlık→subnav→gövde. Canlı 1280 (shots/faz1c-refA).

**Readiness wiring (ba0068b, 1C-d):** `readinessAdapter.ts` (saf `readinessInputFromQueueItem`/`assessQueueItemReadiness`, savunmacı default'lar, +11 test); `publishService.markManualPublished` **düzeltilmiş sözleşme** — kozmetik `edited!==original` edit-gate KALKTI → yayın anında `editedContent ?? content` üzerinde `assessReadiness` RE-RUN: `blocked`→`readiness_blocked`(422), `needs_edit`→`edit_required`(422), `ready`→yayın (düzenleme ŞART değil); "edited" öğrenme sinyali yalnız gerçekten değiştiyse. daily-queue GET payload += `readiness`+`readinessInput`(client canlı)+`whyToday` (sourcePost/newsItem include); `[id]` PATCH += `threadSegments`(Zod) + readiness→422 Türkçe neden; mark-published route aynı eşleme.

**Desktop review UI (ba0068b, 1C-e):** `DraftReviewCard` ton=readiness (ivory ready/peach needs_edit/koyu blocked, `data-readiness`) + **intent-only "X'te aç"** + "Paylaşıldı"(yalnız ready) + canlı readiness; UI edit-gate + "değiştirmeden yayınlayamazsın" KALKTI; A/E/J/K. `ThreadSegmentEditor` (ekle/böl/birleştir/sırala + char count → `serializeThreadSegments` → PATCH). `DraftDetailDrawer` (koyu side panel: whyToday/doğrulama/kaynak scannedAt="tarandı"/ayrışık sinyaller/readiness/model katlanmış; kart ile AYNI sonuç). `readinessMeta.ts`; ReviewQueue readiness noktaları; `useDailyQueueData` (+`saveSegments`, kaydetmede canlı readiness yeniden-hesap). E2E `bugun-queue.spec.ts` (hermetik route-mock, DB-bağımsız, 5 test) + eski kozmetik edit-gate E2E'si kaldırıldı.

**Gate:** typecheck ✓ · unit **1308/1308** ✓ · lint 0 error ✓ · build ✓ · **e2e 26/26** ✓. Kart tonları mock-preview ile canlı doğrulandı (shots/faz1c-cards: ready ivory / needs_edit peach / blocked / thread editör / drawer / gerçek Bugün DB-blocker ErrorState).

**⚠ USER-DB-ACTION (uygulanmadı, kısıt — BLOCKED-EXTERNAL değil):** `QueueItem.threadSegments` kolonu canlı DB'de YOK → daily-queue gerçek istekte "column does not exist" 500 (Bugün graceful ErrorState, sessizce gizlenmez). Kullanıcı additive migration'ı uygulamalı: **`npx prisma migrate deploy`** (veya dev'de eşdeğeri) → gerçek veri akar, Bugün kartları canlı render olur.

**Sıradaki: Faz 1D ÖNCESİ GÖRSEL ONAY KAPISI (ADR-021, tek kapı).** Kullanıcıya gerçek uygulamadan shell/sidebar-account/Plan-Kütüphane + kart mock-preview'ları sunulur; soru: "referans yapı/his yeterince yakalandı mı; bu archetype ile Faz 1D'ye devam?" **Onay gelmeden Faz 1D'ye geçilmez.** (Faz 1D: 6 host + 5 advanced araştırma yüzeyi archetype'a taşınır.)

## Tekrar edilmemesi gereken başarısız denemeler

- Playwright MCP `file://` engelli → mockup'lar için yerel HTTP sunucu (`scratchpad/serve.js`, port 4599) kullanıldı.
- Canlı deploy soğuk cache'te CSS `ERR_CACHE_READ_FAILURE` → stilsiz render; reload sonrası düzeldi (app hatası değil).
